# Tina 可执行 Backlog

本文件是未完成工作的唯一明细表。Roadmap 只表达优先级窗口，ADR 只表达决策，不在多个主题文档
重复维护任务状态。`Done` 仅保留已关闭任务的证据索引，不再作为活跃完成度来源。

## 状态与证据

任务状态只使用：`Planned`、`InProgress`、`Blocked`、`Done`、`Deferred`。

证据强度与任务状态分开记录：

- `Unit`：模块单元测试；
- `Integration`：跨模块测试；
- `Smoke`：sample/CLI 生命周期与结构化输出；
- `Visual`：截图、像素或人工视觉；
- `Platform`：指定 OS/toolchain/backend 门禁；
- `Manual`：Narrator/Inspect 等不能由自动 gate 替代的人工验收记录；
- `Consumer`：脱离源码树、只通过安装/package 边界构建的外部调用方门禁；
- `Benchmark`：固定 workload/fingerprint/baseline 与多进程统计协议产生的可复现性能证据。

`Done` 必须满足验收条件并留下对应证据；“已经写代码”不自动等于产品门禁完成。

## 开工前先验证本表（2026-08-28 校准）

**本表会滞后于代码。** 多个并行会话在同一仓库推进，提交了实现却未同步任务行；2026-08-28 一次校准发现三处「说待做、其实已完成」和两处「说有证据、其实没有」，其中一条直接导致选题浪费了一轮。

因此 `AGENTS.md` 的事实优先级（当前源码 / CMake / 实际运行结果 > Accepted ADR > 主题文档）对本表同样适用：**动手前先 grep 该任务声称要实现的字段或调用点**，确认它是否已存在。判据参考：

- 「待做 X」→ 搜 X 的关键类型/函数名。零命中才是真的待做。
- 「有单测」→ 直接查 `tests/*/CMakeLists.txt` 是否列出该文件，而不是相信描述。
- 「容量/数值为 N」→ 查字面量。后续功能常把它改了却不回写本表。
- 与本表冲突时以代码为准，并**顺手更正本行**，注明关闭它的 commit。

本轮已校准：`MOBILE-001` 切片 A②（多点触控已完成，`4ec987a9`）、`ASSET-ID-001`（派生已替换，`3499f8ee`）、`FX-ASSET-001`（authoring 单测不存在）、`2D-EDITOR`（DisplayList/draw-call 容量已被 layout debugger 取代）、`NET-001`（范围已扩到 TCP/TLS/HTTP）、`design-freeze.md` 的 ADR 0031/0032 行。

## Now

| ID | 状态 | 优先级 | 工作 | 依赖 | 验收条件 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| RENDER-POST-001 | InProgress | P1 | 关闭后处理链的「已发布无生产者」缺陷：补设备 SPI、Null 真实消费、bgfx fail closed、零覆盖补测试 | 无 | **先校准：** 「缺 HDR / tone mapping / bloom / fog / decal / RenderTexture / offscreen pass / 后处理链」这一描述**已过期**（`6800e13f` 已提交这些契约）。真正的问题更严重：整条链**没有任何后端消费** —— `IRenderDevice` 无 render-texture SPI（`RenderErrors.hpp` 为它预留的 12 个码中 8 个零使用）、Null/bgfx 对 `frame.postProcess` **零命中**（设完整 HDR 链会得到 `success()` 且画面无变化、无错误）、测试覆盖为零。这重演了 ADR 0034 `nativeBindingRevision` 的同类问题。本轮：①新增 `GpuRenderTextureId` 与四个默认 fail-closed 的虚方法（`createRenderTexture`/`destroy`/`set`/`clearRenderTextureBinding`），binding key 0 保留给主 surface；②Null 成为**真实消费者**：维护 binding 表、校验 chain、构建扩展 schedule、对 1×1 探针像素执行共享 reference math，并记入新增计数器 `liveRenderTextures`/`postProcessChainsExecuted`/`postProcessPassesPlanned`；③bgfx 对非空 chain 显式返回 `RenderTextureUnsupported`（GPU 实现属 `RENDER-POST-002`）。**测试抓出两个真实缺陷并已修复：** ①`ToneMappingDesc::operation` 默认 `AcesFitted` 使 `enabled()` 对**默认构造**的 chain 返回 true —— 而每个 `RenderFrame` 都带一个，故 bgfx 的 fail-closed 本会拒绝**所有**帧；tone mapping 是 offscreen 的一个阶段而非独立开关，已从 `enabled()` 移除；②`buildRenderPipelineSchedule` 未检查 `enabled()`，对未启用的 chain 仍规划一个写主 surface 的 tone-mapping pass，已加早退。**2026-08-31 证据：** 新增 `RenderPostProcessTests`（33 个：RenderTextureDesc 边界、chain 校验 fail-closed、schedule 顺序/容量/边界值可用、三个 operator 的单调性与非有限输入）与 8 个 `NullRenderDevicePostProcessTest`；`tina_render_scene_tests` **85/85**、`tina_tests` **463/463** 全绿零 warning。`tina_sample_2d --frames=300` 与 `tina_sample_3d --frames=30` 均 exit 0 且证据数字未变（schema 29：`submittedRenderFrames=304`、`spritesPerFrame=26`、`navigationBlockedCells=11`、`fxInitialFingerprint` 一致；schema 16：transparent draw=`2`、sort checksum=`6050478067199599831`、`renderResourceLedgerBalanced=true`）—— `submittedRenderFrames=304` 正是 bgfx 未误伤主路径的证据 | Unit + Smoke |
| SCENE-QUERY-001 | InProgress | P2 | Scene 通用性收口：metadata 值筛选查询、forward_range 契约固定与 name 往返覆盖 | 无 | **先校准：** 「World 只有逐类 set/get/clear、缺 typed query/runtime name/tag/layer/group」这一描述**已过期** —— `WorldReadableComponent`（17 类型）、`get/has/view/query`、`WorldView` 惰性 range + `each()`、`EntityMetadata` 六个 setter 全部**已实现且有实现体**，不是待做项。本轮补三处真实缺口：①新增 `EntityMetadataFilter` 与 `viewWhere/queryWhere`，按 tag/layer/group **值**筛选；三项各为 `optional`，未设置不参与筛选 —— 0 是三者的合法默认值，「筛 0」与「不筛」必须可分别表达，否则默认值永远查不出来。筛选先判组件再取 metadata，`contains()` 同样遵守 filter（这正是做成声明式而非 `each()` 内手写 `if` 的原因：后者无法被 `contains()`/`empty()` 复用）。②`WorldView` 的 `forward_iterator_tag` 此前只是声明，现由 `static_assert(std::forward_iterator/forward_range)` 固定并加多趟遍历测试。③**capture 的 name 往返此前零覆盖** —— `World2DSnapshot.cpp:571` 已在读 `runtimeName()`，但 round-trip fixture 从未设名，byte-equality 断言恰好绕过；现加入 ASCII、多字节 UTF-8 与故意不命名的实体，并做过注入验证（去掉 name 写入该测试立即失败并指名 `scene-root`）。同时补 metadata 槽位复用重置、`setMetadata` 失败原子性、63-byte 边界值可用等（原仅 2 个测试）。**不做**反向 name/tag 查找：需维护随 mutation 同步的反向索引（新失效面），而 `viewWhere` 已够用，且 authored name 查找归 `World2DSceneIndex`。ADR 0030 的 name 归属矛盾已补「后续修正」节（原 D2 拒绝把 name 放进 `EntityRecord`，实现相反；原因是索引只是 instantiate 快照，运行中改名会让 capture 写回旧名）。**2026-08-31 证据：** 新增 8 个 World 测试 + header isolation 断言；8 个受影响 TU（含 `ExtractRenderScene`/`World2DSnapshot`/`Scene2DRuntime`）在 `/W4 /WX` 零 error 零 warning。首轮因另一会话在途编辑 `SkinnedMeshPayload.cpp`（调用未定义的 `bytesAreZero`）阻塞 `tina_asset_format`，只能手工链接既有 `.lib` 验证；该文件修复后已按正式 preset 复跑：`tina_scene_tests` **181/181** 全绿（含我的 8 个新测试），同轮复验 `tina_math_tests` 114/114、`tina_save_tests` 44/44、`tina_editor_tests` 146/146 均未回归 | Unit |
| MATH-001 | Done | P1 | 建立 `Tina::Math` 作为引擎唯一几何类型定义点，并删除全部重复实现 | 无（只依赖 Core） | header-only `Tina::Math` 提供 `Vec2/3/4`、`Quaternion`、列主序右手系 `Mat4`、`Aabb2/3`、`Rect`、`Sphere`、`Plane`、`Ray`、`Frustum` 与几何查询；失败用 `optional`/`bool`，不占 `ErrorDomain`/`MemoryTag`。**已删除且无别名残留：** `Scene::Vec3`/`Vec2`/`Quaternion` 及其 8 个自由函数、`Physics2D::PhysicsVec2`（52 处引用）、`RenderScene.cpp` 的第三份 `Vector3`/`Quaternion` 与手写 TRS 矩阵、`ExtractRenderScene.cpp` 的 `WorldBoundingSphere` 与球-视锥剔除、`Animator3D.cpp` 的 4x4 乘法与 `shortestPathSlerp`、`PhysicsNavigationSync2D.cpp` 的旋转保守 AABB、`Product3DUI.cpp` 的局部 `Rect`。按 ADR 0035 D7 逐字保持算式与中间精度，四个数值等价性回归把被删实现原样复制进 `tina_math_tests` 并逐元素比对。刻意保留：`UI::UILogicalRect`（独立 logical-pixel 语义）、`bgfx` 后端的 `bx::Vec3`（私有实现细节）、`RenderScene.cpp` 的 float 精度 `sphereIntersectsPerspectiveCamera`（同时返回 view depth，改精度会改 draw count）。**2026-08-31 统一 gate（product-2d 常驻 build tree）：** `tina_math_tests` 114/114、`tina_save_tests` 44/44、`tina_tests` 455/455、`tina_scene_tests` 173/173、`tina_render_scene_tests` 54/54、`tina_physics2d_tests` 76/76、`tina_asset_tests` 323/323、`tina_navigation2d_tests` 16/16 全绿，零 warning。`tina_sample_2d --frames=300` exit 0 且 D7 数字未变（schema 29、`navigationSolidTileCells/BlockedCells=11/11`、`navigationPhysicsBlockerUpdates=5`、`navigationBasePathCost=40`、`spritesPerFrame=26`、`fxInitialFingerprint=f42636648bfc4ec027edacd1f2c7e803`）；`tina_sample_3d --frames=30` exit 0 且 schema 16 保持（static/skinned mesh=`2/1`、transparent draw=`2`、sort checksum=`6050478067199599831`、point/spot 剔除=`2/1`、`renderResourceLedgerBalanced=true`——后者证明 `Mat4::fromTrs` 替换后 3D 变换与骨骼调色板无漂移）。`Ray` 的生产者由 `EDITOR-PICK-RAY-001` 补齐。**状态更正（2026-09-01）：** 本行此前标 `InProgress`，而 [design-freeze.md](design-freeze.md) 的 ADR 0035 行同期标 `Implemented`，两处口径矛盾。核实后以 `Done` 统一：七个公开头、全部调用点迁移、重复定义删除与四个数值等价性回归均已落地，2026-08-31 统一 gate 全绿且 2D/3D 产品证据数字未变，唯一的「待」项（`OBB`/`Mat3`/SIMD）在 ADR 0035 与本行都**明示不在范围**，不构成未完成工作；`Ray` 的生产者亦已由 `EDITOR-PICK-RAY-001` 补齐 | Unit + Integration + Smoke |
| EDITOR-PICK-RAY-001 | Done | P2 | Editor 3D 视口单击拾取改用世界空间 ray，并修复两个既有拾取缺陷 | MATH-001 | 新增 `Tina::Editor` 的 `EditorViewportPick`（`editorViewportPickRay` + `pickNearestViewportCandidate`），固定 64 容量、失败用 `optional`。**修复的两个真实缺陷：** ①`MeshRenderer3D::localBounds` 的 `centerX/Y/Z` 此前**从未被读取**（`rg` 仅一处命中且只取 `.radius`），热区固定在 transform 原点，故 authored bounds 偏离原点的 mesh（glTF 导入常态）拾取位置错误；②拾取用屏幕 2D 包围盒 + **最小面积**决胜，无深度概念，点击重叠物体命中「屏幕上更小的」而非「更靠前的」。现改为最近命中优先、距离相等按 stable ID 升序。**边界划分：** 单击 3D 走 ray，marquee 框选保持屏幕矩形（框选本就是 2D 语义），2D 单击不变（正交投影无深度歧义），preselection 高亮框仍用屏幕矩形。顺带删除 `editor_app` 重复的 `DegreesToRadians`/`RadiansToDegrees`（改用 `Math::` 同值常量）并把 4 处硬编码 `55.0F` FOV 收敛为 `ViewportPerspectiveFovDegrees`——拾取射线必须与投影同源。**2026-08-31 证据：** `tina_editor_tests` 146/146（含新增 `EditorViewportPickTest` 16 个 + header isolation）、`tina_editor_app_tests` 23/23；`TinaEditor --auto-demo --frames=70` 2D 与 `--workspace=3d` 各一次均 exit 0、`status=ok`、`automaticAuthoringStage=57`（等于 `AutomaticAuthoringStageCount`，非 stall）、`selectionVerified=true`、gizmo/marquee `rejects=0/0`，3D 侧 `gpuViewportMeshes=3`。**状态更正（2026-09-01）：** 本行此前标 `InProgress`，但本行**不存在任何「待」项** —— `EditorViewportPick` 已落地、两个真实拾取缺陷已修、Unit 与 Editor Product 证据齐全，[design-freeze.md](design-freeze.md) 的 ADR 0035 行亦记「`Ray` 的生产者已由 `EDITOR-PICK-RAY-001` 补齐」。故改 `Done`；边界划分（marquee 与 2D 单击保持屏幕矩形语义）是刻意结果而非缺口 | Unit + Smoke |
| GAMEPLAY-TOOLING-001 | InProgress | P1 | 建立 `Tina::Gameplay` 时序工具层：Timer/Scheduler、Tween/Sequence/Parallel/Repeat/Delay/Easing 与 scoped signal | MATH-001（公开头命名 `Math::Vec2/3/4`） | 新增 `include/tina/gameplay`：`Easing`（28 曲线，端点精确、非有限输入按已完成处理）、`Scheduler`/`TimerId`（`Repeat`、per-timer `ignoresTimeScale`、暂停、`remaining()`）、`Action`/`ActionRunner`/`ActionId`（`tween`/`tweenFloat`/`tweenVec2..4`/`delay`/`call` 叶子与 `sequence`/`parallel`/`repeat` 组合子）、`Signal<T>`/`SignalSubscription`（`emit` 立即 + `post`/`drain` 延迟）。**填补的是真实空白：** 本切片前 `Timer`/`Scheduler`/`Tween`/`Sequence`/`Coroutine` 在 `include/` 与 `src/` 零命中；唯一的 easing 是 `UIMotion.hpp` 的三值 `UIEasing`，唯一的 tween 是 UI keyframe timeline（只作用于九个 `UIAnimatableProperty`、由 `UIContext` 按窗口分配容量，无法驱动 gameplay float）。**三个 owner 共享契约：** 固定容量、单 owner、delta 由调用方给（帧循环拥有 fixed/frame 划分，自采样时钟的对象无法从 `fixedUpdate` 驱动）、回调内可自由 schedule/play/cancel 且新增下一次生效、dispatch 重入返回 `ReentrantDispatch` —— 合起来使投递顺序不取决于回调嵌套深度；dispatch 标志由 scope guard 恢复，否则抛异常的回调会让 owner 在此后整个进程拒绝每次 advance。**两个方向相反且刻意的时间规则：** 不足一周期的余量**携带**（否则周期每次损失一次舍入而漂移；sequence 子节点边界同理），超过追赶上限的整周期**丢弃并计入** `discardedCatchUpSteps`（携带会让卡顿后每帧都发满上限，把一次停顿变成一列停顿）。tween 最后一次 apply 的 alpha 恰好为 1，避免「停在 199.997」。授权 fail-late：工厂返回 `Action` 而非 `Result`，毒化只存 `ErrorCode` + 字符串字面量故零分配，由 `play()` 上报首个失败并指向出错的子表达式。占用 `ErrorDomain::Gameplay = 17`、`MemoryTag::Gameplay = 14`（`MemoryTagCount` 14→15，连带更新 `tests/core` 两处 static_assert）。**2026-08-31 编译证据（用户限定 compile-only，未运行任何测试）：** `windows-msvc-vnext` 全量 build 零 error 零 warning，`tina_gameplay.lib` 与 `tina_tests.exe` 产出；六个 header-isolation 单 TU（含显式 `Signal<Unit>` 实例化）独立编译通过，暂挂在 `tina_tests` 上。**待：** `tina_gameplay_tests` 单元测试与 sample 消费面（届时把 isolation objects 迁到该 target）。明确不在范围：coroutine、tween 的 relative/reverse/speed 变体。**更正（2026-09-01）：** 「尚无 `tina_gameplay_tests` target」这个说法**已过期** —— `tests/gameplay/CMakeLists.txt` 已定义该 target 并经 `tests/CMakeLists.txt:155` 的 `add_subdirectory(gameplay)` 注册，六个 header-isolation TU 也已按原计划迁入该 target（`$<TARGET_OBJECTS:tina_gameplay_header_isolation>`），依赖只经 `Tina::Gameplay` 的 PUBLIC link 传递故与消费者同一 include 集。**2026-09-01 单测落地证据：** 五个源文件全部落盘，`tina_gameplay_tests` 127/127 通过（`windows-msvc-vnext` Debug，直接运行 exe 未用 CTest）；六个 header-isolation TU 已从 `tina_tests` 迁入本 target，`tina_tests` 551/551 无回归。覆盖面按契约而非按函数组织：28 条曲线的端点精确性/单调性/越界与非有限输入、`Scheduler` 的余量携带与整周期丢弃（全部取 1/16 的倍数使二进制精确，并用「16 次 0.1875s 应投递 12 次而非 8 次」把携带与清零区分开）、零 interval 每次 advance 恰好一次、自我取消与跨 timer 取消、回调内 schedule 下一次生效、抛异常回调后不卡死、`Create` 后零分配、moved-from 对象惰性；`Action` 授权 fail-late（首个失败胜出、被拒绝的 combine 仍消费全部 child、节点上界 256 的边界两侧）、tween 末次 alpha 恰好 1、sequence 余量跨子节点携带、parallel 取最小余量。**发现并修复两个真实缺陷（均由测试运行暴露，非静态阅读）：** ①`ActionRunner::stepRepeat` 在触发 `maximumRepeatIterationsPerAdvance` 上限时未 `resetSubtree` 就返回，child 仍处 finished，故下次 advance 的 `step()` 立即返回却照样 `++iterations` —— `Repeat::times(3)` 配上限 2 只 apply 2 次，**少一次且无任何报错**；改为先 reset 再判上限，使上限只推迟迭代不吞掉迭代。②`Signal::subscribe` 只用 `subscriberCount` 判容量，但 dispatch 期间 unsubscribe 的 slot 既不 active 也未回到 free list（计数已减、槽位仍占），此时 subscribe 会 `push_back` 越过 Create 预留的容量而**重分配 slots 向量**，移动掉当前正在执行的那个 callback —— 实测为 access violation（0xc0000005）而非漏投递；改为在增长分支上补 `slots.size() >= subscriberCapacity` 判断并说明该窗口。sample 消费面仍缺 | Unit 127/127 + compile + header isolation |
| ANIM3D-001 | InProgress | P1 | 建立 `Tina::Animation3D` 动画图（crossfade / 状态机 / blend tree / layer+mask / root motion / 两骨 IK），并把 SkinnedMesh wire 提到 v2 加骨骼名称 | MATH-001、GAMEPLAY-TOOLING-001（`Easing`） | **先解阻塞：** SkinnedMesh v1 无骨骼名称（零字段、零字符串表），而 cooker 按 `(depth, sourceIndex)` 重排 joint 以保证 parent 先于 child，故 **cooked index 是消费者无法反推的排列** —— mask/retarget/IK goal 无 key 可依，且 `GltfCook.cpp` 对 `cgltf_node::name` 的读取次数为 0。v2 追加逐 joint 64 字节 UTF-8 名称块（无条件，不设 flag：本 payload 全部 offset 仅由 count 推导），空名合法、重名在 encode/parse/cook 三处拒绝（glTF 允许重名，故 cook 拒绝承重），不合成 `joint_7`（生成名与授权名对消费者不可区分，会在承诺「按名字」的接口下重新引入 index 依赖）。**新模块建在 `Animator3D` 旁而非替代它：** 后者三处结构限制都在公共契约上 —— `evaluatePose` 私有故无法「不推进播放状态地采样」（crossfade 正是同一 clip 采两个时间）、输出只写自己成员缓冲、`setPlaybackSpeed` 要求 `> 0`。`Animator3D.cpp` 一字未改，仍有 5 个固定契约测试与 `samples/3d_product` 消费面。**关键取舍：** pose 为 joint-local（混合 global pose 会拉伸肢体，子关节世界位置独立插值不保骨长）；`JointMask{}` 默认表示「全部 joint」而非「无」（没要求 mask 的调用方想要整副骨架动，「什么都不动」用 weight 0）；未知骨骼名直接失败不跳过（静默丢弃会产生处处略有不对的动画，成因通常是按另一副 rig 授权）；基础层不可 mask（被排除 joint 会持有 pose 缓冲残留）；additive 参考姿势无默认值（clip 按自身首帧授权时减 bind pose 会把偏移翻倍，是 additive 最常见缺陷）且在 Create 时解析一次；缺失 transition 直接拒绝不回落默认时长（授权缺口会被藏到「动画只是有点生硬」为止）；进行中 transition 被打断由 `canInterrupt` 决定（一律丢弃则输入像被忽略，一律叠加则 pose 累积）；root motion 从 pose 中**移除**并单独上报（两者都留角色会移动两次，倍速看起来像调参问题）；`cyclesCompleted` 上报一次 advance 跨越的整周期数（只由 `(previous, current)` 算 delta 会在跨循环点时丢整周期位移，低帧率下常态，表现为角色走得比动画短）；IK 用两骨解析解而非 CCD/FABRIK（闭式解存在，迭代法只是逼近同一答案还引入不可见的预算），超出可达范围指向目标绝不拉伸骨头，默认不完全伸直（满伸时余弦解方向数值不稳定会抖），pole target 可选、默认沿用当前弯曲平面（默认世界轴会让腿摆过该轴时膝盖瞬间反向）。占用 `ErrorDomain::Animation3D = 18`、`MemoryTag::Animation3D = 15`（`MemoryTagCount` 15→16）。**2026-08-31 证据：** `tina_animation3d_tests` 28/28（含 7 个 header-isolation 单 TU、`CountingMemoryResource` 固定 advance+evaluate 每帧零分配、170° 处 slerp 单位长度、root motion 从 pose 移除、跨 graph 句柄拒绝）；`tina_asset_format_tests` 126/126 与 `tina_asset_tests` 325/325（含 v2 名称 round-trip、重名拒绝、cooker 跨重排保名、malformed corpus 的未终止/脏 padding/非法 UTF-8）；`tina_tests` 455/455、`tina_scene_tests` 181/181 无回归；product-2d 全图零 error 零 warning。**过程中发现的两个真实缺陷：** ①`TypedPayloadMalformedCorpusTests` 手算块偏移，v2 名称块插入后每处破坏都打到名称 padding 上而 parse **成功** —— 运行时失败而非编译失败；②两个 graph 测试断言了「最后一帧」的值，但 Loop clip 推进到恰好等于 duration 会回绕到时间 0，故改为停在 duration 之前。**待：** retargeting（v2 名称已解阻塞但尚无消费者）、sample 消费面、pose-aware bounds。明确不在范围：morph target、2D blend space、sync group、animation event。**口径说明（2026-09-01）：** 本行 `InProgress` 与 [design-freeze.md](design-freeze.md) 中 ADR 0037 行的 `Implemented` **不矛盾，两者是不同的轴** —— design-freeze 记录的是「ADR 决定是否已落进代码」（`Skeleton3D`/`Pose3D`/`PoseBlend3D`/`ClipSampler3D`/`BlendTree3D`/`AnimationGraph3D`/两骨 IK 与 SkinnedMesh wire v2 确实全部已实现），本表记录的是「任务是否满足验收条件」（见本文件开头：「已经写代码」不自动等于产品门禁完成）。故本行保持 `InProgress`：**核实 `AnimationGraph3D` 在 `samples/` 与 `editor/` 零命中**，整个模块目前只有 `tests/` 一个消费者，`Animator3D` 才是 `samples/3d_product` 用的那个；缺消费面即缺产品级证据，与 `MATH-001`/`EDITOR-PICK-RAY-001` 那种「只剩明示范围外项」的情形不同。附带一处需对齐：retargeting 与 pose-aware bounds 在 design-freeze 记为「明确不在范围」，在本行记为「待」，以 design-freeze 的范围声明为准，本行的「待」应读作可选后续 | Unit + header isolation |
| UI-STUDIO-DESIGN | InProgress | P1 | `Tina Studio Compact` Material-inspired 设计系统与 Editor 迁移 | Retained UI Theme/RadioButton/StyleRole | 默认 Button 从 Primary 改为 Tonal；补齐 Primary/Danger/Tonal/Outlined/Text 与 Segmented recipes；Segmented 复用 RadioButton 互斥、输入、UIA 和 Selected state；Editor mode/tools/marquee/filter/tabs 删除 disabled-active 旧视觉，按钮层级和 14/15/20 desktop typography 已接入。默认 role 翻转波及的产品主题校验已对齐（2D/3D sample 与 runtime_ui facade 断言改用 `makeTonalButtonChrome()`）。**本轮证据（2026-08-11，UI worktree 临时 build tree）：** `tina_ui_tests` 624/624、`tina_runtime_ui_tests` 125/125、`tina_editor_app_tests` 13/13、`tina_ui_uia_tests` 12/12 全绿；`tina_sample_ui_showcase`、`TinaEditor`、`tina_sample_2d`、`tina_sample_3d` 编译通过且无新警告；`CheckDocs.ps1` errors=0 warnings=0（**证据失效，更正 2026-09-01：** `73e050d3` 说明该门禁遇到含 C++ lambda 的文档即在 `GetFullPath` 抛未捕获异常，**中止其后全部文件**，故一直什么都没扫 —— 一次没有输出 `markdown_files=` 行的运行是崩溃而非通过。本条 errors=0 不成立，需重跑）。`TinaEditor --auto-demo --frames=60` 2D/3D 均 exit 0 且 `status=ok`：`automaticAuthoringStage=46`、Translate/Rotate/Scale commit=`1/1/1`、marquee Replace/Add/Toggle commit=`1/1/1`、gizmo/marquee rejects=`0/0`、`selectionVerified=true`，证明 segmented 化后的 tool/marquee 互斥选择与回调在真实输入下未回归。`RunUiStyleVisualGate.ps1`（`-Frames 400 -WarmupMs 2500`）`status=ok`、maxChannelDelta=`78.29`（阈值12），Dark/Light accent ROI 为 `(74.9,151.1,249.9)` / `(27.9,108.0,171.6)`，与新 palette `0x4C9AFF`/`0x1769AA` 一致。**待：** `RunUiStateFeedbackVisualGate.ps1` 需独占前台桌面合成指针输入，本轮宿主有常驻窗口稳定抢占指针目标（`actual=65776`）导致无法执行，须在干净门禁机补跑；该项完成后转 Done | Unit + Editor Product + Visual |
| UI-002 | InProgress | P1 | Windows UIA 产品验收收口 | UI-002-SPI / UI-002-UIA-MAP / UI-002-HWND / UI-002-HOST | **自动证据于 2026-08-18 当前工作树复跑：** Showcase 的 Profile/Notes TextEdit 均发布明确 accessible name，gate 按 `Edit + Profile name` 精确选择 action target，不再依赖多 TextEdit fragment 遍历顺序。`RunUi002UiaGate.ps1` → `artifacts/gates/ui-002-uia-20260818-200137.json`（ok、providers/bounds=`71/71`、focusable=27、fragment、Invoke/Toggle/Range/Value/Focus、normalShutdown）；`tina_ui_uia_tests` 12/12。**待：** 按 [ui-002-narrator-inspect-checklist.md](ui-002-narrator-inspect-checklist.md) 完成 Narrator/Inspect 人工金标后转 Done。Linux AT-SPI = `UI-002-LINUX` | Unit + Integration + Platform + Manual |

### 交接：宿主与环境依赖（2026-08-16）

当前 Windows 宿主已完成六项 2D 任务、`3D-SKIN-001` schema 15、`RENDER-002-TRANSPARENT` schema 16，
并于 2026-08-16 完成 `ASSET-SEC-002` 与 `SDK-001` 收口；[ADR 0026](adr/0026-ui-keyframe-timeline-and-layout-animation.md)
已 Accepted，`UI-MOTION-002` 已于同日完成 typed paint/bounded-layout timeline 与统一 gate 收口。`TEXT-001` 的
T1/T2/T3 自动化实现已完成；当前 Windows 主工作树重新增量构建了 UI、Runtime UI、GLFW、Asset、bench 和
2D/3D/showcase sample 目标，并通过 TextEdit/grapheme/IME/Motion、Runtime placement/timeline、GLFW placement/
IMM32、typed malformed corpus、Asset/Catalog/EnvironmentMap 与 motion workload 定向测试。下一步仅保留
`TEXT-001` 的真机 IME 人工矩阵、Linux 原生 XIM/Wayland 与 BiDi/复杂 shaping 后置范围；依赖宿主之外资源的
项目仍按下列边界等待，不在本机反复重试。

`UI-PAINT-002-A` 的源码、测试、Showcase 与文档已完成并通过统一 Windows gate；2026-08-17 复用常驻
UI/FreeType tree 增量构建六个目标，五个 GoogleTest executable 为 672/672、130/130、28/28、111/111、
10/10，UI Showcase 120 帧 smoke exit 0，输出 `controls=21`、`asymmetricCornerProducts=3`。以下残留项依赖本宿主之外的资源，在获得对应
环境前不要在本宿主反复重试：

- `UI-STUDIO-DESIGN`：`RunUiStateFeedbackVisualGate.ps1` 需要可独占前台合成指针输入的干净门禁机（2026-08-11 已验证本宿主被常驻窗口稳定抢占指针目标，不可行）；
- `UI-002`：Narrator/Inspect 人工金标需要操作员按 checklist 实操读屏；
- `UI-003` / `RENDER-LINES-001`：多显示器混合 DPI 与跨 GPU 像素金标需要第二块 GPU/独立门禁机；
- `PERF-002`：`status=approved` machine profile 与 Release hard gate 需要固定门禁机；
- `2D-EDITOR`：Linux `zenity`/`kdialog` 真实 open/save/folder/cancel 产品门禁需要 Linux 桌面环境。

## Next

| ID | 状态 | 优先级 | 工作 | 依赖 | 验收条件 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| RENDER-POST-002 | 待做 | P2 | bgfx 后处理 GPU 实现 | RENDER-POST-001、ASSET-SHADER-001 | 实现 offscreen framebuffer、HDR 格式协商（`Rgba16Float` 可用性回退）、bloom mip 链与 tone mapping 程序，把 `submitFrame` 的 `RenderTextureUnsupported` 换成真实执行。需视觉门禁（Dark/Light 与 HDR 亮部差分），且 `CustomShader` 步骤依赖 `ASSET-SHADER-001` 的 shader binding，故它是硬前置。当前 Null 后端已可验证契约与 reference math，故本项只补 GPU 侧 | Unit + Visual |
| ASSET-TEX-002 | InProgress | P2 | Texture2D cooked schema v2：mipmap、sRGB、压缩格式与 sampler | 无 | **schema/runtime 已落地：** `Texture2DWire::SchemaVersion = 2` 使用 32B header + 有界 level table，支持 `Rgba8Unorm`、BC1/BC3/BC7、ASTC 4x4，Linear/sRGB、四种 wrap、Point/Linear 与成对启用的 Anisotropic min/mag；单级纹理必须 `mipFilter=None`，多级纹理必须提供到 1x1 的完整 chain 且选择 Point/Linear。Asset 层逐字段翻译到 backend-neutral `Texture2DUploadDesc`；Null/bgfx 共用校验，bgfx 做 native format/sRGB/sampler 映射和连续 mip blob 上传，不支持的 adapter format fail closed；cooked type v1 fail closed，binding 失败补偿销毁新 texture。**mip 生成已落地（MediaCook + GltfCook）：** 新增 `include/tina/asset/TextureMipChain.hpp` + `src/asset/TextureMipChain.cpp`，在**线性空间**做 premultiplied-alpha 盒式降采样（sRGB 用精确曲线而非 2.2 近似；alpha 即使在 sRGB 纹理里也按线性处理，因为它是覆盖率不是颜色），奇数边长的末级取 3 tap 否则尾行/尾列不参与任何目标像素而边缘消失，全透明块四通道归零以避免除零得 NaN；level 以 offset 而非 span 存储，move 后不会指向被搬走的 buffer。建链与写 payload 收成单一入口 `writeMippedTexture2DPayloadBytesRgba8`，因为 chain 与 `sampler.mipFilter` 必须一致（wire 拒绝多级配 None，而多级却声明 None 会发出一条采样器永不选择的链），由刚产出的 level 数推导 filter 使二者无法被设成不一致。`MediaCook`（导入图像视作 sRGB）与 `GltfCook`（base-color sRGB、normal/MR Linear，色彩空间同时决定降采样在哪个空间里做平均）已接入。**`CatalogCook` recipe 有意不接入：** recipe 里的 `texture2d` 全部是图集（`sample_2d.recipe` 注释即写明 `2x1 atlas: left floor, right wall` 与 `3x1 atlas: idle, walk, wall hit`，`sprite` 条目在 0→0.333/0.333→0.667/0.667→1 上切子矩形），无 gutter 的图集自动生成 mip 会**跨精灵边界混色** —— 3x1 角色图集的 1x1 级就是 idle+walk+hit 的均值，缩小时角色变成一团浊色，比不做 mip 更糟。正确形状是让 recipe 作者显式 opt-in（属于下面仍缺的 source sampler authoring），但 recipe 的像素列表是尾部变长的，新 token 只能插在 height 与首个像素之间，靠"不是 8 位十六进制"来区分，语法脆弱，故先留作待定。**更正（2026-09-01）：** 上一段「recipe 有意不接入」与紧随的「尚未完成：recipe 与 assetc 仍只产 Rgba8 单 mip」**均已过期**，该决定已被推翻并实现，原文保留以说明判据由来。混色顾虑并非靠新增 token 解决，而是靠**从 recipe 已经陈述的引用关系反推**：`93708d80` 新增 `recipeReferrerSamplesWholeTexture()`（`src/asset/CatalogCook.cpp:673`）与 `writePendingRecipeTexturePayloads()`（同文件 `:705`），一个 `texture2d` **当且仅当没有任何 referrer 从它身上切子矩形时**才得到完整 chain —— Material 的 UV 来自 mesh 顶点故不算切割，`sprite` 只有 UV 恰为 `0 0 1 1` 才算采样整图，`tileset` 按定义切网格，**无法识别的 referrer 一律按切割处理**（猜「整图」会给图集生成 mip 并混掉不相关精灵，猜「子矩形」只是少一条链，两个方向的代价不对称）。因此上文 `sample_2d.recipe` 的 2x1/3x1 图集仍然**不**产 mip，而整图纹理自动产链，无需作者 opt-in，也无需在 height 与首个像素之间插新 token。代价是纹理 payload 必须在整个 recipe 解析完成后才写（切割引用可能出现在更后面的行），故按 authoring 顺序预留槽位再回填，使该决定不会重排资产。`9e498629` 同日让 assetc 默认 fixture 走 mipped producer（`tools/assetc/main.cpp:447`）—— 该 fixture 是被全 UV sprite 采样的整图，此前单级输出与同内容写成 recipe 时产链的结果自相矛盾；派生规则已写入 `include/tina/asset/CatalogCook.hpp` 的 recipe 语法注释。**仍未完成（口径收窄）：** recipe 与 assetc 的像素格式仍只有 `Rgba8Unorm`，没有压缩/transcode，也没有 source sampler authoring（wrap/filter/anisotropy 仍不可在 recipe 里授权）；高级格式目前只能由直接 C++ payload API 提供。**2026-09-01 证据：** 全量构建零 error 零 warning；`tina_asset_tests` **340/340**、`tina_asset_format_tests` **129/129**、`tina_tests` **471/471**、`tina_render_scene_tests` **89/89**、`tina_render_bgfx_tests` **128/128**、`tina_ui_tests` **839/839**、`tina_scene_tests` **181/181**、`tina_editor_tests` **146/146**、`tina_network_tests` **166/166**（1 skip 为 host 限制）全绿。`tina_sample_2d --frames=300` 与 `tina_sample_3d --frames=30` 均 exit 0 且证据数字未变（schema 29：`submittedRenderFrames=304`、`spritesPerFrame=26`、`fxInitialFingerprint` 一致；schema 16：transparent draw=`2`、sort checksum=`6050478067199599831`、`renderResourceLedgerBalanced=true`）。**本轮抓出三个真实缺陷：** ①`MediaCook` 建 `CatalogCookAssetSpec` 时漏填 `assetTypeVersion`，吃到默认值 1 而 payload 已是 v2，**Editor 导入 PNG 会产出通不过校验的纹理**（46 个 asset 用例的共同根因，另一处是共享 fixture 手写的条目版本号）；②Editor Inspector 的 `mixedBool` 把 u8 颜色通道窄化成 bool，两个不同的非零通道被判等，**多选颜色不同却显示为不混合**（原为 C4805 warning），已改为按访问器实际类型比较并更名 `mixedField`；③`HttpRequestTest.StallLimitEndsASilentServer` 的 fixture `m_closeAfterReply` 默认 true，服务器其实立刻关连接而非沉默，故 `HttpClient.cpp:686` 的 stall-limit（防止对沉默 peer 无限 pump 的唯一保险）**实际零覆盖**，已补 `setCloseAfterReply(false)`。**bgfx 上传契约已补齐（原为零覆盖）：** 把 format/flag 映射与 blob 拼接从 `BgfxRenderDevice.cpp` 抽到 `src/render/bgfx/BgfxTexture2DUpload.{hpp,cpp}`（纯移动，逐字未改逻辑），新增 `tests/render_bgfx/BgfxTexture2DUploadContractTests.cpp` 14 例：五种 format 各自映射且 `Invalid` 不与真 format 撞值、`BGFX_TEXTURE_SRGB` 仅 sRGB 置位、bgfx 默认态（Repeat + Linear + 无 mip filter）不产生任何 bit、两条 wrap 轴各留在自己的双位域内（`U_MIRROR|V_CLAMP` 不能塌成 `U_BORDER`）、三级 filter 各自置位、mip chain 按 level 顺序**逐字节**拼成单块 blob（只校验总长会漏掉顺序颠倒）、单级上传不得声明 `hasMips`、压缩纹理按整块计费（BC1 8x8→8/4/2/1 各 32/8/8/8 = 56B）、adapter 探测携带与 create 相同的 format/flags 且不按 cubemap 探测、单格式被拒时仍不影响其它格式、alloc 失败与 create 被拒都 fail closed 而非返回无效 handle、非法 descriptor 在碰 bgfx 之前就被拒。同时把 fake 的 flag 取值全部改为照抄 `defines.h`（原先是编造的独立位，会掩盖 bgfx 的双位域语义），并补 `alloc`、按格式拒绝的 `isTextureValid` 与探测参数记录。**新证据（本轮改动后重跑）：** 全量构建零 error 零 warning；23 个套件全绿，`tina_render_bgfx_tests` **128/128**（原 114）、`tina_asset_tests` **341/341**（新增的 `GltfCookTests.CooksAFullMipChainAndFiltersEachChannelInItsOwnColorSpace` 已反向验证：把 GltfCook 换回单级 helper 后它确实变红。此前所有 glTF 纹理夹具都是 1x1，而 1x1 只产一级链，故根本观测不到 mip 生成）；`tina_sample_2d` 与 `tina_sample_3d` 均 exit 0，2D pixel 指纹 `d240ae857772792d16bab7f0ccd0874d`、3D `177156268a1bc5c4eec105bbd470a02a`（三次运行一致；此前 backlog/笔记记的 3D 值有误）。仍需真实 adapter smoke | Unit + Contract + 待 Smoke |
| ASSET-SHADER-001 | 待做 | P3 | Shader/Font typed payload、import 与 runtime binding | ASSET-TEX-002（sampler 契约） | 目前只有枚举、Catalog 标签与 Editor 展示，无 typed payload/import/runtime binding；`RenderErrors.hpp` 已预留 `ShaderUploadUnsupported`/`InvalidShaderUpload`/`ShaderNotFound`/`ShaderBindingKeyExhausted` 四个**零使用**错误码。是 `RENDER-POST-001` 的 `CustomShader` 步骤与 `RENDER-POST-002` 的前置。**注意**：先做它会再造一个无消费者的面，故排在后处理 GPU 实现之前但在契约收口之后 | Unit |
| MOBILE-001 | InProgress | P2 | 移动端（Android/iOS）平台契约扩宽，分两个可独立验证的切片 | [ADR 0032](adr/0032-mobile-platform-contract-boundaries.md)（**Proposed，D3 待 maintainer 明确**） | **切片 A（桌面可验证，不需要移动后端）：** ①**已完成 pointer presence**：`PointerSnapshot::present` 区分「没有指针」与「指针停在上次位置」，缺席时保留最后位置而非哨兵、且不允许持有按键（`isValidWindowSnapshot` 拒绝）；GLFW 由 `glfwSetCursorEnterCallback` 驱动并在离开时走既有 cancel 路径释放按键与清 hover；默认 `true` 故既有后端语义不变。`tina_tests` 396/396（+1），移除该 invariant 后新测试确实失败。②**已完成多点触控**（`4ec987a9`，2026-08-28；本行此前写作「待做」并给出了一份与实现不符的计划，**已更正**）：实现走的不是原计划的「把 134 处引用改成表索引」。`src/ui/detail/UIContextImpl.hpp:293` 新增 `PointerInteractionState` 与 `pointerInteractionStates[PointerCapacity]` 表，原有单槽字段保留为**活动指针的工作寄存器**，由 `loadPointerInteractionState()`/`savePointerInteractionState()` 在每次路由时 save/restore 交换（`UIContextPointerInput.cpp:322`，配 scope-exit 还原 Primary）；因此全部调用点无需改动而语义变为 per-pointer。原计划中「放宽 `PlatformFrame` 四处 `PrimaryPointerId` 校验」**并非必要**：`PlatformFrame.hpp:923-932` 那段与紧随其后 `:936` 的全 slot 循环等价重复，本就不构成对非 primary pointer 的限制。`destroySubtree` 的 cancel 逐 pointer 遍历（`UIContextPointerInput.cpp:290-300`）。验收已达成：`UIPointerCaptureTest.MultiplePointersKeepCaptureAndArmsIndependent` 覆盖双指针独立 capture/arm/释放，`UIVirtualStickTests.ASecondPointerCannotStealOrMoveAnEngagedStick` 是真实多点消费者；`tina_ui_tests` 的 `*Pointer*` 过滤 64/64。③**已完成 per-pointer cancel（2026-08-28）：** 切片 A 的 ①② 之间原有一道接缝没接上 —— `PointerSnapshot::present` 已是 per-pointer，平台因此能说「第 2 根手指抬起了」，但下游唯一的取消入口 `cancelPointerInteraction(WindowId)` 会 `for (state : pointerInteractionStates) state = {}` **清空全部 8 个 slot**。桌面上不可见（只有一个指针，全清等于清它自己），触摸设备上就是 ADR 0032 引 cocos2d-x 为反例的那类缺陷：一根手指抬起会让另一根丢掉正握着的控件。修法是给取消加 pointer 作用域：`InputCancelTransition::pointer`（`std::optional<PointerId>`，nullopt 保持全量语义，供 focus-lost/stream-reset 用；与 `gamepad` 互斥，越界拒绝）；`UIInputRouter::cancelPointerInteraction()` 增加同名可选参数；新增私有 `cancelSinglePointerInteraction()` 只清目标 slot，并**跳过** focus/tooltip/IME/command-latch 那些键鼠共享状态的拆除——一根手指抬起不构成拆它们的理由；capture 持有者仍按既有 committed ancestry 收到 synthetic `PointerCancel`。`PlatformFrameBuilder` 的最终态一致性校验相应按 slot 生效（scoped cancel 只把该 pointer 的按键期望置 Released）。GLFW 的 cursor-leave 现在只取消 Primary。**证据：** `tina_tests` 420/420（+2）、`tina_ui_tests` 839/839（+3）、`tina_runtime_ui_tests` 151/151、`tina_platform_glfw_tests` 53/53；`tina_sample_2d` 300 帧 `status=ok` 指纹 `488c124f…` 未变，`tina_sample_virtual_stick --auto-demo` `status=ok`、`maximumMagnitude=1`、`recentredAfterRelease=true`。三处不变量已 revert 验证会失败（UI 侧退回全量取消、平台侧退回全量期望清除）。**D3 已定（2026-08-28）：** maintainer 选择备选方案「外部驱动」而非原推荐的「保持阻塞 `run()`、后端内部适配」—— 后者在 iOS 上要引擎线程 + `CADisplayLink` 线程 + 每帧信号 + 渲染提交 marshal 回主线程（`CAMetalLayer` 限主线程），反而违背它想保住的 C4。实现为 `EngineHost::start()` + `tick()`：`tickOnce()` 是唯一帧函数体、`run()` 变 `while` 封装，故 **75 处 `run()` 调用点零改动**。原 5 个 loop-local 变量收进 `m_frameLoop`；循环内 57 个 return 里 48 个本就走 `stopNormally`/`failAfterStartupCommit`、其余 9 个是 lambda 体内的正常返回而非循环出口，因此 `optional<Result<RunExitReason>>` 足以表达「继续/终态」，无需新增 outcome 枚举。三条入口共用 `guardRunBoundary()`，异常→`Result` 与 teardown 完全一致。与原文预估的差异也记录在案：**ADR 0014 的四相位阻断不受影响**（它作用在一帧之内，与谁拥有循环无关），`RunExitReason` 语义未变。约束：`start()`/`run()` 互斥（任一消耗唯一 run 预算，顺序无关）、`tick()` 需先 `start()`、终态后 `tick()` 拒绝，各有测试；`ExternallyDrivenFramesMatchRunExactly` 逐事件比对两条路径完全一致。**证据：** `tina_tests` 425/425（+5）、`tina_runtime_ui_tests` 151/151、`tina_editor_app_tests` 23/23；`tina_sample_null` 300 帧 `status=ok`、`tina_sample_2d` 300 帧指纹 `488c124f…` 未变、`TinaEditor --auto-demo --frames=70` `status=ok` stage=57。三条 tick 守卫已 revert 验证（去掉后三个用例失败）。**切片 B 因此解锁**，其三项（C3 / C6 / C5）当时均为零实现；至 2026-08-29 **C3 与 C5 已完成，仅 C6 仍零实现**：**C3 已完成（2026-08-29，新增 [ADR 0034](adr/0034-native-surface-rebind.md) 取代 ADR 0020 的「不支持 live native rebind」）：** 原判断「需 RenderDevice 在同一 run 内重建 GPU 资源，牵动 bgfx 后端与全部资源 registry」**高估了范围**。实测两点：①bgfx 的 `setPlatformData()` 虽在头文件写「必须在 `init` 之前」，实现里的断言只禁止改 display type 与 context（`bgfx.cpp:448-458`），**native window handle 明确允许更换**，且 `renderer_vk.cpp:7622-7626` 已有对应 surface/swapchain 重建路径——bgfx 自己的 Android 示例正是靠它（`entry_android.cpp:238` 对 `APP_CMD_TERM_WINDOW` 什么都不做）。②surface/swapchain 属于 backbuffer，而 program/texture/mesh/shadow atlas（`BgfxRenderDevice.cpp` 内 75 处 handle）属于 **device**，不随 surface 失效；故 resident `AssetLease`、两个 binding registry、retirement ledger **全部无需参与 rebind**，零资源重新上传。所以 ADR 0020 那条限制记录的是 2026-07 的实现边界却被写成能力限制 —— 与本仓库反复出现的形态相同。实现：`RenderSurfaceState::nativeBindingRevision`（单调，仅 native window 更换时递增，与 `surfaceRevision` 正交）；校验集中在 `RenderSurfaceStateTracker`（递增必须伴随新 `surfaceRevision` 与新 `sourceMetricsRevision`，后退与 0 拒绝），并以 latch 告知 backend，故不 rebind 的 backend 零改动、被拒提交不遗留待处理 rebind；bgfx 侧 `setPlatformData` + **强制** reset（resume 时 extent 可能不变，只看几何会漏掉 same-size resume），不调 `shutdown()`；**未新增 `IRenderDevice` 纯虚**（surface 状态本就是每帧数据，再加命令式入口会产生两个真相来源并强迫每个测试替身实现它不需要的方法）；**未新增 drain 协议**（`FramePin` 的 CPU 借用期只覆盖 submit→present，不存在跨 rebind 的 in-flight pin）。`RenderStatistics::nativeSurfaceRebinds` 计数，桌面恒为 0。**证据：** `tina_tests` 430/430（+2）、`tina_render_bgfx_tests` 112/112、`tina_runtime_ui_tests` 151/151、`tina_ui_render_integration_tests` 32/32；`tina_sample_null` 300 帧 `status=ok`、`tina_sample_2d` 300 帧 `evidenceFingerprint=488c124f…`/`pixelFingerprint=1428d901…` 均未变（桌面不触发 rebind，符合 ADR 预期）。两条守卫已 revert 验证。**待：** Android 真机验证切后台/回前台（需工具链与后端）；`IPlatformBackend` 的 show/hide keyboard 与键盘遮挡矩形 —— `grep showKeyboard/hideKeyboard/keyboardOcclusion` 零命中（**C6 是唯一仍完全零实现的契约**）。C6 不是「在 Windows 上补测试」能推进的。**C5 已完成（2026-08-28）：** `TINA_RENDER_BGFX_MOBILE_SHADERS=ON` 时 `tina_bgfx_shader_profiles()` 额外 cook `essl`(300_es, platform=android)，4 张表的 11 个 `RendererType::OpenGL` 条目各增一个 `OpenGLES` 兄弟（Android 与 iOS GLES 同报此 renderer，一份二进制覆盖两者）。option 与编译宏在同一个 `if` 里设置，故表不可能引用未产出的 header；该选项在桌面可开启是刻意的，否则 ESSL 分支只会被没人运行的工具链编译 —— 正是已删除 `cmake/ShaderUtils.cmake` 的成因。后缀取自 bgfx 自己的 `_bgfx_get_profile_ext()`（`120→glsl`/`300_es→essl`/`spirv→spv`/`s_5_0→dxbc`/`metal→mtl`，注释标明与 `embedded_shader.h` 一致），`spv` 固定 `platform=linux` 亦与 `bgfxToolUtils.cmake:650` 一致。**未采用 bgfx 的 `bgfx_compile_shaders()`**（已通读）：它按 profile 分子目录输出，与我们按后缀平铺引用的表不兼容且 `100_es`/`300_es` 会撞同一 `essl` 目录；它按**宿主**平台选 profile，与「在 Windows 上 cook android essl」的目标相反；它把 `-O` 绑定 CMake 配置，会改动 Debug 的 shader 二进制与产品像素指纹。只采纳其命名权威。**顺带修一个真实可移植性缺陷：** `fs_tina_ui_image_quad.sc` 的局部变量名叫 `sample`，而它是 GLSL ES 3.0 **保留字**（multisample 插值限定符），ESSL 编译报 `illegal use of reserved word`；glsl120/spirv/dxbc 三者都接受，所以直到 cook OpenGLES 才暴露 —— 这类缺陷只有真去编译目标 profile 才会现形。改名 `texel`，语义不变。**证据：** 11/11 shader 在 `--Werror -O 3` 下 cook 出 essl；临时树以 `TINA_RENDER_BGFX_MOBILE_SHADERS=ON` 完整构建 `tina_render_bgfx` 成功并生成 11 个 `*_essl.bin.h`，随后按 `AGENTS.md` 回收该 2.9 GB 临时树（常驻树未动；option OFF 时 essl header 数为 0）；三个桌面 profile 对改名后的 shader 复验通过；`tina_ui_render_integration_tests` 32/32、`tina_render_bgfx_tests` 112/112、`tina_sample_2d` 300 帧 `status=ok` 且 `evidenceFingerprint=488c124f…`/`pixelFingerprint=1428d901…` 均未变。**限制：** 只证明编译与查表命中，GLES 上的实际绘制仍需真机或模拟器。**已执行：** 删除死的 `cmake/ShaderUtils.cmake`（555 行、零 include、引用已删除的 `tina2d_bgfx_shaders`，且假装 Metal/GLES 已就绪）。**Android 交叉编译已打通（2026-08-29，compile-only）：** 15 个引擎静态库（core/task/platform/render/scene/asset_format/asset/ui/audio/runtime/network/navigation2d/editor + 两个 integration）可为 Android 交叉编译，**NDK 28（libc++ 19）与 NDK 29（libc++ 21）× arm64-v8a 与 x86_64 四种组合均零 error**；此前**一个公共头都编译不过**。三类缺口性质完全不同：①**三处 libc++ 库缺口**（不是语言缺口，Clang 接受 `-std=c++23`，故等新 NDK 无用）—— `std::move_only_function` 在两个 NDK 的 `<version>` 里都仍被注释掉；浮点 `std::from_chars` 在 NDK 28 无 `__charconv/from_chars_floating_point.h`（只有整数与 `to_chars` 两半，NDK 29 才补）；`std::stop_token` 在 NDK 28 被挡在 `_LIBCPP_HAS_NO_EXPERIMENTAL_STOP_TOKEN` 后。maintainer 选择**保持支持 NDK 28**，故各加一个引擎类型：`Core::MoveOnlyFunction`、`Core::parseStrictFloat`、`Core::CancellationSignal`/`CancellationToken`（ADR 0007 原文即允许在有明确消费者时实现 `InlineFunction` 一类结构）。**未采用 `_LIBCPP_ENABLE_EXPERIMENTAL`**：它一次打开全部未完成 libc++ 特性，代价远大于一个取消标志。`MoveOnlyFunction` **必须有堆回退**，inline-only 设计被编译器证否：`TaskGroup::add` 把调用者的 `TaskCallable` 再包一层做完成记账，外层 target 因此含一整个 `MoveOnlyFunction`，**按构造**必然大于它要放进的缓冲区（128→256 只是重演同一失败）；仓库真正的不变量是有界队列/arena 不静默增长，type-erased callable 不属于那类。堆分支只在构造时分配一次，移动仅搬指针故仍 noexcept。②**两个真实 Clang-only 可移植性缺陷**（MSVC 静默接受，与 Android 无关时也是缺陷）：四处 fixed-inline callback（Button/RoutedPointer/Slider/Flow）的 `requires CanStoreCallable<...>` —— 变量模板**不短路**，首个 conjunct 已为 false 时仍实例化全部 trait，`is_nothrow_constructible_v<Self, Self&&>` 因此重入构造函数自身的约束，Clang 报 `satisfaction of constraint depends on itself`；修法是在 requires-clause 前置自排除使合取短路（accept/reject 语义逐条复验未变）。以及两处 `?:` 窄化（`UIGridLayout.hpp` 的 `0U : item.row`、`GltfCook.cpp` 的 `0U : values[slot].joint`）—— 整型提升把条件表达式的公共类型抬成 `int`，在 initializer list 里窄回 `u8`/`u16` 是 ill-formed。③**vcpkg 的 `ANDROID_NDK_HOME` 陷阱**：即使已传 `VCPKG_CHAINLOAD_TOOLCHAIN_FILE`，vcpkg 仍用自己的 `scripts/toolchains/android.cmake` 做 compiler-hash 探测，那个文件只认该环境变量（`ANDROID_HOME` 不够），否则报 `Could not find android ndk`。**证据：** `tina_tests` 448/448（+23：MoveOnlyFunction 9、ParseFloat 6、CancellationSignal 4、三个 header-isolation TU）、`tina_ui_tests` 839/839、`tina_asset_tests` 323/323、`tina_editor_app_tests` 23/23；`tina_sample_2d` 300 帧 `status=ok`、`evidenceFingerprint=488c124f…`/`pixelFingerprint=1428d901…` 均未变；SDK 公开头扫描 302/302（+3）。四处不变量已 revert 验证会失败（inline husk 析构、heap 单次析构、约束短路、`UISliderChangeCallback` 自构造）。**顺带补两处此前空缺：** `UIFlowHeader.cpp`/`UISliderHeader.cpp` 两个 header-isolation TU 原本只有 1–8 行、对 callback 零断言，现补齐 accept/reject 矩阵；`parseNumberFieldValue` 是公开 API 且**此前零测试**，替换时刻意保留其 `+` 前缀语义并显式拒绝 `"++1"`（`strtof` 会接受，`from_chars` 不会）。**限制：** 全部为 compile-only，**无任何 Android 平台后端故无可启动产物**，真机/模拟器零验证；`TINA_BUILD_PLATFORM_GLFW` 必须 OFF（GLFW 不支持 Android），`desktop`/`editor_app` 因此不参与 Android 构建，其 `std::jthread` 从不进入该编译；Physics2D/miniaudio/FreeType 三个第三方图未尝试交叉编译；刻意未加 CMakePresets 条目（preset 会暗示存在可交付的 Android 产品图）。配方记于 [building.md](building.md)。**bgfx 交叉编译已打通（2026-08-29）：** 结论是**阻塞点在 shaderc 而不在 bgfx**，且 bgfx 侧一行未改。`bx`/`bimg`/`bgfx` 与 `tina_render_bgfx` 在 **arm64-v8a 与 x86_64 上均零 error**；编进去的是真实实现而非空壳：`renderer_vk.cpp.o` 1341 符号、`renderer_gl.cpp.o` 696 符号，而 `renderer_d3d11.cpp.o` 仅 87，`glcontext_egl.cpp.o` 在列 —— 与「Android 偏好 Vulkan」的既有决定吻合。首轮 22 个失败**全部是 shader cook、零个 C++ 错误**：`shaderc` 是构建期**宿主**工具（把 `.sc` 烤成 `*.bin.h` 编进库，设备运行时只查表，故 Android 侧从不需要 shaderc），而 bgfx.cmake 上游以朴素 `add_executable(shaderc ...)` 声明、零交叉编译处理，于是交叉构建把这个宿主工具也编成目标架构 —— 实测产物为 460 MB AArch64 ELF（`llvm-readelf -h` 报 `Machine: AArch64`），宿主无法执行。**修法：** 新增 `TINA_BGFX_SHADERC_EXECUTABLE`（FILEPATH cache），交叉构建指向任一桌面树里现成的 `shaderc.exe`（PE，约 43 MB）；两个 cook 函数原本各自重复一份 shaderc 调用，现收敛到 `_tina_resolve_bgfx_shaderc()` 单点解析，并区分依赖形态 —— 原生构建依赖 `bgfx::shaderc` **目标**（改 bgfx 会重新 cook），导入的宿主二进制只能是**文件**依赖（本树无法重建它）。同时 `thirdparty/CMakeLists.txt` 在给定该变量时把 `BGFX_BUILD_TOOLS` 关掉，省下那 460 MB 无用产物与相应编译时间。**证据：** `TINA_RENDER_BGFX_MOBILE_SHADERS=ON` 下两个 ABI 各产出 11 个 `essl` + 11 `glsl` + 11 `spv`、**0 个 dxbc**（Android 不需要 D3D，`WIN32` 语义正确指目标平台，与 C++ 表的 `BX_PLATFORM_WINDOWS` 一致）；`libtina_render_bgfx.a` 内 11 个 essl 符号、0 个 dxbc，`llvm-objdump -f` 报 aarch64 / x86_64。三条错误路径均在 **configure 阶段**给出可操作诊断并逐条验证：交叉但未提供、提供路径不存在、`bgfx::shaderc` 缺失（此前要构建到 852/871 才抛出 22 条难以归因的失败）。桌面回归：`tina_render_bgfx` 正常构建、`tina_render_bgfx_tests` 112/112、`tina_ui_render_integration_tests` 32/32、原生 preset configure 正常，且 `touch` shader 源码仍触发三个 profile 重新 cook（依赖追踪未因改 DEPENDS 而退化）。**不得**用「关掉 shader cook」绕过缺失的 shaderc —— embedded header 正是 `*Shader.cpp` 里 RendererType 表引用的对象，跳过只会把清晰的构建错误换成难查的链接错误。**顺带发现一个与文档冲突的事实：** `docs/testing.md` 把 `pixelFingerprint` 描述为同机可复现的 exact gate，但实测同机同二进制连跑 6 次，前 2 次为 `76bc083a…`/`58f17167…`、后 4 次才稳定在基线 `1428d901…`（6 次全部 `status=ok`、`pixelGoldenMatched=true`）。故**单次指纹变化不构成回归证据**，已在 testing.md 记录现象与应对；根因未查明，未声称定位。**已执行：** 删除死的 `cmake/ShaderUtils.cmake`（555 行、零 include、引用已删除的 `tina2d_bgfx_shaders`，且假装 Metal/GLES 已就绪）。**Android 平台后端骨架已落地，并首次在真模拟器上运行（2026-08-29）：** `src/platform/android/` → `tina_platform_android`（与 `tina_platform_glfw` 对称的独立 target），实现全部 7 个纯虚（`IPlatformBackend` 4 + `IWindowSurfacePlatformBackend` 3）。先补上前置：`NativeWindowBindingKind::Android` 分支 + bgfx 解码 —— `ANativeWindow*` 自包含（bgfx 自己的 Android entry 直接交给 `nwh`、用默认 handle type、从不设 `ndt`），故该分支**拒绝**携带 display 的 binding，否则那字段会被 bgfx 静默忽略；缺 window handle 无需新代码，`WindowSurfaceLease.cpp:51` 早在更前一层拒绝（原以为要在 bgfx 层加检查，写测试才发现防线更靠前）。`toBgfxPlatformData()` 的 switch 改为**穷尽且无 `default`**，故新增 kind 编译失败而非运行期拒绝，非枚举值由 switch 前一处显式范围检查负责。后端三个取舍：①**按目标平台门控**（`if(ANDROID)`）而非 option —— option 会宣告不存在的选择；②native window 以不透明 `std::uintptr_t` 跨边界，公开头不出现 `ANativeWindow*`；③**零第三方依赖**（不链 libandroid/无 JNI/无 SDK 头），足迹与 Headless 相同，该依赖属 JNI 桥那一片不在此预留。extent/scale 为 0 一律拒绝而非取默认值（会静默改变下游每个 UI 元素尺寸，比启动失败难查）。**「编译通过 ≠ 能用」被实测抓到一次：** 骨架交叉编译零 error 零 warning，但在模拟器上 `pollFrame()` **每次都失败** —— 默认构造的 `WindowInputSnapshot` 通不过 `PlatformFrameBuilder` 校验（input 的 window id 与 metrics revision 必须与其伴随 metrics 一致）。修正时顺带发现一个语义问题：该默认值让 primary pointer 保持 `present`，适合总有位置的鼠标而对触摸是错的 —— 两次点击之间手指没有位置，present 却空闲的指针会把 hover 永久钉在上次触摸的控件上（正是 C2 要解决的缺陷），故 Android 静默输入把**全部** pointer 置 absent。**证据：** `tina_platform_android_tests` **10/10 在 `emulator-5554`（Android 36 x86_64）实机通过**，非仅编译 —— 这是本仓库首次有 Android 代码真正运行；`tina_render_bgfx_tests` 114/114（+2 Android binding 用例）、`tina_tests` 448/448 桌面无回归。Android display 守卫已 revert 验证：去掉后测试**挂死在 `bgfx::init`** 而非优雅失败。**触摸输入桥已落地（2026-08-29）：** 形态刻意不照 cocos2d-x —— `docs/platform-input.md` 记载它每事件经 `runOnGLThread` marshal，**每事件分配一个 Runnable 并捕获一个 String**，事件洪流下是实打实 GC 压力；而 `PlatformFrameBuilder` 本就是固定容量 per-poll，故改为**单生产者/单消费者无锁环形缓冲**（`AndroidTouchEventQueue`，容量 256）：UI 线程 push、owner 线程每 poll 排空。有界且**有意可丢** —— 满时丢最新并计数（`droppedEventCount` 单调不重置，故按帧采样看到的是总量而非窗口，不会漏掉两次采样之间的丢弃）；增长会破坏固定容量不变量，阻塞会让 UI 线程卡在停滞引擎线程后冻结整个 app。留一个空槽使「满」与「空」无需额外 size 计数即可区分（那计数自身又要同步）。索引为 atomic 且各自 `alignas(64)`：每个索引只有一个写者，但**会被另一线程读**，release/acquire 配对同时负责发布 slot 内容；分开对齐避免两线程每事件互相弹 cache line。**`AndroidTouchSlotTable` 是对 cocos 具体缺陷的修复：** Android 的 pointer id 稀疏且复用，必须映射到 0..7 密集槽；cocos 形态对而实现错 —— `handleTouchesBegin` 对**已在 map 中的 id 没有 else 分支**，静默丢弃，而 id 复用使一个陈旧条目让该 identity 上**后续每根手指永久不可见**。本实现把映射收敛到唯一一处且每条路径都有明确结果：重复 Down **保留原槽**（重复 Down 意味 Up 丢了，重用最坏是重启该手指，丢弃则让它整个手势不可见）、槽位耗尽**拒绝**而非驱逐仍按下的手指、`releaseAll()` 供手势整体被收回时使用（cocos 没有该钩子，后台化的拖拽把手指卡到进程退出）。表按 `PointerCapacity` 而非字面量定尺，且**不可默认构造** —— 零初始化会让八个槽都声称在追踪 Android pointer id 0，而那是合法且最常见的 id（第一根手指）。**Cancel 按 pointer 作用域绝不按窗口**（`InputCancelTransition::pointer` 命名该指，nullopt 意为全部八个，正是 ADR 引的多点缺陷）；**未新增 `InputCancelReason` 枚举值** —— `FocusLost` 语义已足，新增只会是无行为差异的公开契约变更。物理像素→window-logical 的换算放在 backend（那里才知道 content scale），生产者只报 Android 原样的物理像素。**证据：** `tina_platform_android_tests` **27/27 在 `emulator-5554`（Android 36 x86_64）实机通过**（+17）；两条不变量已 revert 验证会失败 —— 重新引入「重复 Down 返回 InvalidSlot」使 `ADuplicateDownKeepsTheSlot...` 失败，把 cancel 改成窗口级使 `CancellingOneFingerLeavesTheOtherHolding` 失败；桌面 `tina_tests` 448/448、`tina_render_bgfx_tests` 114/114、`tina_platform_glfw_tests` 53/53 无回归。**C3 surface 重建接线 + C6 软键盘均已完成（2026-08-29）：** ①**修好一处「已发布却无生产者」的契约** —— ADR 0034 的 `RenderSurfaceState::nativeBindingRevision` **自落地起从未被任何生产代码赋值**：全仓库唯一写它的是 `tests/render/NullRenderDeviceTests.cpp`，`EngineHost::toRenderSurfaceState()` 根本没转发该字段（故恒为默认值 1），而平台侧来源 `Integration::WindowSurfaceSnapshot` **连该字段都不存在**。tracker 校验与 bgfx `setPlatformData`+强制 reset 全都写好了，但整条 rebind 路径只能从测试到达；「桌面恒为 0」读起来像设计意图，实际是没有任何东西能让它非 0。本轮补齐三处：snapshot 新增字段、EngineHost 转发、Android 后端递增，并在 ADR 0034 加「后续修正」节记录该缺口。②**C3/C6 走 Android 专属接口 `IAndroidPlatformBackend`（`dynamic_cast` 取 facet），而非新增 `IPlatformBackend` 纯虚** —— 没有任何桌面后端能有意义地实现它们，加到通用接口会迫使 GLFW、Headless 与**每个测试替身**都实现一个只能返回失败的方法；这也正好消解了 D7 未定案的顾虑（担心的就是「新增纯虚波及全部后端」）。③**C6 形状：请求是意向，遮挡由宿主上报。** `requestShow/HideSoftKeyboard()` 只 latch 意向（只有 Java 能调 `InputMethodManager`），`takePendingSoftKeyboardRequest()` 读取即清空 —— 故一次请求恰好产生一次 IME 调用而非每帧重复施加；键盘遮挡高度**必须由宿主上报**（物理像素）不可由引擎推算，因为 Android 的 IME 高度取决于键盘应用、语言、候选条与分屏几何，猜测会把聚焦输入框错位任意距离；读取侧返回 window-logical 供 UI 直接相减 —— 没有它，聚焦文本框会躺在键盘背后且无从得知需要滚动。高于窗口的遮挡值**拒绝而非 clamp**（意味宿主与后端量的是不同几何，clamp 会掩盖分歧）。**`updateTextInputPlacement` 仍然拒绝**：caret placement 是 IMM32 那类 IME 定位协议，Android 无对应物，与「显示/隐藏键盘」是两件事。④**窗口丢失释放全部手指**（被任务切换打断的拖拽绝不能留下滞留手指 —— cocos2d-x 正是把手指卡到进程退出），且该方法**幂等**（Android teardown 时会在无前置 INIT 的情况下投递 TERM）。窗口替换要求**三个 revision 同时推进**，因 tracker 拒绝「binding 变了但 surface/metrics revision 未变」的提交，否则后端会在两个不同帧里分别观察到 rebind 与它该 reset 到的几何；几何校验与创建路径**共用同一函数**（rebind 若接受工厂会拒绝的几何，就是直通 `bgfx::reset` 的洞）。**证据：** `tina_platform_android_tests` **38/38 在 `emulator-5554`（Android 36 x86_64）实机通过**（+11）；两条不变量已 revert 验证会失败（去掉 `++nativeBindingRevision`、去掉窗口丢失时的 `releaseAllPointers()`）；桌面 `tina_tests` 448/448、`tina_render_bgfx_tests` 114/114、`tina_platform_glfw_tests` 53/53、`tina_runtime_ui_tests` 151/151、`tina_ui_render_integration_tests` 32/32 无回归，`tina_sample_2d` 300 帧 `status=ok` 且 `evidenceFingerprint=488c124f…` 未变。**JNI 桥 + 可安装 APK 已落地，Android 上首次真正运行（2026-08-29）：** `android/` 为 AGP 工程（gradle 8.13 + AGP 8.13.2 + JDK 17 + compileSdk 36，两个 ABI 的 `.so` 都进包），`src/platform/android/jni/` 是唯一出现 Java 名字的地方且**不含引擎逻辑** —— 每个入口都转发进 `tina_platform_android`，故槽映射/环形缓冲/生命周期规则只有一份实现且能在无 JVM 下测试。**用 `RegisterNatives` 而非 name mangling：** platform-input.md 记载 cocos2d-x 只靠符号修饰导出、用字符串查反向调用类名，而该类只存在于可选模块，默认工程调用即失败且无诊断；显式注册改为在 `System.loadLibrary` 时就失败并点名签名不匹配的方法，故签名漂移不可能静默出货。**跨语言枚举权威在 C++**（lesson 5）：Java 侧只有整数常量，C++ 侧 `static_assert` 钉住四个值且**拒绝**未知值而非强转（强转会把 Java 改动变成越界枚举并破坏下游 pointer 状态）。**Java 侧多点拆分有两个不对称，写错任一个都是静默缺陷：** `ACTION_POINTER_DOWN/UP` 必须**只**取 `getActionIndex()` 那一个 pointer（该事件携带全部活动 pointer，全报一遍会重复按下已按下的手指），而 `ACTION_MOVE` **没有** action index、一次报告全部 pointer，必须全部转发（否则除第一根外的手指会卡住不动）。**四个 gradle/CMake 坑（均实测踩出，记于 building.md）：** ①SDK 自带 CMake 3.22.1 而引擎要求 3.25+，必须用 `local.properties` 的 `cmake.dir` 指向更新的安装；②AGP 的 `path` 必须指向 `android/app/CMakeLists.txt` 这个 wrapper 而非仓库根 —— 指向根会让 AGP 读到同目录的 `CMakeSettings.json`（Visual Studio 文件，带 UTF-8 BOM）并按自己的 schema 解析，报 `Expected BEGIN_OBJECT but was STRING`；③vcpkg 必须反过来 chainload NDK toolchain（AGP 传的 `CMAKE_TOOLCHAIN_FILE` 直指 NDK，wrapper 把它移入 `VCPKG_CHAINLOAD_TOOLCHAIN_FILE` 并把 vcpkg 放前面），否则 `find_package(xxHash)` 失败；④`VCPKG_MANIFEST_DIR` 必须显式给 —— vcpkg 只在 `CMAKE_SOURCE_DIR` 找 `vcpkg.json`，而 wrapper 让那里变成 `android/app`，不指定会**静默跳过 manifest 模式**再在第一个 `find_package` 报一个完全不提 vcpkg 的错误；triplet 按 `ANDROID_ABI` 选（AGP 每 ABI 单独 configure，写死会让另一 ABI 拿到错误架构依赖），未验证 ABI 直接 FATAL 不猜。**实机证据（`emulator-5554`，Android 36 x86_64）：** APK 安装并启动，进程存活、窗口获焦、**`-Xcheck:jni` 零警告**；注入真实 tap 与 swipe 零故障；**旋转**与**后台/前台循环**后仍是同一 pid 且零故障 —— surface 销毁后重建的 rebind 路径**在真机实际跑通**，为 ADR 0034 首次端到端闭环；`tina_platform_android_tests` 38/38 同轮通过；桌面 `tina_tests` 448/448、`tina_platform_glfw_tests` 53/53 无回归。**渲染器已接进 APK，引擎首次在 Android 上画出画面（2026-08-29）：** 截图分析确认主色 RGB(16,42,67)（1134/1300 采样点）即 bgfx 清屏色，而非 Android 默认黑/白；**旋转**与**后台/前台循环**后画面均恢复同色且零错误 —— ADR 0034 的 GPU 侧重建（`bgfx::reset` 对真实 `ANativeWindow` 替换）至此验证完毕。渲染器经 `-Ptina.shaderc=<宿主 shaderc 路径>` 可选启用（shaderc 是构建期宿主工具，交叉构建产不出可用的），不给则 APK 仍可装可跑、画面空白。**真机暴露两个模拟器与单测都看不见的缺陷，症状完全相同 —— 屏幕变黑、无崩溃、无日志，靠加一行结构化错误日志才定位（这类失败在视觉上与「清屏色恰好是黑」不可区分）：** ①**`surfaceRevision` 一次跳 2** —— Android 在后台/前台循环里**背靠背**投递 TERM_WINDOW 与 INIT_WINDOW，中间无任何 poll，而原实现在每个生命周期回调各加一次，于是 `RenderSurfaceStateTracker` 以「revision must advance exactly once for each committed state change」拒掉 resume 后的**每一帧**；修法是让计数器跟踪**被观察到的状态**而非事件（两次观察间的多次变化合并为一步，观察点为 `pollFrame()` 完成时 —— 放这里而非让宿主显式调用，宿主就无法忘记），已补单测。②**被拒绝的 submit 不能推进 frameIndex** —— device 要求索引连续，原实现用 `frameIndex++` 传参，一次瞬时失败会让其后**每一帧**都以「frame indices must be contiguous」被拒，把瞬态错误变成永久故障。**模拟器 Vulkan 驱动不可用（非引擎缺陷）：** SDK 模拟器的 `vulkan.ranchu.so` 在 `SetDebugUtilsObjectNameEXT` 内 SIGSEGV（栈顶即它，第二帧才是 bgfx 的 `SwapChainVK::createSwapChain`），故 `RendererApi` 需可选择 —— 真机保持 Automatic（Android 偏好 Vulkan），模拟器按 `Build.HARDWARE` 含 `ranchu`/`goldfish` 自动改用 GLES；这正是 `RenderDevice.hpp` 那条「GLES 变体作为 Vulkan 驱动不可用时的回退」注释预留的情形，本轮首次真的用上。**另修一处 CMake 静默失效：** JNI 目标原用 `if(TARGET tina_render_bgfx)` 判断，但 `src/platform` 在 `src/render` **之前**被 add_subdirectory，故该判断恒假 —— 产出一个不含渲染器却仍能链接并运行的库，只有数 `.so` 里的 bgfx 符号才发现（4.6MB/0 符号 vs 15.6MB/4202 符号）；改用选项判断。**证据：** `tina_platform_android_tests` **39/39 实机通过**（+1 revision 用例）；桌面 `tina_tests` 448/448、`tina_render_bgfx_tests` 114/114、`tina_runtime_ui_tests` 151/151、`tina_sample_2d` 300 帧 `status=ok` 且 `evidenceFingerprint=488c124f…` 未变。**EngineHost 已接进 APK，真实引擎相位在 Android 上运行（2026-08-29）：** JNI 不再直接驱动 RenderDevice，而是持有 `EngineHost` 并每帧调 `tick()` —— D3 的落地形态（引擎不拥有循环，Android 自己的节拍说话）。实机日志 `frameUpdates=300 fixedUpdates=314`，两个计数**刻意不同步**：固定步长累加器走独立时钟，这正是它从阻塞 `run()` 迁到外部驱动 `tick()` 后仍正确的证据。两个结构性约束在实现时才被迫面对：①`EngineHost::Create()` **会立刻运行平台工厂**，而 Android 的窗口异步交付，故 host 必须在首个 `surfaceCreated` 之后才能创建（这个不对称正是 D3 选外部驱动的原因）；②EngineHost **不暴露它的后端**（刻意封装），故 Android 生命周期需要一个在平台工厂内部捕获的观察指针 —— 那是唯一同时能看见具体类型与所有权交接的位置。**第三处 revision 缺陷（本轮第三个同类）：Runtime 侧校验漏了 `nativeBindingRevision`。** `RenderSurfaceStateTracker` 早已把 binding 变化并入 facts 比较（`|| nativeBindingChanged`），但 `EngineHost` 的同名校验没有 —— 于是**几何不变的窗口替换**（Android 以相同尺寸回前台时恰好如此）让 revision 前进了、而该校验仍认为「什么都没变」，从而拒绝 resume 后的每一帧。两处校验必须一致，否则同一次 rebind 对一方合法对另一方非法。三个同类缺陷都只在真机后台/前台循环出现、症状都是「画面停住、无崩溃」，全靠结构化错误日志定位。**顺带修一个日志洪水：** Java 侧未对「引擎已终止」加 latch，一次后台/前台循环产生 **50 万条**相同日志；终态是永久的，必须停掉帧循环而非每帧重试。**证据：** 实机 `frameUpdates`/`fixedUpdates` 持续推进、`droppedTouches=0`、旋转与后台/前台循环后渲染恢复 RGB(16,42,67) 且零 `rejected`；`tina_platform_android_tests` 39/39 实机通过；桌面 `tina_tests` **449/449**（+1 `AcceptsANativeBindingReplacedAtUnchangedGeometry`，已 revert 验证）、`tina_render_bgfx_tests` 114/114、`tina_runtime_ui_tests` 151/151、`tina_platform_glfw_tests` 53/53、`tina_sample_2d` 300 帧 `status=ok` 指纹 `488c124f…` 未变。**UI 内容已渲染：引擎在 Android 上画出真实 Retained UI（2026-08-29）：** 游戏状态在 `updateUI` 发布一棵 UI 树（百分比尺寸面板 + 每 60 帧换色的子面板），**像素与引擎状态逐次对应** —— `pulseOn=false` → RGB(60,190,120)、`pulseOn=true` → RGB(220,90,40)，八次连续采样全部命中；旋转后 UI 完整重排（横屏下橙色采样点由 561 增至 798，符合百分比布局）。动画是刻意的：静态面板在「引擎在跑」与「引擎卡死」下截图完全相同，而那正是本周两次调试的失败形态。**两个 UI 组合约束：** `UIRootOwner` 必须在 `onEnter` 经 root builder 创建而非只声明（默认构造的 owner 没有 root，updater 以「UI tree updater requires a root owner」拒绝）；root 必须显式设为 100% 尺寸，否则百分比子元素相对 auto 尺寸父节点解析并塌成零 —— 症状只是「面板没出现」。**第四个真机独有缺陷（唯一会崩进程的）：旋转时 `SIGABRT`**，中止信息 `FORTIFY: pthread_mutex_lock called on a destroyed mutex`，栈顶 `Surface::hook_query`。根因是 **bgfx 有自己的渲染线程且仍持有那个 `ANativeWindow`**：surface 挂起只让 `submitFrame` 跳过绘制，却仍 `pumpRetirementOnlyFrameIfNeeded()` → `bgfx::frame()`，该线程会 query window；而 `surfaceDestroyed` 此前立即 `ANativeWindow_release` 放掉最后一个引用，下次 query 即整进程中止。修法是**不在 surfaceDestroyed 释放**，持有到替换窗口到达（rebind 成功后再放旧的）或 session 销毁（那时先拆 render device）—— Java 的 Surface 对象照常死亡，这只让 native handle 多活一会儿，`ANativeWindow_acquire` 本就为此存在。前三个真机缺陷都表现为「画面停住、无崩溃」，这个是唯一崩的。**证据：** 实机 `frameUpdates`/`fixedUpdates`/`uiUpdates` 同步推进（uiUpdates 与 frameUpdates 相等即 UI 相位未被跳过）、`droppedTouches=0`、旋转与后台/前台循环后零崩溃零 `rejected`；`tina_platform_android_tests` 39/39 实机通过；桌面 `tina_tests` 449/449、`tina_render_bgfx_tests` 114/114、`tina_runtime_ui_tests` 151/151、`tina_ui_tests` 839/839、`tina_sample_2d` 300 帧 `status=ok` 指纹 `488c124f…` 未变。**触摸闭环已完成：手指改变了画面（2026-08-29）：** UI 注册 routed pointer listener，按住面板让子面板同时换色并放大（50%→90%）。**像素证据：空闲绿色采样 0、按住 11242、松开 0**；`presses` 与 `releases` 始终相等、`droppedTouches=0`。整条链闭合：手指 → Java `MotionEvent` → JNI → 无锁环形缓冲 → `PlatformFrame` → UI 路由 → 游戏代码 → 画面。**同时改颜色与尺寸**是刻意的（只改颜色在截图里难与 1 秒周期动画区分）。两条多点规则在此复用而非重新发明：第一根手指 latch 视觉、第二根不得重新触发（否则即 ADR 引 cocos2d-x 的「第二根手指抢走第一根的控件」）；释放按 pointer 作用域，另一根手指抬起不得解开这一根；子面板设 `Ignore` 命中策略避免从父面板抢触摸（与屏上摇杆对旋钮一致）。**第五个真机缺陷：`LAUNCH_MULTIPLE` 造出第二个 engine。** Activity 默认允许多实例，故从启动器再次启动会产生**第二个 `onCreate`（且无 `onDestroy`）**，第二个 session 试图再初始化 bgfx —— 而 bgfx 是进程级单例，于是 `EngineHost::Create` 以「bgfx failed to initialize」失败、app 冻结且**无任何其他症状**。修法：manifest 声明 `android:launchMode="singleTask"`。诊断手段是在 `onCreate`/`onDestroy` 打印 session 句柄 —— 两次 create 之间没有 destroy 就是该缺陷的指纹；此前绕了数轮都在猜，加上句柄日志后一次命中。**另记两个把我误导的陷阱：** ①用 `adb shell input swipe ... &` 加 job control 会在 adb 会话结束时**杀掉被测 app**（logcat 显示 `exited cleanly (3)` + `Remote process closed the socket`），看起来像引擎崩溃实为测试方法，正确做法是把 `&` 放进设备端 shell；②XML 注释里出现 `--` 会让 manifest 解析失败（`ManifestMerger2$MergeFailureException`），而我一度用旧 APK 验证并误以为已修好 —— 构建退出码必须单独确认，不能串在长命令末尾。**证据：** 实机 `presses=1 releases=1`、`onCreate=1`、零 `rejected`，`am start -n`（此前必坏路径）现已通过；`tina_platform_android_tests` 39/39；桌面 `tina_tests` 449/449、`tina_render_bgfx_tests` 114/114、`tina_runtime_ui_tests` 151/151、`tina_ui_tests` 839/839、`tina_sample_2d` 300 帧 `status=ok` 指纹 `488c124f…` 未变、`CheckDocs` errors=0 warnings=0（**证据失效，更正 2026-09-01，本行三处同类 CheckDocs 结论一并作废：** 见 `73e050d3` —— 该门禁遇到含 C++ lambda 的文档即在 `GetFullPath` 抛未捕获异常并中止其后全部文件，故 2026-09-01 之前一直扫描零文件；这些 errors=0 不成立，需重跑）。**按键输入已完成（2026-08-29）：** 实机 `keys=3`、`droppedKeys=0`，按键期间绿色采样 1458、松开后 0 —— 按键与触摸产生**同一个**视觉结果（一个状态驱动呈现而非两条路径）。链路：Android `KeyEvent` → Java → JNI → 无锁环形缓冲 → C++ 映射 → `PlatformFrame` → `InputAction` → 游戏代码。**跨语言键映射权威全在 C++**（platform-input.md lesson 5 的直接落实）：Java 侧**没有任何键表**，只原样传 Android `KEYCODE_*`，`androidKeyFromKeyCode()` 是唯一映射点；字母/数字/功能键三段连续区间按范围映射并用 `static_assert` 钉住 Tina `Key` 枚举的连续性（手写 26 个字母 case 正是该函数要防的转写错误，枚举重排会静默错位每个字母）；未映射键码返回 `Key::Unknown` 并**丢弃**而不发布（`Unknown` 对消费者永不可操作）。**两个键语义决定：** `BACK`→`Escape`（它就是取消/返回，与 Escape 同义；新增移动端专属枚举会迫使每个消费者处理同一意图的两种拼法）、`DPAD_CENTER`→`Enter`（D-pad/遥控器确认键）；`DEL`→`Backspace` 与 `FORWARD_DEL`→`Delete` 有专门测试，写反是经典失误且会让文本编辑删错方向。**未被引擎映射的键交还系统**（`onKeyDown` 仅在 native 确实消费时返回 true）—— 吞掉 `BACK` 会把用户困在 app 里，吞掉音量键会破坏设备控制。**窗口丢失同时释放按键与手指**（按下时窗口消失永远收不到 Up，留着位会 latch 到 run 结束，与滞留手指同一失败模式）。**触摸/按键两个环形缓冲共用一份实现**（`AndroidEventQueue<Event, Capacity>` 模板）—— 唯一复杂的是内存序，写两遍正是第二份出错的方式。**与 cocos2d-x 对照（读源码确认）：** cocos 的 6 个 `AndroidManifest.xml` **全部**用 `android:launchMode="singleTask"`，与本项目为修第五个缺陷所做的选择一致 —— 这不是 Tina 特有，是 Android 上持有进程级图形状态的通用要求；但 cocos 还加 `android:configChanges="orientation|keyboardHidden|screenSize"` **阻止**旋转重建 activity，即绕过问题而非解决它，Tina 刻意不加，因为让 Android 正常重建才会真正走 ADR 0034 的 surface rebind。**证据：** `tina_platform_android_tests` **50/50 实机通过**（+11：键映射区间端点、DEL/FORWARD_DEL 不互换、移动键折叠、未映射丢弃、区间不越界、键队列有界、按键 transition/held/repeat/丢弃/窗口丢失释放）；桌面 `tina_tests` 449/449、`tina_render_bgfx_tests` 114/114、`tina_runtime_ui_tests` 151/151、`tina_ui_tests` 839/839、`tina_platform_glfw_tests` 53/53、`tina_sample_2d` 300 帧 `status=ok` 指纹 `488c124f…` 未变、`CheckDocs` errors=0 warnings=0。**文本输入已完成（2026-08-29）：** 实机 `textCommits=8`、`droppedKeys=0`，`dumpsys input_method` 确认 `mInputShown=true` 且 `mServedView=dev.tina.TinaSurfaceView`。链路：软键盘 → `InputConnection.commitText` → JNI → 环形缓冲 → 严格 UTF-8 校验 → `TextInputTransition`。**文本必须走 `InputConnection` 而非按键：** 软键盘**根本不产生 key code**，它通过 `commitText` 交付整串（自动补全/粘贴/IME 转换都是一次调用），只处理 `onKeyDown` 的宿主在用户打字时什么都收不到；`onCheckIsTextEditor()` 必须返回 true，否则 IME 从不索取 `InputConnection`，键盘弹出但打字毫无反应。**事件自持字节：** `AndroidTextEvent` 内联 256 字节，因为 `string_view` 会指向在 JNI 返回瞬间失效的 Java 字符串而消费者在另一线程数帧后才读；校验放在**生产端**（JNI 转换处），那是唯一能把失败报告给始作俑者的位置；校验**复用引擎自己的 `countStrictUtf8CodepointsWithoutNul`** 而不另写 —— `PlatformFrameBuilder` append 时施加同一规则，两份实现终会分歧，届时被拒的是**整帧**而非一个 commit。**emoji/星平面文本已支持（同轮补齐，上一段记录的限制已解除）：** 必须用 `GetStringChars` 而**非** `GetStringUTFChars` —— 后者返回 **modified UTF-8**，NUL 编两字节且非 BMP 字符以 CESU-8 代理对到达，一个 emoji 变成两个非法三字节序列被严格校验器拒绝、字符静默丢失；UTF-16 才是 Java 实际存储形式。新增 `Core::convertUtf16ToStrictUtf8()` 放 **Core** 而非 Android 侧，因为这是「平台 IME 说 UTF-16、Tina 契约要严格 UTF-8」的通用缺口，Windows adapter 将来同样需要；只产最短形式（否则自身输出会被下游校验器拒绝），未配对代理/内嵌 NUL/输出溢出一律 nullopt 而非截断（截断出的半个多字节字符本身即非法 UTF-8）。实机 `textCommits=1` 确认 U+1F600 通过。**验证手段亦记录：** 非 ASCII **无法**从测试工具注入（`adb shell input text` 按 keycode 合成、表达不了代理对；驱动真实 IME 需要人），故 APK 提供 `--ez tina.commitEmoji true` 走**与真实键盘同一条** `commitText` 路径，而非绕过转换伪造结果。`tina_tests` 455/455（+6 UTF-16 用例：四种序列长度边界、代理对成四字节、未配对代理拒绝、内嵌 NUL 拒绝、溢出拒绝而非截断、空输入）、`tina_platform_android_tests` 60/60。**`setComposingText` 刻意不转发**（Tina 的四阶段 preedit 模型与 Android 不直接对应，C6 明确拒绝伪造阶段 —— cocos 的 `setMarkedText:` 存下 marked text 却什么都不发，因为其抽象无 preedit 概念可送；组词终以一次 `commitText` 结束）。**`deleteSurroundingText` 译成 Backspace 按键**（引擎此处无可删的 editable buffer，按键路径是其 TextEdit 消费者已处理的）。**一个按键切换软键盘**（手机无硬件键；游戏 latch 意向、宿主执行，且必须交替否则无法收起）。**证据：** `tina_platform_android_tests` **58/58 实机通过**（+8：UTF-8 接受/拒绝含截断与 overlong 与内嵌 NUL、容量边界可用与越界拒绝、队列有界、commit 成 transition、无残留状态、多字节字节完整、无队列亦可运行）；实机三条输入路径共存 `presses=1 releases=1 keys=2 textCommits=8` 零丢弃零错误；桌面 `tina_tests` 449/449、`tina_render_bgfx_tests` 114/114、`tina_runtime_ui_tests` 151/151、`tina_ui_tests` 839/839、`tina_platform_glfw_tests` 53/53、`tina_sample_2d` 300 帧 `status=ok` 指纹 `488c124f…` 未变、`CheckDocs` errors=0 warnings=0。注：`adb shell input text` 不支持非 ASCII（按 keycode 合成），故多字节路径由单测覆盖而非实机注入。⑧**已完成 preedit 组词文本与 caret placement（2026-08-30）：** 上一轮把 preedit 记作「需先决定如何映射」，本轮先定映射表再实现（见 [ADR 0032](adr/0032-mobile-platform-contract-boundaries.md) 的「C6 补充」）。`AndroidCompositionSession`（C++，Java 侧**不持有** composition 状态）把 `setComposingText`/`finishComposingText`/`commitText` 映射到四阶段：首次非空为 `Started`、后续为 `Updated`、空 preedit 与 `finishComposingText` 均为 `Cancelled`（Android 没有 cancel 调用；`Cancelled` 被 flow device observation 排除，故「放弃组词」不算用户活动），无 session 时这两个调用**什么都不发**。**commit 与 preedit 共用一条队列**，因为 `Ended` 必须先于它产生的文本而两条队列会被依次排空 —— 顺序反了「也能工作」（UI 自己会清 composition），但帧里会读成「文本先出现、组词后结束」。光标从 UTF-16 code unit 换算成 codepoint 并夹取（越界让 `PlatformFrameBuilder` 拒绝**整帧**）；preedit 容量 512 与 `UIImeCompositionState::MaximumPreeditBytes` 对齐（接受更多会让 `routeTextComposition` 以 `CapacityExceeded` 拒绝整帧）。窗口销毁取消在飞的组词，下一次 poll 发布（事发处在帧之外）。**同时修掉一个会让帧循环永久停止的缺陷**：`updateTextInputPlacement` 原先拒绝任何非空 placement，而 Runtime 每帧都在 TextEdit 聚焦时 publish caret，协调器把拒绝升级为 `LifecycleInvariantViolation`、`tick()` 终止且被 latch —— 应用在文本框获得焦点那一刻停止产帧，此前未暴露只因 demo 里没有 TextEdit。改为 latch physical-pixel caret，供宿主经 `CursorAnchorInfo` 上报（候选窗属输入法进程，应用只能上报几何；读取**不清除**，且只在 `requestCursorUpdates()` 后上报）。新增 `UIUpdateContext::imeCompositionActive()`，因为平台的 stage 计数在无焦点时照样递增 —— 只有它能区分「平台通了、UI 从未看见」。demo 加了可聚焦 TextEdit（否则 UI 半条链不可观测）。`tina_platform_android_tests` **80/80** 实机（+20）。**剩余：** **真实设备上跑 Vulkan 路径**（模拟器 Vulkan 驱动不可用，只能验证 GLES）、**候选窗跟随的人工验收**（所装输入法是否索取 cursor updates 不由本工程决定，归 `TEXT-001`）、iOS。**不在范围内：** 资源打包、CI | Unit + Platform |
| GAMEPLAY2D-001 | InProgress | P1 | 让 authored 2D 场景真正跑起来：`tina_gameplay2d` 的 `Scene2DPhysicsBridge` 与 `Scene2DRuntime`，以及 `samples/2d_authored_scene` 消费者 | `2D-SERIALIZATION` schema v4；[ADR 0030](adr/0030-gameplay-2d-binding-and-physics-bridge.md) 与 [ADR 0031](adr/0031-scene-2d-runtime-ownership.md)（**均为 Proposed，未经 maintainer 审阅**） | restore/capture 对八个 payload 往返（含 physics 与 name）；`World2DSceneIndex` 重名与 stale-handle；`Scene2DPhysicsBridge` 父子归属/容量/shutdown 顺序；四种 resource kind 各自实例化与 shutdown；乱序调用（extract 早于 commitReady）必须失败；`active=false` 既不 update/extract 也不可达；audio clip lease 活到 voice 终态；shutdown 幂等且先停 voice 再放 lease；TileMap 全 layer stream 而仅 visible layer emit；authored transform 决定 TileMap 原点与 Fx burst origin；`fixedUpdatePhysics()` 固定 `step→applyTo→updateWorldTransforms` 且子节点随父 body 重新发布；`tileMap()`/`tileLayers()` 让 GridCollision 查到真实 solid cell。**2026-08-27 证据：** `tina_physics2d_tests` 76/76（Scene2DRuntime 15）、`tina_asset_tests` 316/316、`tina_scene_tests` 171/171；`tina_sample_2d_authored_scene --frames=300` `status=ok` exit 0，两次运行逐字节一致，60 帧亦通过；29 个 gate 字段与真实输出逐一核对为 0 mismatch。**待：** 两个 ADR 的 maintainer 审阅；gate 脚本本轮受 shell 权限限制未能整体执行（字段断言已用等价逻辑离线核验） | Unit + Product |
| NET-001 | InProgress | P2 | `Tina::Network`：数值 IP/endpoint、UDP、readiness poller、TCP 连接与 listener、`IByteStream`、HTTP/1.1、WebSocket、DNS，以及可选 `Tina::NetworkTls`（mbedTLS） | [ADR 0033](adr/0033-network-module-boundaries.md)（**Accepted**，2026-08-29 按已落地实现校准，含「与首版的偏差」九条） | 传输统一 owner-thread readiness 多路复用（每帧一次 `WSAPoll` 或 `poll`，timeout=0），除 DNS 外零 worker、零锁、零 marshal；`getaddrinfo` 无可移植非阻塞版本，故 `DnsResolver` 用 `scheduleIo` + per-request `atomic<Outcome>` + owner 轮询，**不用 `postMain`**（`pumpMain` 不捕获异常、`postMain` 与 `scheduleIo` 共用 `m_mutex`、`TaskCallable` 每事件一次堆分配），亦不像 c-ares 自建 DNS over UDP（会放弃 nsswitch/DNSSEC/mDNS/split-horizon/NRPT）。`IByteStream` 使 `http`/`https` 与 `ws`/`wss` 各只有一份实现，且 `Tina::Network` 不依赖可选 TLS 适配器。TLS 信任锚取平台 store（进程内只读一次并缓存）、显式锚点替换而非追加、不内嵌 bundle；跳过校验需两个独立 opt-in 且 Release 拒绝。协议层对请求走私面 fail closed：同时带 `Content-Length` 与 `Transfer-Encoding`、header 冒号前空格、body 短于声明长度、WebSocket 保留位/未知 opcode/分片控制帧/孤立 continuation。声明上限本身可用（接收 slot 比上限多一字节，使 POSIX 静默截断可检测）；丢弃与延迟分开计数，队列满不计入丢弃。公开头不出现 socket/Winsock/mbedTLS 类型。**2026-08-29 证据：** `tina_network_tests` 166/167（1 skip：本宿主 loopback 吞下 8MB 发送，背压不可达；字节守恒仍无条件断言）、`tina_network_tls_tests` 27/27（含对 in-process mbedTLS 服务端的真实握手、主机名不匹配与无关签发者拒绝、应用数据往返、`close_notify`、HTTP over TLS、WebSocket over TLS）、`tina_tests` 425/425、`tina_sample_network --frames=300` `status=ok` 且两次运行逐字节一致（60 帧亦通过，参数错误 exit=2）、SDK 头扫描 299/299（token 清单已扩 socket/Winsock/mbedTLS 并以合成泄漏头反向验证）。`tina_sample_network` 是 tests 之外首个消费者，headless 无 GPU 无 EngineHost，在同一 pump 循环里跑 UDP/DNS/listener/三条客户端连接/HTTP/WebSocket。`tools/windows/RunNetworkGate.ps1` 已固化该拓扑（跨两棵树、sample 跑两次要求逐字节一致、按 JSON 字段类型比较、TLS 侧显式要求四个握手用例出现在 OK 列表），证据 `artifacts/gates/network-20260829.json` `ok=true`；反向验证：把任一期望值改错，门禁确实失败。`CheckDocs.ps1` errors=0 warnings=0（本轮首次执行成功，此前受 shell 权限限制）（**证据失效，更正 2026-09-01：** 见 `73e050d3` —— 该门禁遇到含 C++ lambda 的文档即抛未捕获异常并中止其后全部文件，「执行成功」实为扫描零文件，本条 errors=0 不成立，需重跑）。**待：** ①**Linux 一次未验证** —— 十个组件的 POSIX 分支写了但从未编译或运行，含 `SystemTrustStore.cpp` 的六条 bundle 路径探测，为当前最大未知面；②所有测试在 loopback，真实丢包/乱序/MTU 分片/NAT 与 `WouldBlock`、`receive()` syscall 上限分支未覆盖；③`RunSdkConsumerGate.ps1` 完整 relocated consumer gate 未随 `Tina::NetworkTls` 复跑；④无证书固定（UE5 有 `[SSL] +PinnedPublicKeys`），且不委托验证裁决给 OS（distrust/EKU/CTL/吊销未查询） | Unit + Product |
| CORE-DIAG-001 | InProgress | P1 | 收口 Core CrashHandler 与 TinaEditor fatal report 的跨平台公共契约 | 当前 `CrashHandler` 首切片与 Editor `%TEMP%/tina_editor_crash.txt` 接线 | 保持 opt-in、first-report-only、terminal/non-recovery 边界；Windows report path 使用真实 UTF-8→wide API，mandatory hook/file-open failure 的返回语义明确；受控子进程覆盖 terminate/abort/选定 fatal SEH/purecall/invalid parameter、非 ASCII report path、armed marker、symbol/no-symbol 与重复安装；Linux terminate/abort 生成可读 artifact并明确无 backtrace；Editor 顶层 `Core::Error` 的 domain/code/origin/context 与 `status=fatal` 可验证。不得把自动文本 gate 写成 minidump、损坏堆必达或 crash recovery。**2026-08-28 本轮（跨平台契约对齐，仅 Windows 验证）：** ①`BacktraceNamesTheCallingFunction` 此前无平台守卫就断言符号名，而非 Windows 的 `emitBacktrace` 只输出 `unavailable on this platform` —— 该测试在 Linux 必然失败，且失败原因不在 handler。现改为按平台分别断言：Windows 要求解析出 `reportFatalAndTerminate` 符号，其他平台要求显式出现 `unavailable on this platform`，**两边都断言**而非 skip，因为「段落消失」会让读者无法区分「没有栈帧」与「没装 handler」；`captureBacktrace` 公开注释同步限定符号解析为 Windows-only。②armed marker 与 per-run 截断此前只有 Windows 具备（POSIX 用 `"ab"` 打开且不写 marker），而 `docs/testing.md`/`editor-2d.md` 让操作员在任意平台依赖它 —— POSIX 探测改为 `"wb"` 并写同样的 marker，报告因此必定描述本次运行。新增 `InstallArmsTheReportFileAndTakesOwnershipOfThisRun` 覆盖 marker 存在、命名应用与旧报告被截断。③`EditorApplication` 此前丢弃 `installCrashHandler()` 返回值，而 GUI subsystem 的 stderr 不可见，report file 打不开时每份崩溃报告都静默丢失；现在输出 `status=warning` 并指明路径。**证据：** `tina_tests` 414/414（+1，`CrashHandler*` 11/11）、`tina_editor_app_tests` 23/23、`tina_editor_tests` 119/119；`TinaEditor --auto-demo --frames=70` 2D/3D 均 exit 0 `status=ok`（2D `automaticAuthoringStage=57`、`selectionVerified=true`），真实 `%TEMP%/tina_editor_crash.txt` 确认含 armed marker；`tina_sample_2d` 300 帧 `status=ok`、指纹 `488c124f…` 未变。新测试用 revert 验证过：改坏 marker 文案即失败（首版测试因 applicationName 含 `armed` 子串而假通过，已改用 `tina_crash_probe`）。**2026-08-28 第二轮（Windows fatal 矩阵，+4 用例）：** 补上四个绕开 `std::terminate` 的入口，因此 terminate hook 的既有证据对它们不成立：access violation（受控 null 写）、pure virtual call（基类构造期调用纯虚）、CRT invalid parameter（`fclose(nullptr)`），以及 cascade 只产一份报告（`terminate → abort → SIGABRT` 两个入口一次死亡，banner 计数=1 且第一个 reason 存活、未被 `SIGABRT` 覆盖）。全部按 `#if defined(_WIN32)` 守卫，因为这四者都是 CRT/Win32 机制，在 POSIX 会以「无 handler 输出的 SIGSEGV」失败——正是 backtrace 用例踩过的坑。**AV 用例发现一个真实缺陷：** operation/address 的分类只写在 `onUnhandledSeh`（`SetUnhandledExceptionFilter`）分支，但 vectored handler 以 `AddVectoredExceptionHandler(1, ...)` 注册在前、且 report latch 是 first-wins，所以**真实 AV 一直由 vectored 路径上报并丢掉读/写分类与 faulting address**，只剩 `code 0xC0000005`；现抽出 `describeExceptionDetail()` 供两个入口共用，报告实测为 `detail: vectored, write to address 0x0000000000000000`。**证据：** `tina_tests` 418/418（`CrashHandler*` 15/15）、`tina_editor_app_tests` 23/23、`tina_editor_tests` 119/119；`TinaEditor --auto-demo --frames=70` 2D/3D 均 exit 0 `status=ok`（2D `automaticAuthoringStage=57`）；`tina_sample_2d` 300 帧 `status=ok`、指纹 `488c124f…` 仍未变。AV 用例已用 revert 验证：把 vectored detail 改回只印 code 即失败。**待：** 非 ASCII Windows 路径的真机验收；Linux 真机 terminate/abort artifact 复跑（Linux 分支按平台条件编写但未构建未运行）；stack overflow / GPU driver fault / 损坏堆**明确不纳入**自动 gate（那些状态下报告本身也可能不完整） | Unit + Platform + Manual |
| ASSET-ID-001 | InProgress | P1 | 版本化并统一 importer 默认 output `AssetId` 派生（glTF path/XOR 身份缺陷已关闭） | Source Import schema 1.1、importer version/full recook、`GltfCookIds` | glTF 默认 seed 使用 canonical source-root-relative locator + asset kind + output ordinal/channel，所有输入字节影响完整 identity；`a.png`/`b.png` 类、转位、长路径、多 prim/texture/animation 与跨 unit corpus 不再出现旧构造碰撞；工程根移动/reopen 保持默认 ID；算法切换显式 bump importer contract 并让旧 baseline full recook，不静默复用或双读；显式 ID 与 duplicate-owner fail-closed 保持；公共文档不宣称数学上无碰撞。**状态更正（2026-08-28）：** 本行此前是 `Planned` 并把 path/XOR 描述为现存缺陷，但主体实现已由并行 agent 于同日 `3499f8ee` 落地 —— `src/asset/DerivedAssetId.hpp` 的 `deriveVersionedAssetId()`（version 2，双流 FNV-1a over locator + AssetKind + roleTag + ordinal + channel）已被 glTF 与 media cooker 共用，旧 XOR 构造在仓库中已无残留。该提交注明「verified rather than reviewed line by line」，故本轮补审并补齐验收面缺口。**本轮（corpus 补齐 + 一处隐患记录）：** ①新增 media 跨 kind 与 repeat 稳定性、长 locator（仅差最后一字符）用例；长 locator 走 derivation 入口而非真实文件，因为足够长的路径在 fixture 根下会超过 Windows MAX_PATH，文件版只能证明文件系统拒绝了路径。②新增 mesh-only glTF 输出与 media id 的交叉比对，并在 `CooksMetallicRoughnessAndNormalTextureDeps`（唯一真正产出 tag 复用输出的 fixture）内加入 `0x75` 对比。③**记录一处隐患：** `TextureMediaAssetIdTag` 与 `GltfMetallicRoughnessTextureAssetIdTag` 同为 `0x75`、`AudioMediaAssetIdTag` 与 `GltfAnimationAssetIdTag` 同为 `0x77`，而 tag 是 AssetId 的首字节；`0x75` 两侧 AssetKind 均为 `Texture2D`，仅靠 `channel`(1 vs 0) 区分。实测把 `assetKind` 或 `channel` 从 hash 中移除，323 个 asset 用例**无一失败** —— 说明这两个输入当前并非真正的判别项（同 importer 内靠 tag、跨 importer 靠 locator 中的扩展名），属 defence in depth。已在头文件写明该结论与「新 producer 应选未占用 tag」的约束，不改动派生算法以免触发无谓的 identity 迁移。**证据：** `tina_asset_tests` 323/323（+3）、`tina_asset_format_tests` 124/124、`tina_editor_tests` 119/119、`tina_editor_app_tests` 23/23；`tina_sample_2d` 300 帧 `status=ok`、指纹 `488c124f…` 未变。**待：** 多 prim/多 texture/多 animation 的更大 corpus 与 Editor 产品级 reopen/工程根移动验收（Editor Product 证据面尚未覆盖） | Unit + Integration + Editor Product |
| UI-MODERN-DESKTOP-001 | InProgress | P1 | 建立 `Tina Modern Desktop` 设计系统与 Desktop Shell；现有 `Tina Studio Compact` 成为 Compact 密度基线，新增 Comfortable，用 semantic Token、State Layer、Shape/Spacing/Elevation 和统一控件 metrics 替换平铺 Theme 与直接 lighten/darken | `UI-STUDIO-DESIGN` 的代码基线；Retained UI Theme/Style/Motion/Surface/Menu/SplitView/TabView 已落地。其外部 state-feedback 补跑不阻塞当前 program | **TMD-00..06 已完成：** 旧 Theme 字段/factory/compatibility 已破坏式删除；Dark/Light x Compact/Comfortable semantic Theme、State Layer/FocusVisible/Elevation、统一 metrics、基础与导航/集合/浮层 chrome 已落地；`UIIconButton`/`UIFormField`/`UIDialog` 复用既有行为并提供精确 fixed-capacity budget、UIContext/UITreeUpdater/Runtime facade。2026-08-19 集中门禁为 UI 761/761、Runtime UI 142/142、UI-Render 28/28、UIA 13/13，新增公开头 header-isolation 与第三方 token 扫描通过。**TMD-07 已完成并通过集中 gate：** Showcase 已改为 Command Bar/Explorer/可滚动 Component Canvas/Token Inspector/Status Bar 工作台；已消费 IconButton/FormField/Dialog，并输出 24 controls、3 profiles、5 bands 与 density lifecycle JSON。修复两个 density 生命周期缺陷：density 现在经 `PrimaryWindowUIRootBuilder::setProductTheme()` 在 `createRoot()` 之前确立（此前先建 root 再设 Theme，使 Showcase 无法以默认 Comfortable 启动）；density 重建改为 destroy-then-rebuild —— 旧 root 在 `requestReplace()` 前释放，replacement 在零 live root 下构建，`updateUI()` 在交接未提交时 fail closed，实测 live-root 峰值为 1 故 `rootCapacity` 由 2 降为 1（双 root 原子交接不可行：committed structure/layout 遍历全部 root、candidate Modal 会关闭 active root 浮层、layout 容量 guard 跨 root 计数）。Sample 尺寸断言放宽为不得大于请求值，宿主压缩（1600x1066 → 1600x980 @200%）不再误报为产品失败。2026-08-19 集中门禁为 UI 762/762、Runtime UI 142/142、UI-Render 28/28、UIA 13/13；Showcase smoke 12/12（dark|light × compact|comfortable × 960x640|1280x800|1600x1066）；`--auto-demo` 300 帧 densityRebuilds=2、state/root 生命周期 3/3、finalDensity 回到 comfortable、imageFreeFrames=0；DOC-002 errors=0 warnings=0。**TMD-08 已实现主体：** 新增 `samples/desktop_shell/` 作为 Desktop Shell reference 与 SplitView 的首个产品消费者。一棵 root、五个 band，Workspace 由三个嵌套 SplitView 组成（`左 Dock \| main`、`center \| Inspector`、`Viewport \| Timeline`）；几何一律从 `splitViewMetrics()` 读取 committed 值，不由产品推算。命令、Document Tabs、View Menu、Modal Dialog、Tooltip、产品 icon atlas 已接线，破坏性命令经 Dialog 确认。Pane 可见性分为「用户意图」与「档位解析」两层，使响应规则 3 的显式 Inspector 切换真正可达。记录两个实现约束：focus 目标必须先进入 committed 快照（GameStateEnter 期间无提交，初始 focus 必须延后），`setSplitViewFraction()` 的效果要跨一次发布才可验证。证据从 committed 状态读回而非由意图断言：Menu 取自 Menu store、Dialog 取自 committed Modal 几何、Tooltip 取自 committed metrics、Splitter 取自跨帧 pane 宽度；`leftDockWidth=200` 恰为冻结最小值，证明 0.01 fraction 被 SplitView clamp。产品 icon atlas 使用同一 `Texture2DFrameResourceResolver`/FramePin/DisplayList 链，自动 gate 验证 65 帧 resolve/pin 命中与释放后 texture invalidation。`--auto-demo` 65 帧在 dark/light × compact/comfortable 与 960/1280/1600 共 7 组全通过；门禁 UI 762/762、Runtime UI 142/142、UI-Render 28/28、UIA 13/13，DOC-002 errors=0。**真实 DPI 门禁已就绪：** `RunTmd08DesktopShellVisualGate.ps1` 在 build/launch 前校验主显示器实际 scale，只接受 FreeType 产品图，逐轮采集 Dark/Light × Compact/Comfortable × 960/1280/1600 共 12 个 case，并可用 peer report 强制同 EXE/字体 hash 与 logical geometry 一致。**DPI 证据暂缓：** 当前宿主固定 200% 且机器负载较高，真实 100%/150% 配对报告留待合适环境补跑；auto-demo 不合成指针/键盘输入，端到端输入仍由 Runtime UI 测试覆盖。**TMD-09 源码迁移已完成：** 2D/3D 产品已消费 Compact Theme metrics；EditorApp 在 `createRoot()` 前绑定 Dark/Compact Modern Desktop Theme，根按 Command Bar、Document Tabs、Context Toolbar、Workspace、Status Bar 排列，Workspace 使用 `Left Dock | Main`、`Center | Inspector`、`Viewport | Timeline` 三层 SplitView；Button/TextEdit/Tab、List/Tree row、Splitter、Status Bar 与 spacing/padding 改读 Theme metrics，旧局部 surface ColorToken/StyleClass 和自动换色路径已删除，Scene/Asset/Render owner 不变。**TMD-10 源码已完成：** `Desktop::CreateEngineOptions::followSystemColorScheme` 默认关闭并显式 opt-in；Windows GLFW 私有 observer 发布 Tina-owned Dark/Light event，未成功 append 时保留待发布状态；Runtime owner-thread coordinator 在 UI Update phase 保持 density、事务应用 canonical Theme，并忽略含 event-stream reset 的不完整批次；Platform/observer/coordinator 测试源码已补。**TMD-09/10 集中 gate 已通过：** 2026-08-19 单一 product-2d build tree 增量构建全部受影响 target；Core/Platform 370/370、UI 763/763、Runtime UI 145/145、GLFW 43/43、UI-Render 28/28、FreeType 3/3、UIA 13/13、Editor 114/114、EditorApp 13/13。Editor 2D/3D 各 600 帧、Desktop Shell 120 帧、2D 300 帧、3D 60 帧均 exit 0。修复 OS scheme 事件同帧后续 capacity reset 时 observer 提前提交、导致不重试的问题。installed `DesktopBootstrap` consumer gate 另验证 262 个安装头无第三方 token，relocation、exact-version、missing-component/component-isolation 及外部运行均通过。**TMD-11 进展（2026-08-19）：** `tina_bench_tests` 10/10；`ui_static_commit_v1`、`ui_component_build_v1`、`ui_style_state_v1`、`ui_motion_v1`（seed 0/1/2，0/64/1024 active tracks）冻结 workload 均 `status=ok`，确定性 counter/checksum/allocation/clean-rebuild invariant 通过；开发机墙钟维持 provisional，fixed-machine hard gate 由 `PERF-002` 跟踪。**待：** 真实 100%/150% DPI 配对视觉与最终 doc 收口 | Unit + Integration + Visual + Product + Platform + Consumer |
| UI-GRID-COLLECTIONS | Done | P1 | 收口 `UIVirtualGridView` 与 `UIDataGrid`，并迁移 Editor Project Assets / Source Imports | 既有 Retained UI transaction、Theme、Input、Runtime phase facade | VirtualGridView 响应式等宽列、固定 item pool、纵向滚动和二维选择；DataGrid 固定 column/row/cell pool、精确列宽/header、虚拟行、双轴滚动和二维选择。两者均接入 Pointer/Keyboard/Gamepad、paint、semantics、committed metrics、failure-atomic publication 与 `PrimaryWindowUITreeUpdater`；100k logical item/row、capacity overflow、disabled navigation、scrollbar、warmup 后 PMR 不增长由新增单测覆盖。Editor Project Assets 使用 VirtualGridView，Source Imports 当前使用 `Kind | Source | Status` 三列 DataGrid（Source 约 190 logical px）；Point Light color callback 重复注册已消除。**2026-08-21 集中 gate：** VirtualGrid/DataGrid 定向 10/10、Runtime facade 定向 3/3、UI 782/782、Runtime UI 148/148、UI-Render 28/28、EditorApp 20/20；Editor 2D/3D 各 68 帧 exit 0。gate 同时修复 2D 自动缩放裁剪 TileMap chunk，以及负斜线 AABB 从 double 转 float 时向内舍入导致 3D 网格提交失败；最终 2D `tileMapEmittedSprites=12`、`gpuViewportSprites=13`，3D `viewportGridSegments=61`，两边 `renderExtractions=68` | Unit + Runtime + Integration + Product |
| UI-003 | InProgress | P1 | 建立跨 DPI/GPU 容差视觉门禁 | 稳定门禁机 | **已完成** ContentScale* 单测 + 逻辑尺寸矩阵 + font fingerprint。**2026-08-03：** 五尺寸 ROI baseline 已按 tip 重冻，size-matrix 默认 tol=40 验证 ok。**2026-08-10：** 当前 Windows 宿主完成 100%/150%/200% raster（logical 960×540 → capture 960×540/1440×810/1920×1080）对应 baseline 与独立复跑，ROI logical rect、avgRgb、font fingerprint 全部匹配；100%/150% Win32 system DPI 探针为 96/144（[ui-003-tip-evidence.md](evidence/ui-003-tip-evidence.md)）。**待** 多显示器混合 DPI 与跨 GPU 像素金标 | Unit + Visual |
| PERF-002 | InProgress | P1 | 建立固定机 hard gate 与多进程 median/MAD 协议 | PERF-001 `tina_bench` schema v1/fingerprint | **协议 + tip provisional 证据：** `run_benchmark_gate.py` 13 unit OK；5 进程 provisional 跑通 `null_runtime_frames` / `ui_static_commit_v1` / `ui_motion_v1`（[perf-002-provisional-evidence.md](evidence/perf-002-provisional-evidence.md)）。**待：** 固定门禁机 + `status=approved` machine profile/baseline + Release hard gate | Benchmark + Platform |
| SDK-001 | Done | P1 | 建立可安装的 Tina Game SDK 与外部 consumer gate | 当前 Public API/target 边界稳定；不依赖 `UI-FLOW-001` | Windows moved-prefix 四 consumer、Linux Docker GameSDK 与 Ubuntu24.04/GCC13 producer → Debian13/GCC14 consumer 均 exit 0。2026-08-16 [ADR 0024](adr/0024-sdk-abi-compatibility.md) Accepted，package 收紧为 pre-1.0 strict exact-version ConfigVersion；`DesktopBootstrap` 最终 gate 验证 241 个安装头、三段 `0.0.1` exact 以及相邻版本/tweak/range 拒绝、12 个 relocated package 文件、missing-component/component-isolation 及外部 configure/build/run，consumer 输出 `status=ok`（[evidence](evidence/sdk-001-windows-consumer-evidence.md)）。**2026-09-01 校准：** 上述 241 已过期，当前为 319 个安装头。更要紧的是本行标 `Done` 期间门禁在干净树上其实是坏的——脚本手抄了一份「install 前先编哪些目标」的清单，只列 `tina_runtime`/`tina_scene`/`tina_asset`，`Save`/`Gameplay`/`Network`/`Animation3D` 无人链接因而从未被编译，install 撞上不存在的 `tina_save.lib` 直接失败（`tina_editor` 只是碰巧被别的构建留在树里才没暴露）。已由 `75bfbb06` 改为在每处 `install(TARGETS)` 旁收集目标名并聚合出 `tina_sdk_install_artifacts`，三个门禁脚本只编这一个目标，使「装什么」与「先编什么」同源。正式 supported ABI tuple 的 artifact/API/symbol baseline 与 previous-object probe 继续作为 release checklist，不回写成 SDK-001 已证明 | Consumer + Platform |
| EDITOR-UI-UX-001 | InProgress | P2 | Tina Editor 整体 UI/UX 美化与 Project Assets 视觉升级：AssetTile、缩略图/类型图标、EmptyState、导入状态、外部文件拖入、Hierarchy/Inspector/Viewport/Timeline/Output/Status Bar 对象语言 | `UI-MODERN-DESKTOP-001` 的 Tina Studio Compact 基线；`2D-EDITOR` 当前 Project Browser、SourceImportService、Hierarchy/Inspector/Viewport 状态流 | **已落地：** Project Assets 的 ASCII case-insensitive name/kind 搜索、All/2D/3D/Media 组合过滤、stable key/selection、Grid/List 固定 metrics（约 132x72 / 280x30）；Texture/Sprite 使用 cooked 缩略图，其他类型使用类型 icon fallback，并显示 secondary label/status slot 与 Missing 语义；Breadcrumb、Start Center、EmptyState、导入 Activity phase 文本、失败 InlineCallout、Retry/Open Output、双击按 AssetId 打开、anchored 右键菜单与稳定 AssetId 命令已接入；Source Imports 的 `Kind | Source | Status` 三列（Source 约 190）；已加入按 AssetId 的 import history，稳定记录 `Queued/Preparing/Copying/Cooking/ReadyToCommit/Committed/Error`，只标记当前批次已知输出，Catalog commit 后补齐新输出并在项目切换时清理；GLFW 3.4 FileDrop 通过 Tina-owned Platform→Runtime→Editor 有界转发，覆盖 Windows、X11 与 Wayland ingress；释放到 Project Assets 或 active viewport 都自动导入资源，且不会创建或修改场景节点；Editor 私有 DropOverlay 已接入 release 后 Accepted/Processing/Committed/Rejected/Failed 状态，失败仍保留旧 Catalog。**2026-08-24 Windows 统一历史证据：** `tina_editor_tests` 112/112、`tina_editor_app_tests` 23/23，`tina_tests` 377/377，2D/3D 70 帧 smoke 均 exit 0。**当前限制：** 标准 GLFW callback 仅提供最终 drop/release，没有 drag-enter/drag-over hover；新资源在 worker 返回 validated outputs 后才获得 AssetId 级历史。**待：** Linux 构建/运行与真实产品拖放证据、Explorer 人工拖放，以及 Dark/Light、DPI、UIA、跨 GPU 视觉证据；保持 InProgress，全部证据闭环前不得转 Implemented | Source + Unit + Product + pending Visual/Platform |
| 2D-EDITOR | InProgress | P2 | 完整 2D/3D Editor：current-schema World2D/Prefab/TileMap/SpriteAnimation authoring、动态 Hierarchy、TRS Inspector、viewport navigation/grid/gizmo/marquee、scene operations、隔离 PlaySession、多文档/项目资源与保存闭环 | 2D-SERIALIZATION | **功能代码已落地：** 四类 current-schema document/file、bounded history、per-tab session/baseline、Save/Save As 与 dirty-close Modal；Project Browser/Asset Inspector、Catalog refresh、New/Open、source import fresh-stage/participant transaction 与 active-stage 恢复；Windows native dialog 的 Import Files 支持有界多选；项目内 source 直接使用，外部 PNG/JPEG/WAV 经整批预检后事务复制到 `Source/Imported`，同名同内容复用、异内容追加后缀且失败回滚，外部 recipe/glTF 因相对依赖明确拒绝；临时候选继续完成物理路径校验、去重和原子 intended-set 提交，既有/批内重复 unit 可触发 reimport；左侧 `Source Imports` 的 `Kind | Source | Status` 三列 DataGrid（Source 约 190 logical px）显示完整 intended set、unit 状态与选择，Remove 不读取被删文件，可删除磁盘上已经消失的 stale unit；删除最终 unit 从有效 baseline 增量发布空 Catalog/空 import state，首次 full cook 仍拒绝空集合；Linux `zenity`→`kdialog` adapter 保持单选包装。Editor 默认无帧数上限交互运行；只有显式 `--auto-demo --frames=<N>` 且 `N >= 60` 才执行自动 authoring，普通 `--frames` 只检查通用不变量。Hierarchy 从 canonical 2D/3D Node 的 stable ID/parent 动态重建，不依赖预定义名称或固定行号；Add/Duplicate/Delete/Focus 同时接入 2D/3D，拖放行中部执行 reparent、上下边缘执行同级排序，拖到 document root 回到场景根，循环、跨父级边缘排序与 no-op 均 fail-closed。Inspector 已接入多选 `Mixed` 显示与 optional batch apply，一次 `replace()` 保留未编辑字段，2D Rotation Z 强制发布平面 quaternion，no-op 不发布 revision/dirty。Editor 已破坏式切换为单一 Node 模型：Create Node registry 是 Hierarchy 创建器、Kind 和 Inspector 的唯一类型词表；Inspector 删除 Components Header、Add/Remove Component 与兼容菜单，只由 `EditorNodePropertyOperations` 编辑当前 Node kind 固有的 Rendering/Camera/Light/Occlusion/Animation 属性。World2D optional payload 与 3D mesh/material 仅作为 current-schema wire 表示；旧式多 payload 混合不再按组件优先级兼容，而是 fail-closed。多选只在 Node kind 一致时发布专属属性，`Mixed` 保留未编辑值，成功最多一次 `replace()`，类型不匹配、非法值与 no-op 保持 canonical history 不变。旧空节点包装 API 已删除，公开 Scene authoring 统一使用 `addWorld2DNode` / `addWorld3DNode` 及 Node hierarchy operations；既有 Windows 统一 build/gate 历史证据：`tina_editor_tests` 112/112、`tina_editor_app_tests` 23/23、`tina_tests` 377/377，2D/3D 70 帧 `TinaEditor --auto-demo` 均 exit 0；仍待跨 DPI/GPU 视觉金标与 Linux `zenity`/`kdialog` 真实 dialog 产品门禁。`EditorViewportNavigation` 已接入 2D pan/anchored zoom、3D orbit/pan/dolly、Focus/Frame All、direct view publication 与 Perspective/六轴 preset；2D/3D workspace 分别持有 camera session，preview rebuild 不覆盖已初始化视角；`EditorViewportGrid` 提供 2D orthographic/3D perspective ground grid。`EditorTransformGizmo` 已接入 Translate/Rotate/Scale、World/Local、snap 与 axis/plane/ring/uniform handle；EditorApp group transaction 过滤 selected parent 的 child，以 root 平均 world position 为 pivot，批量 world→local 反算并在 shear/零 scale 时 fail closed，最终一次 `replace()` 提交；`EditorMarqueeSelection` 已接入固定容量 Replace/Add/Toggle 多选、selection revision 冲突取消与空选择同步 Hierarchy document root；`EditorPlaySession` 已接入 Play/Pause/Step/Stop，以 canonical snapshot 隔离运行并在 active 时锁定 authoring/tab switch。`Ctrl+S`/`Ctrl+Shift+S`/`Ctrl+Z`/`Ctrl+Y`/`Ctrl+D`/`Ctrl+0`/`Ctrl+1`/`Ctrl+2`/`Ctrl+F`、`Delete`、`F6`/`F7`/`F8` 与 `Escape` 的 Editor frame shortcuts 已接线，未绑定会干扰 TextEdit 的裸 Q/W/E/R。navigation/grid/gizmo/marquee/scene operations/play session 的模块单测与 header-isolation 源码均已接线；本轮五组 Editor 定向 suite 33/33 通过，2D/3D 60 帧 `TinaEditor --auto-demo` 扩展 smoke 均 exit 0：`automaticAuthoringStage=46`，Translate/Rotate/Scale commit=`1/1/1`，Rotate/Scale group commit=`1/1`，maximum gizmo targets=`2`，navigation batch=`2`，marquee commit=`3`，scene Add/Duplicate/To Root/Reparent/Delete=`1/1/1/1/2`，Play Start/Pause/Step/Resume/Stop=`1/1/1/1/1`，最终 entity count=`5`。source-import 定向 executable（含 batch selection 与 Texture/Audio 混合选择）通过；mixed recipe/glTF 60 帧 smoke exit 0，unit total/recooked=`2/2`、start/complete/failure=`1/1/0`、Catalog reload=`1` 且 state committed。普通图片音频一步导入当前源码契约：`.png/.jpg/.jpeg` 经 Texture importer 只 cook 一个 canonical root-relative path-derived Texture2D，Sprite2D 可直接引用该 ID，不再生成全幅 Sprite wrapper；`.wav`（PCM16）仍 cook 为 AudioClip，四层 kind（capture/probe/pipeline/editor）与 `--import-texture`/`--import-audio` CLI 保持一致。media ID 已修复首字符覆盖导致的构造碰撞并增加 distinct-path case。旧 `MediaCookTests` 3/3、PNG+WAV smoke `objects cooked=3` 是契约切换前证据，只保留为历史，不证明当前 tip；下一次 Editor 大功能统一 gate 需重建并运行当前 4 个 MediaCook case，产品预期改为 units=`2/2`、objects cooked=`2`、state committed 与 reopen 恢复。Editor 视口视觉链的 Line/Ellipse 代码已落地：grid、gizmo 轴线/平面边界使用 Line，rotation ring 使用单个向内描边 Ellipse，旧 axis-aligned 多段近似及其预算代码已删除；`EngineConfig::renderMsaaSamples` 新增引擎级 backbuffer MSAA 配置（0/2/4/8/16，默认 0），Editor 已删除旧 stair 网格遗留的 12K node / 32K paint / 16K DisplayList 启动容量（`editor/app/` 内已无 `12288`/`32768` 字面量，`createEngineConfig` 不再覆盖 node/paint 容量）。**更正（2026-08-28）：** 「恢复默认 UI/DisplayList 容量」与「8K draw-call 上限」在写下时（`ce768bf1`，08-10）成立，但已被 08-25 的 layout debugger（`18f55cfa`）取代 —— 当前 `EditorApplication.cpp:109-112` 按 `LayoutDebugDisplayListEntryCapacity = 48K`（`EditorWorkspaceState.hpp:171`）设置 DisplayList command/batch 容量，draw-call 上限随之为 `EditorRenderDrawCallCapacity = 48K + 1024`。这不是内存修复的回退，而是后续功能的新基线；单 encoder 与 world pass 无 MSAA 仍成立；world pass 不再启用 8× MSAA，bgfx 使用单 encoder，三类 shadow map 只在首次出现对应 shadow pass 时事务创建，samples 与像素 gate 保持 0；这些内存修复尚未构建或运行，旧的 runtime_ui 125/125、render_scene 52/52 与 2D/3D 600 帧 smoke 证据不覆盖本轮改动。Line/Ellipse 的跨 DPI/GPU 视觉证据继续由 `RENDER-LINES-001` 跟踪。**待：** 跨 DPI/GPU 视觉金标和 Linux `zenity`/`kdialog` 真实 open/save/folder/cancel 产品门禁。上述证据完成前保持 InProgress | Unit + Integration + Product + Visual + Platform |
| EDITOR-E1-TILE-PALETTE | InProgress | P0 | TileMap Inspector 固定容量 Tile Palette，消费 Tileset cooked metadata 并驱动真实 localId 画刷 | 2D-EDITOR / TilesetPayload | **自动证据（2026-08-24）：** Editor tests=`112/112 + 23/23`，2D/3D 短 smoke=`status=ok`；Palette selection 不产生 document revision，Tileset 缺失时 Paint 禁用。**待：** 手动多次选择/绘制确认真实 localId 与重复绘制稳定 | Unit + Product + Manual |
| EDITOR-E2-SETTINGS | InProgress | P0 | Editor 私有 UTF-8 settings carrier，保存布局/可见性/Bottom Panel/snap 与 Recent Projects | 无 | **自动证据（2026-08-24）：** Editor tests=`112/112 + 23/23`，2D/3D 短 smoke=`status=ok`；versioned schema、原子 sibling replace、损坏回默认已落地。**待：** 跨重启手动验证布局、面板、snap 与 settings 缺失/损坏回退 | Unit + Product + Manual |
| EDITOR-E3-RECENT | InProgress | P0 | Recent Projects 固定容量列表与 Start Center 打开/失效移除 | EDITOR-E2-SETTINGS / Project switch | **自动证据（2026-08-24）：** Editor tests=`112/112 + 23/23`，2D/3D 短 smoke=`status=ok`；New/Open/Temporary Save As 成功提交后最近优先去重，最多10条，Start Center 与 File/Open Recent 共用列表。**待：** 人工项目切换、重启恢复与失效路径移除 | Unit + Product + Manual |
| EDITOR-E6-PHYSICS-INSPECTOR | InProgress | P1 | Physics2D Body/Collision Shape Inspector 属性编辑 | 2D-EDITOR / World2D current schema | Physics Body 支持速度、角速度、阻尼、重力倍率与 Enabled；Collision Shape 支持 Box/Circle/Capsule、尺寸、local center/angle、密度、摩擦、恢复系数，以及 `Sensor` / `Sensor Events` / `Contact Events` / `Hit Events` 开关；多选 `Mixed`、单次 `replace()`、Undo/dirty、非法/非有限值与 kind mismatch fail-closed。**2026-08-28 定向证据：** `tina_editor_tests --gtest_filter=EditorNodePropertyOperationsTests.*` 13/13、`tina_editor_app_tests` 23/23，`tina_editor_desktop` 编译成功；四个 event toggle 已接入 Inspector、EditorCommand 与批量属性操作。**待：** Polygon/Chain、viewport outline/gizmo 联动与完整物理 authoring | Unit + Editor Product |
| UI-PAINT-002-A | Done | P2 | Retained 逐角圆角 authoring 首切片：`UIBoxPaint` 与 Canvas `SolidRect` 共享固定四角 logical radii，统一半径 helper 只生成 canonical 四角值；提交快照、border/inset/shadow、UI→Render 投影与 DisplayList checksum 全链路传播。产品场景为 Tina Studio Compact / UI Showcase 的 Segmented/header surface：相邻项只圆外角、共享边保持方角，并用 Canvas preview 展示四角差异 | Render `UIPixelCornerRadii`/DisplayList/bgfx shader 首切片 | 固定 `node/paint/canvas` 容量不增加专用 pool；单个 box/Canvas command 的 slot 与现有 uniform 半径相同。四角必须 finite、非负；setter、descriptor、Canvas assign 或 paint candidate 非法时 fail-closed，旧 retained/committed snapshot 保持不变；超过 half projected extent 由 bridge/backend 逐角确定性夹紧。dirty queue、paint snapshot 或 DisplayList 容量失败均在 backend 副作用前原子回滚。现有 `CornerRadius` Motion 继续以 uniform scalar 写四角相同值。范围明确不含 rounded/stencil subtree clip、backdrop/blur、新 shader/material、跨 GPU/DPI golden（由 UI-003 继续跟踪） | Unit + Integration + Smoke + Visual |
| RENDER-LINES-001 | InProgress | P2 | UI Line/Ellipse 渲染原语与 Editor 视口 grid/gizmo 接入 | 2D-EDITOR 视口链路稳定 | **代码已完成：** `UIBoxPaint` Rectangle/Ellipse/Line、Canvas SolidEllipse/SolidLine、committed SolidLine/SolidEllipse、DisplayList exact 四顶点/SolidEllipse 与 bgfx coverage shader 已贯通；Line 四个 logical 角点分别应用 framebuffer X/Y scale，支持 anisotropic 投影；Editor grid、gizmo 轴线/平面边界使用 Line，rotation ring 使用单个向内描边 Ellipse；旧 axis-aligned 多段近似及其预算代码已删除。**100%/150%/200% 已完成：** Editor 2D/3D 截图中的 grid、斜向 gizmo 与单 Ellipse rotation ring 连续无阶梯；UI-003 使用对应 baseline 分别独立复跑 `ok=true`。**待：** 跨 GPU 证据继续由 UI-003 跟踪。在该视觉证据完成前保持 InProgress | Unit + Visual |
| EDITOR-ANIM-EVENT-TIMELINE | Done | P2 | Timeline 的 per-frame notify event authoring：显示/新增/编辑/删除 `tag@offset` marker | 2D-ANIM-EVENTS；2D-EDITOR Timeline 既有链路 | Timeline 显示选中帧 events，并提供 Prev/Next、Add/Apply/Remove；十六进制或标识符 tag、小数或百分比 offset 统一解析后经 `setFrameEvents()` 发布，marker 按 offset 稳定排序。一次用户意图恰好一条 canonical revision，Undo/Redo/Cook Preview 完整往返；tag=0、非有限/越界 offset、每帧64上限与 stale selection fail-closed，Editor 不缓存旧 span。**2026-08-13 统一证据：** `tina_editor_tests` 114/114、`tina_editor_app_tests` 13/13；`TinaEditor.exe --frames=60 --frame-delay-ms=0 --workspace=2d --auto-demo` exit 0，`animationEventEdits=3`、`runtimePreviewValid=true`；3D 60帧 smoke exit 0 | Unit + Smoke |
| 2D-ANIM-EVENTS-PRODUCT | Done | P2 | product-2d 消费 `SpriteAnimator2D` 的 `crossedEvents`，与 chunk collider/Cooked Nav/Fx/Chain 一并升级产品 evidence | 2D-ANIM-EVENTS | Walk/HitWall recipe 分别携带 footstep/hit notify；fixed-step 消费 `crossedEvents` 并按 tag 计数，未知 tag 与 overflow 显式记账。产品 evidence 最终 schema 29 继承该 notify 字段（schema 28 切片曾单独引入）。**2026-08-13 统一证据：** `tina_sample_2d --frames=300 --frame-delay-ms=0 --ui-theme-demo --ui-tree-demo` exit 0，`animEventFootsteps=15`、`animEventHits=1`、overflow/unknown=`0/0`；product-2d tests 与两项视觉差分门禁均通过 | Smoke + Visual |
| TEXT-001 | InProgress | P2 | 多行 TextEdit、grapheme 边界与 Windows IME 候选窗 | UI-004/UI-005 既有 TextEdit/focus/capture 契约 | T1/T2/T3 代码与自动 gate 已完成：启用 `UITextEditMultilineConfig` 后支持 LF/soft-wrap、固定 byte/visual-line 容量、滚动、二维 hit-test 与 Up/Down/Home/End；scalar offset 的编辑/删除/导航均对齐无第三方依赖的 UAX #29 grapheme 子集；Runtime 从最后一次成功 paint commit 发布 `TextInputCaretRect`，Windows GLFW 完成 logical→DPI-scaled client pixel 转换与 IMM32 composition/candidate placement，poll-local 有界 FIFO 保留 Started/Updated/Ended、合并连续 progress、支持无 preedit direct commit，并在失焦前 drain 已发生事件；clear/focus/hidden/minimized/invalid geometry 会恢复默认策略；Headless 非空 placement 明确返回不支持，Linux 仅保证 committed text。**2026-08-17 自动证据：** `tina_ui_tests` 667/667、`tina_runtime_ui_tests` 130/130、`tina_ui_uia_tests` 12/12、UI showcase smoke exit 0；`tina_platform_glfw_tests` 39/39，`tina_tests` 定向 Headless 1/1；新增 soft-wrap 共享 scalar 边界 Left/Right 双 affinity 回归通过。既有 `PrimaryWindowTextInputPlacementCoordinator` 定向覆盖 logical/DPI/invalid/clear、grapheme、visual-row、IME focus-loss 与容量失败保持通过。**待：** BiDi/复杂 shaping、Linux 原生 XIM/Wayland、Windows 真机微软拼音候选窗跟随/提交/取消/失焦人工矩阵 | Unit + Integration + Platform + Smoke + Manual
| NAV-COOK-001 | Done | P3 | 独立 Cooked Navigation asset、editor bake 与 TileMap→Navigation 同步 | 2D-NAV、2D-EDITOR | `AssetKind::NavigationGrid2D` schema v1 使用32-byte header + row-major flags/cost tables，typed parser 可直接构建 immutable data；recipe、package validation 与 Project Browser 已接入。Editor Bake 从当前 resident TileMap 派生 bit-exact payload，fresh authoring overlay 通过项目 active pointer 持久发布，TileMap revision 变化显式 dirty；Source Import baseline 与 authoring overlay 分离。**2026-08-13 统一证据：** AssetFormat/Asset/Navigation2D tests=`96/298/16` 全绿；Editor 2D smoke `navigationCatalogPublishes=1`、`navigationBakeReady=true`；300帧产品 `navigationFromCookedAsset=true`、`navigationCookedBitExact=true`、static solid/rectangle/blocked=`11/0/11`、weighted/max-cost=`1/5` | Unit + Integration + Smoke |
| NAV-PHYS-SYNC-001 | Done | P3 | Physics2D 动态 body → Navigation2D blocker 同步 | 2D-NAV、2D-PHYSICS-EXPAND、NAV-COOK-001 | `Asset::PhysicsNavigationSync2D` 由 gameplay 显式注册 `PhysicsBodyId + body-local AABB`，读取 Physics transform 后将旋转保守 world AABB 栅格化为 dynamic blocker；固定容量、Create 后稳态零分配、disabled/out-of-grid 临时摘除、stale body 自动 retirement、外部 mutation/wrong owner/capacity fail-closed；Physics 不扫描 World，Navigation 不反向生成 collider。产品 crate 已注册并在每次 Physics step 后同步，静态 Cooked Navigation 不再重复烘焙 crate；schema 29 增加同步计数与 blocker 发布证据。**2026-08-13 统一证据：** `tina_physics2d_tests` 58/58（其中 bridge 9/9），300 帧 sample exit 0，sync/add/update/remove=`301/1/5/0`、registered/published=`1/1`、static solid/rectangle/blocked=`11/0/11`、path cells/cost=`5/40`；`RunProduct2dGate.ps1` 通过 | Unit + Integration + Smoke |
| FX-ASSET-001 | InProgress | P3 | ParticleSystem2D/Trail2D 配置 asset 化与 editor authoring（GPU simulation 留 Later） | 2D-FX、2D-EDITOR | `AssetKind::Fx2D` schema v1 固定184 bytes、一个 required Sprite dependency；39个 recipe authored values 覆盖完整 particle/trail 配置，reserved/range/dependency mismatch fail-closed。`Fx2DAuthoringDocument` 提供 canonical replace/Undo/Redo；Scene factory 构建固定容量 ParticleSystem + initial burst + Trail；EditorApp 尚无 Fx2D document tab/Inspector 消费面，GPU simulation 留 Later。**2026-08-13 统一证据：** AssetFormat/Asset/Scene tests=`96/298/156` 全绿；300帧产品从 Catalog FX asset 创建，`particleEmitted/active/extracted=10/10/10`、trail created/active/extracted=`3/3/3`，并发布稳定 `fxInitialFingerprint`。**证据更正（2026-08-28）：** 本行此前写作「`Fx2DAuthoringDocument` 有 API/单测」，**该单测不存在** —— `editor/tests/editor/CMakeLists.txt` 只有 SpriteAnimation/TileMap/World2D/World3D 四个 authoring document 测试，repo 内 `Fx2DAuthoring` 仅出现在 `editor/include/tina/editor/Fx2DAuthoringDocument.hpp`、`editor/src/Fx2DAuthoringDocument.cpp` 与 `editor/src/CMakeLists.txt` 三处。上述 `96/298/156` 覆盖的是 Fx2D **payload 与 scene factory**（`tests/asset_format/Fx2DPayloadTests.cpp`、`tests/scene/Fx2DFactoryTests.cpp`），不覆盖 authoring document 的 replace/Undo/Redo。故 170 行 authoring 代码（含 canonical replace 与 history）目前零测试覆盖。**已补齐（2026-08-28 同日）：** 新增 `editor/tests/editor/Fx2DAuthoringDocumentTests.cpp` 11 例，覆盖 initial revision 发布、非法 payload 在 Create/replace 两处均被 writer 拒绝且 replace 拒绝后状态不变、replace→undo→redo 走线、`payloadBytes()` 跟随 cursor 且与 `writeFx2DPayloadBytes()` 逐字节一致、同值 replace 不占 history slot、undo 后 replace 丢弃 redo 尾、entry/byte 两种容量各自 fail closed 且拒绝后保留当前值。**并修两处 Create 缺陷：** ①`historyEntryCapacity = 1` 此前被接受，但一个 slot 已被 initial revision 占满，于是每次 replace 都返回 `HistoryCapacityExceeded` —— 调用方会把它读成瞬态，实际是这份 document 永久只读；现按其余四个 authoring document 的下界要求 `>= 2`。②`reserve()` 在超出 `max_size()` 时抛 `length_error` 而非 `bad_alloc`，而 Create 只 catch 后者，故 `historyEntryCapacity` 取极大值时**异常直接穿出这个返回 `Result` 的函数**（违反 ADR 0004 的模块边界约定）；现以 `Fx2DAuthoringLimits`（2..256 entries、<=1 GiB，与 World2D/TileMap 同界）在 reserve 前拒绝，错误码同步由 `InvalidAuthoringOperation` 改为 `InvalidConfiguration`。两处均由新用例先复现再修。**证据：** `tina_editor_tests` 130/130（+11）、`tina_editor_app_tests` 23/23，`tina_editor_desktop` 编译无新警告；`TinaEditor --auto-demo --frames=70` 2D/3D 均 exit 0 `status=ok`（2D `automaticAuthoringStage=57`）；`tina_sample_2d` 300 帧 `status=ok`、`particleEmitted=10`/`trailSegmentsCreated=3`、指纹 `488c124f…` 未变。三个 history 不变量（同值去重、redo 尾丢弃、entry 容量）已用 revert 逐一验证会失败。**待：** EditorApp document tab/Inspector 消费面；GPU simulation 留 Later | Unit + Integration + Smoke |
| PHYS2D-CHAIN | Done | P3 | Physics2D Chain shape 与 Revolute/Prismatic motor/limit 扩展 | 2D-PHYSICS-EXPAND | Revolute/Prismatic spring/limit/motor 已在既有切片落地；本轮新增 static-only、non-sensor open/loop Chain，4..64 finite body-local 点、任意点间距>0.005m，Box2D chain/segment ID 保持 PRIVATE。公开一个 ShapeId 映射多个 backend segment，销毁/tombstone/级联 retirement 完整，AABB/ray query 按公开 ShapeId 去重。**2026-08-13 统一证据：** `tina_physics2d_tests` 49/49；300帧产品 exit 0 且 `physicsChainReady=true`，既有 Polygon/Revolute/Prismatic ready 证据保持为真 | Unit + Smoke |
| ASSET-SEC-002 | Done | P3 | glTF 之外全部 cooked typed payload 的资源炸弹/malformed 矩阵 | ASSET-SEC-001 | `TypedPayloadMalformedCorpusTests.*` 覆盖 Texture2D/Sprite/Tileset/TileMap root+chunk/SpriteAnimationClip/StaticMesh/SkinnedMesh/AnimationClip3D/Material/Prefab/EnvironmentMap/AudioClip/NavigationGrid2D/Fx2D/World2D snapshot 的适用截断、越界/self reference、超容量、非法 UTF-8、NaN/Inf 与长度破坏；Prefab/World2D 失败保留 caller storage，Prefab stable ID/rotation、Audio PCM、Sprite finite、Tileset flags/reserved 与 Catalog EnvironmentMap typed dispatch 已补强。2026-08-16 统一 gate：`tina_asset_format_tests` 124/124、corpus 17/17、`tina_asset_tests` 312/312，EnvironmentMap typed validation 通过；逐格式矩阵和 N/A 见 [testing.md](testing.md) | Unit |
| UI-MOTION-002 | Done | P3 | keyframe timeline 与 bounded layout animation | UI-MOTION-001、ADR 0023 | [ADR 0026](adr/0026-ui-keyframe-timeline-and-layout-animation.md) 已 Accepted；Context-owned typed keyframe timeline、timeline/track/keyframe/active-index 四类固定容量、generation/owner 校验、Runtime facade、retarget/cancel/destroy/reduced-motion、paint timeline 以及 `LayoutWidth`/`LayoutHeight`/`LayoutOffset` bounded layout whitelist 均已落地。layout、mixed paint 与同帧 direct transition 共用 candidate transaction，Layout/Hit/Paint/Semantics 全部成功后才发布；失败保留旧 presentation/active playback 并按绝对时间重试。**2026-08-16 统一证据：** `UIMotionTests.*` 28/28、Runtime `*Timeline*` facade 1/1、`tina_bench_tests` 10/10；`ui_motion_timeline_v1` 与 `ui_motion_layout_v1` 均以 `warmup=30,samples=120` 跑通 seed 0/1/2，retained definition high-water 固定为 timeline/track/keyframe=`256/1024/4096`，compact active timeline/track 为 `0/0`、`16/64`、`256/1024`。paint workload 的 Layout/Hit rebuild 为 0；layout workload 的 active seed 每个 measured iteration 恰好一次 Layout/Hit/Paint publication，`layout_commit_failures=0`，全部 seed 的 UI PMR allocation delta=0。墙钟结论保持 `provisional`，固定机 hard gate 仍由 `PERF-002` 跟踪 | ADR + Unit + Benchmark |
| UI-FLOW-001 | Done | P2 | 游戏页面 Activatable Screen、Layer Stack、Action Router 与输入设备提示 | 2D 产品 Base/Pause Screen Back/Confirm/Menu consumer；继续复用唯一 Element/Focus/Modal/Input 路径 | **Done：**固定容量 Layer/Screen 栈、Collapsed publication、focus/节点回收；topmost active Screen 的 fixed-inline `Back/Confirm/Menu` callback，总注册量受 `flowScreenCapacity` 限制；Dropdown-first Back、focused-control-first Confirm、TextEdit-first printable P、精确 Down/Up gameplay suppression；固定 16 槽 `UIFlowLocalUserId`（Primary=1），键鼠固定 Primary，完整 generation Gamepad assignment/默认 Primary、`UIFlowActionEvent::localUser`、per-user `KeyboardMouse/Gamepad` revision；重分配保留 physical Up latch，断连/reset 精确回落并清理 assignment/latch；2D Pause Primary 用户真实提示、Base Menu 打开 Pause、Pause Back/Confirm/Menu 恢复与 phase-safe Screen/State push/pop | Unit + Runtime + Product |
| UI-PERF-001 | Done | P1 | 建立 UI 演进的版本化 workload、counter 与 provisional baseline | `UIContextStatistics` + ADR 0018 schema v1/fingerprint + ADR 0023 Accepted | **Done：** `ui_static_commit_v1`、`ui_paint_dirty_v1`、`ui_route_v1`、`ui_virtual_collection_v1`、`ui_image_nineslice_v1`、`ui_component_build_v1`、`ui_style_state_v1`、`ui_motion_v1`（seed%3→0/64/1024 active tracks）。clean commit rebuild 为零，paint-only/Motion 不触发 layout/hit；绝对毫秒在 `PERF-002` 前只报 `provisional` | Benchmark + Unit |
| UI-STYLE-001 | Done | P1 | 开放强类型 StyleClass/token 与 node-local pseudo-state stylesheet | UI-COMPONENT-001 + UI-IMAGE-001 + `UI-PERF-001` 首个 milestone | **Done：** 强类型 StyleClass/ColorToken、node-local state、literal/token BoxFill + imageTint、固定容量 compiler/bucket、Runtime startup facade、运行期 reverse-dependency token 更新、Image tint paint-only API、属性 dirty metadata（`UIStylePropertyKind`）、showcase Integration（sheet + Dark/Light token 换肤）、Visual ROI `RunUiStyleVisualGate.ps1`（同机 Dark/Light header accent 差分）。首版不做 descendant/`nth-child`/runtime CSS；公开接口 ≠ 正式 ABI。下一主线 `UI-MOTION-001` | Unit + Integration + Visual + Benchmark |
| UI-MOTION-001 | Done | P1 | fixed-capacity paint-only transition 与 reduced-motion | UI-STATE-FEEDBACK + UI-STYLE-001 + `UI-PERF-001` 首个 milestone | **Done：** 公开 `UIMotion` + fixed-capacity tracks；Context API 全属性；Runtime `PrimaryWindowUITreeUpdater` Motion facade；Style 声明式 BackgroundColor transition 在候选阶段 persistent reservation、原子容量预检、pseudo-state 激活/retarget，reserved/active 分离统计；showcase 主题切换对 6 cards 发 BackgroundColor transition（JSON `motionBegins`）；`ui_motion_v1` | Unit + Integration + Visual + Benchmark |
| METRICS-001 | Blocked | P2 | Runtime Metrics 固定容量 counter registry 首切片：注册、更新与 snapshot 读取闭环 | [ADR 0027](adr/0027-runtime-metrics-registry.md) 仍为 Proposed，待 D1-D8 确认 | **阻塞点（2026-08-17）：** 核验确认 Metrics 零实现且既有 Accepted 文档未定义 owner、生命周期、容量、线程模型、counter 类型、注册方式、snapshot 语义与错误模型（ADR 0002 仅约束常驻成本与 Tracy 插桩成本分别 A/B）；已按「先 ADR 再实现」红线新增 Proposed [ADR 0027](adr/0027-runtime-metrics-registry.md)，待 maintainer 确认其待确认决策表（D1-D8）后开工，确认前不建立占位 API、不修改 Runtime。Accepted 后按确认后的 ADR 0027「决定」节交付 Core `MetricsRegistry` 注册、更新与 snapshot 读取闭环（owner、容量、名称、线程与错误模型的具体数值以确认后的 ADR 为准，不在本行重复），定向测试覆盖容量边界、重复注册、非法名称、无效与跨 registry id、并发 update、snapshot 行为与运行期零分配，并同步 Core/公开 API/性能文档与 header isolation。EngineHost 接线、`metrics_registry_v1` A/B workload、gauge/histogram 等扩展不在本切片 | Unit |
| UI-TEXT-ELLIPSIS | Done | P2 | 单行 Ellipsis 溢出策略与命名 typography ramp | ADR 0011/0022 既有文本与 committed content placement 契约；TEXT-001 的 UAX #29 grapheme 子集 | 公开 `UITextOverflow{Clip,Ellipsis}` 与 `UITextEllipsisUtf8`（U+2026），经 `setTextOverflow()/textOverflow()` 按节点 authoring，存于 `WidgetTextState` 而非 `UITextStyle`，因此换肤不会重置、intrinsic measure 保持未截断。截断在 paint 阶段按唯一 committed content placement 解析，`UIStylePropertyKind::TextOverflow` 只标 Paint；count 与 append 两趟共用同一 `resolveTextTruncation()` 计划，保证容量预留与实际发射一致。切点落在 UAX #29 grapheme cluster 边界（二分查找真实前缀宽度，非按均等 advance 整除）；聚焦 TextEdit、多行 box、含 `\n` 文本与非正 content 宽度一律退回 Clip；accessibility name 继续发布完整文本。`UITheme` 的 `buttonTextSize/bodyTextSize/titleTextSize` 三个散落字段收敛为 `UITypographyScale typography`（display/title/section/body/control/caption，逐级 finite 且为正校验），新增 `makeDisplay/Section/CaptionTextStyle` 与 `makeCompactTypographyScale()`；Editor 私有 `EditorTypographyMetrics` 删除并改用公共 compact ramp，默认主题与 Editor 的既有字号/advance/lineHeight 逐字段不变（视觉零回归）。Editor 长文本消费已接入：document tab 与状态栏文档路径直接设置节点 `Ellipsis`，Project Browser 通过 `UIVirtualGridViewStyle::itemTextOverflow` 同步到私有 materialized item，Semantics 仍保留完整名称。**2026-08-18 统一 Windows gate：** 常驻 `windows-msvc-vnext-bgfx-product-2d` tree 增量构建 UI/Runtime UI/UI-Render/FreeType/Showcase 与 Editor targets 全部 exit 0；`tina_ui_tests` 首轮 699/700，仅新增 multiline 用例缺少既有 `maximumVisualLines` fixture 配置，测试代码补齐后按 gate 规则定向重跑 1/1 通过；其余 `tina_runtime_ui_tests` 131/131、`tina_ui_render_integration_tests` 28/28、`tina_ui_freetype_tests` 3/3、`tina_editor_tests` 114/114、`tina_editor_app_tests` 13/13 全绿。Showcase 120 帧以及 Editor 2D/3D 各 60 帧 auto-demo 均 `status=ok`/exit 0，长文本消费所在的 Project Browser、document tabs、layout/status 生命周期无回归；DOC-002 errors=0 warnings=0 | Unit + Runtime + Product |

## Later

| ID | 状态 | 优先级 | 工作 | 验收条件 |
| --- | --- | --- | --- | --- |
| UI-002-LINUX | Deferred | P2 | Windows UI-002 稳定后实现 Linux AT-SPI adapter 与真实辅助技术验收 | AT-SPI backend 不暴露平台类型到 Game SDK；真实 AT client 可发现、读取并执行支持的 action；生命周期与 stale node 门禁通过 |
| PHYSICS-001 | Deferred | P2 | Jolt 3D adapter | 独立 Tina::Physics3D API、Jolt PRIVATE、生命周期/查询/性能门禁 |
| UI-PAINT-002 | Deferred | P2 | 圆角子树 clip 与 backdrop/blur 后续切片 | `UI-PAINT-002-A` 完成 Retained 逐角 authoring 后，另行冻结 rounded/stencil clip 与 backdrop/blur 的 backend-neutral 描述、专用容量预检、失败原子 publication 和跨 DPI/backend 视觉门禁；不把 A 的 box chrome 四角半径写成后代 clip，也不重复已完成的统一 RoundedRect |
| UI-BEHAVIOR-SPI-001 | Deferred | P3 | 标准 Behavior + routed listener 不足时的 startup-only 自定义 Behavior SPI | 只有真实插件场景证明 `UI-COMPONENT-001` 无法表达后才进入；固定 state arena、最大 dirty effects、callback 不取得 Context/Renderer/allocator |
| 2D-LIGHT | Deferred | P2 | 2D lighting 与 shadow | **已完成 N1/N2/N3/N4/N5：** backend-neutral、frame-scoped 0..8 committed PointLight2D + ambient + 0..32 ShadowOccluder2D segment，World 稳定提取、Null/bgfx submit preflight、径向衰减、硬/finite-source 软阴影、resolved/pixel-snapped Camera2D light culling，以及 Sprite2D 可选 normal map；透明排序保持。当前 product-2d schema 29 继承 schema 19 的 normal-map 与 authored/committed/culled=`3/2/1`、soft=2、soft/hard 差分证据。**待：** 跨 GPU lighting exact golden |

## Done

| ID | 完成项 | 证据入口 |
| --- | --- | --- |
| CORE-APP-PATHS-001 | `Core::applicationDirectory()` / `applicationFilePath(relativePath)`（`core/io/ApplicationPaths.hpp`）解析正在运行的 exe 所在目录及相对它的路径，Windows 读已加载模块路径、其它平台读 `/proc/self/exe`。**填补的是真实空白：** 本切片前 `GetModuleFileName`/`argv[0]`/`current_path`/`/proc/self/exe` 在 `src` 与 `include` 零命中，装出去的游戏只能依赖编译期绝对路径（只在编译它的机器上对）或进程工作目录（在玩家启动它的地方），两者都找不到随包资产。与 `UserPaths.hpp` 刻意分开：那边答「可以往哪写」按 application name 索引，这边答「只读数据在哪」除 exe 位置外无输入。两个函数都只拼路径不碰文件系统，所以只读/空安装目录里 resolve 不会失败，资产缺失由随后失败的读取报告；逐字拼接，凡可能解析到 exe 目录之外的值（绝对、含 `\`、含 `:`、`.`、`..`、任何空组件含 `//` 与尾部 `/`）一律 `InvalidArgument`；根目录保留分隔符，因为 `""` 是相对路径而 `C:` 指 C 盘当前目录而非根。**未接入消费者**：样例的 `TINA_SAMPLE_2D_RECIPE_PATH` 等编译期源码树绝对路径仍未改用它。**更正（2026-09-01）：** 上一句**已过期**（原文保留）。`TINA_SAMPLE_2D_RECIPE_PATH` 在全仓库已**零命中**（`samples/` 与 CMake 均无残留），且已有三个真实消费者：`src/desktop/UiFontFile.cpp:78`（桌面 UI 字体改为在 exe 旁解析，`395c3c2c`）、`samples/2d_tilemap_bgfx/main.cpp:2009`（`applicationFilePath("catalog/sample_2d.recipe")`）、`samples/3d_product/main.cpp:629`（`applicationFilePath("assets/complete_pbr/complete_pbr.gltf")`）；路径整体改为相对可执行文件见 `b09d7268` | [Core](core.md) · `ApplicationPathsTests`（8 项）· product-2d tree `tina_tests` 479/479、零警告 |
| RUNTIME-HOOK-DEFAULTS-001 | `IGameState` 的 `onEnter`/`onExit`/`initialPolicy` 与 `IGameApplication::onShutdown` 由纯虚改为带默认实现，`createInitialState` 保持纯虚。修正的是一处倒置：原先三个各只有一种合理写法的钩子（成功、空、不阻断）是必须实现的，而真正决定游戏行为的 `fixedUpdate`/`updateFrame`/`extractRenderScene`/`updateUI` 反而都有默认值。`tests/sdk_consumer_desktop/main.cpp` 是这一约定的编译期断言——只重写 `updateFrame` 与 `createInitialState`，任何钩子退回纯虚它就编不过 | [Runtime](runtime.md) · [公开 API](public-api.md) · DesktopBootstrap consumer gate（对安装头编译，输出 `status=ok`）· product-2d `tina_tests` 479/479 |
| UI-RESPONSIVE-LAYOUT | 普通 Label 按最终 content width 自动换行与 max-lines clamp、Flex Row/Column 子项 Wrap/alignContent、aspect ratio、min/max-content size/basis/constraint、最多4条直接父 content-width bounded responsive rules（Flex/Grid/Visibility/Gap/Padding/MinMax override）及最多三轮 Measure/Arrange 稳定化已落地；Desktop Shell 已删除 resize callback 的手工 width tier，Inspector/Timeline pane 与 splitter 直接使用规则；Runtime facade 暴露 text wrap/clamp intent，不保留隐式兼容降级 | [UI](ui.md) · [公开 API](public-api.md) · `UILayoutTests` / `UIFlexLayoutTests` / `UIIntrinsicSizingTests` / `UITextTests` / `PrimaryWindowUICapabilityTests` |
| UI-SNACKBAR-001 | 公共 `UISnackbarHost`/`UISnackbarMessage` 固定 4 条 inline queue、显式 monotonic Entering/Visible/Exiting 状态、可选 action token、精确 component build budget、Floating surface recipe 与 Runtime facade 已落地；消息 Label 发布 Polite live-region，Windows UIA 提供 LiveSetting property 和仅在 live 文本/setting 变化时触发的 LiveRegionChanged event。TinaEditor 已消费 tone、Undo、120/100 ms opacity+offset Motion，且不改变 Focus | [UI](ui.md) · [公开 API](public-api.md) · [Modern Desktop](ui-modern-desktop.md) · `UISnackbarTests` / `WindowsUiaAccessibilityTests` / Editor product gate |
| RENDER-002-TRANSPARENT | Material v2/recipe、Scene/Prefab、RenderScene、Null/bgfx 全链路使用显式 `Opaque`/`Blend`；Blend static/skinned 统一 back-to-front 全序，Transparent3D 使用 straight-alpha、depth less、不写 depth且不投 shadow，pass 固定为 CSM×4→Spot→Point×6→Opaque3D→Transparent3D→Sprite2D→UI。2026-08-15 schema 16 集中 Windows gate 的11个 GoogleTest executable 全部 exit 0；300帧主 sample 固定3 Mesh/4 Material/3 Texture、双 static transparent witness、透明 draw count与非零稳定排序 checksum；30帧 transparency on/off 中央 RGB fingerprint 不同且 L1 达到门槛，IBL、skin-animation、point-shadow A/B 同轮通过，临时 raw capture 已回收 | [3D](game-3d.md) · [Rendering](rendering.md) · [资源](resources.md) · [测试](testing.md) · `RunProduct3dGate.ps1` |
| 3D-SKIN-001 | A1-A4 全部闭环：SkinnedMesh v1/AnimationClip3D v1 wire/cook/typed 与 joint remap、互斥 `SkinnedMeshRenderer3D`、无分配 `Animator3D` CPU pose、packet-local palette、Null/bgfx preflight 与 GPU vertex skinning；schema 15 固定 total/static/skinned mesh=`3/2/1`、joints=`2`、skinned Prefab instances=`3`。2026-08-14 集中 gate 的11个 GoogleTest executable 全部 exit 0，300帧 pose changes=`300`、packet fingerprint changes=`299`；30帧 skin on/off scene fingerprint 各自稳定且跨模式不同，RGB L1 达到门槛，raw capture 已回收 | [3D](game-3d.md) · [资源](resources.md) · [测试](testing.md) · `RunProduct3dGate.ps1` |
| 2D-ANIM-EVENTS | SpriteAnimationClip 唯一现行 schema v2：32-byte header、12-byte frame（sprite dependency index、正有限 duration、event 区间）与 8-byte notify event（非零 u32 tag + u16 定点**帧内** normalized offset，改帧长事件随帧移动；上限每帧64、单 clip 16384）；v1 以 `UnsupportedSchema` 拒绝，无双读迁移。recipe 帧后缀 `#<tag>@<offset>`（hex 或 FNV-1a 标识符、小数或百分比），同帧按 offset 升序稳定存储。`SpriteAnimator2D::update()` 用 `Create()`/`setClip()` 预分配的固定容量缓冲零分配上报 `crossedEvents`：半开 `[from,to)` 判定使边界事件在连续 update 间恰好触发一次，Once 完成帧闭合终点，跨整周期 delta 只报一圈，PingPong 反向段镜像判定并报 `forward=false`，超预算按时间序截断并置 `crossedEventOverflow`。editor authoring Desc/revision 各自拥有 per-frame event 存储并在 copy/insert/set/erase/move 后重绑借用 span，消除跨对象拷贝的悬垂 span | [Scene](scene-ecs.md) · [资源](resources.md) · [公开 API](public-api.md) · `tina_asset_format_tests` 96/96 · `tina_scene_tests` 156/156 · `tina_asset_tests` 298/298 · `tina_editor_tests` 114/114 · product evidence schema 29 |
| 2D-SERIALIZATION | `AssetFormat` 提供唯一现行 schema-v4 World2D snapshot（32-byte header、448-byte named entity、含 SpriteAnimation2D 绑定、4096 entity/4 MiB gameplay 上限）与 canonical parser；持久化只含 stable entity/parent ID、节点名称、LocalTransform、五类2D组件（含 SpriteAnimation 绑定）、稳定 `AssetId` 和 game-owned schema/version/blob，不保存 Runtime generation/Handle/Lease/Render identity。Scene capture 按 depth→stable ID 确定排序并拒绝3D有损写入；restore 在 mutation 前完成容量/资源/组件预检，publication failure 以 subtree rollback 保留既有 World；旧 schema 直接拒绝，不保留兼容分支 | [World2D 序列化](world2d-serialization.md) · [Scene](scene-ecs.md) · `World2DSnapshotTests.*` 5/5 · `World2DSnapshotSceneTests.*` 5/5 · AssetFormat/Scene header isolation |
| 2D-NAV | 独立 `Tina::Navigation2D` 提供唯一当前 immutable weighted row-major grid、`[1,16]` traversal cost、固定容量 generation dynamic blocker、重叠引用计数/revision，以及确定性四向/对角同步/分步 A*、严格/允许切角策略与整数 path cost；`Asset::buildTileMapNavigation2DData()` 从 resident solid tile layer、exact material-cost rule 与 property-tagged visible Rectangle 原子派生数据，non-resident chunk fail closed；当前 product-2d schema 29 的静态 Cooked grid 固化 solid/rectangle/blocked=`11/0/11`、weighted/max-cost=`1/5`，Physics bridge 运行时发布 crate blocker并保留基础/动态/严格/切角 path cells/cost=`5/40`、独立 weighted path=`7/60`、单步 expanded=`1`、revision/mutation=`10/2` 与取消终态 | [2D 导航](navigation2d.md) · [2D](game-2d.md) · `tina_navigation2d_tests` · `tina_physics2d_tests` · `tina_sample_2d` 300-frame smoke |
| RENDER-001 | Opaque3D Cook-Torrance GGX/IBL、唯一 P3N3T4UV2 tangent、三类有界 Scene light 与 directional/spot/point shadow 已闭环；最后的 shadow extent 切片新增 startup-only `ShadowMapExtentConfig`，以 `[128,4096]` 内2次幂配置 directional cascade tile、spot map 与 point face，默认保持 `1024/1024/512`。`EngineConfig` 在任何 factory 前校验并向 independent/window-surface `RenderDeviceCreateParams` 传播；Null/bgfx 直接 factory 同样 fail closed；bgfx 的 D16 资源、view rect 与3×3 PCF texel size 只使用当前配置，不保留固定尺寸兼容分支 | [Rendering](rendering.md) · [公开 API](public-api.md) · `ShadowMapExtentConfigTest` / `EngineConfigTest` / `EngineHostCreationTest` / `WindowSurfaceRuntimeTest` / Null/bgfx factory + shadow resource contract tests |
| TRACE-002 | `TINA_TRACE_BACKEND=none\|tracy` 唯一选择与 `profile-tracy` feature 门禁已落地；Tracy 0.13.1 zone 只经 Tina-owned 64-byte opaque RAII bridge 进入 PRIVATE adapter，None 继续在预处理阶段完全丢弃参数。nested zone、Runtime phase consumer、relocated GameSDK consumer 与 195 个安装公共头的第三方 token 扫描通过；session/capture 控制面仍明确后置 | [ADR 0002](adr/0002-tracy-and-benchmark.md) · [Core](core.md) · [构建](building.md) · [测试](testing.md) · `TraceCompileTest` / `GameStatePolicyDispatchTest` / `RunSdkConsumerGate.ps1` |
| ASSET-002 | Catalog watcher/revision、immutable change planner、fresh-stage validation、current-only import state/provenance、mixed dirty-unit executor 与 all-clean reuse 已落地；pipeline 返回 exact validated owning snapshot，Editor 通过 `reloadPreparedCatalog()` 避免主线程二次 package validation；reload 以双驻留 CPU generation staging + 显式 Sprite/Mesh registry participant transaction 原子发布 Catalog/index/active GPU binding，prepare/active-frame failure 保持旧状态，commit 后旧 Lease/GPU owner 可重试 retirement | [资源](resources.md) · [公开 API](public-api.md) · [测试](testing.md) · `AssetSystemCatalogReloadTests` / Catalog watcher/change planner/source import tests |
| TRACE-001 | backend-neutral `TINA_TRACE_ZONE(nameLiteral)` frontend 与 None backend 已落地；None 宏不引用参数，因此不进入 C++ 语义、不求值、不构造对象、不调用、不分配且无全局状态。`EngineHost` 在每个实际 `FrameUpdate` State dispatch 放置首个 consumer；非法 backend configure 明确失败。可选 Profile backend 已由 TRACE-002 闭环，Metrics 仍未接入 | [ADR 0002](adr/0002-tracy-and-benchmark.md) · [Core](core.md) · [公开 API](public-api.md) · `TraceCompileTest` / State stack + Runtime 定向测试 · Windows relocated GameSDK consumer / installed public-header scan |
| UI-COMPONENT-001 | Runtime GameStateEnter/UIUpdate phase-scoped move-only transaction 在 component root 创建前统一预留 node、UTF-8 byte、Canvas command 与 Activate/Toggle/Range/TextInput/Scroll/Select slot；任一池失败保持 live tree 零变化，失败/reset/析构/phase escape/subtree destroy 只释放一次，commit 后无 wrapper。各池发布 requested/reserved/published/failure/outstanding counter；`ui_component_build_v1` 固定 256×4 节点并证明全池 counter、稳定 tree checksum、PMR allocation delta=0 与 clean rebuild=0 | [UI](ui.md) · [UI 框架](ui-framework.md) · [性能与内存](performance-memory.md) · `UIElementTests` / `PrimaryWindowUICapabilityTests` / storage tests · `tina_bench_tests` / `ui_component_build_v1` |
| UI-DIALOG-001 | `UIDialogStateStorage` generation-safe 保存 open intent；Dialog build 后默认 closed，Context/root-scoped Updater/phase-scoped Runtime facade 提供幂等 open/dismiss/query。effective Collapsed 与 Modal/Hit/Paint/Semantics/Focus 只在成功 commit 原子发布；单 Window 单 open Dialog、Menu/Tooltip 协调、dirty capacity 回滚、destroy/root/generation 清理与 authored visibility 单轨约束已闭环。Editor Create Node/dirty-close/Scene Delete/About、Showcase 与 Desktop Shell 已迁移 | [UI](ui.md) · [公开 API](public-api.md) · [UI 框架](ui-framework.md) · `UIDialogTests` / `UIComponentProfileTests` / `PrimaryWindowUICapabilityTests` |
| UI-IMAGE-001 | Image content、强类型 `UIIconContent` authoring profile、Canvas Image 与 Stretch-only NineSlice 已贯通 backend-neutral authoring、committed paint、root-scoped `(scope, AssetId)` resolve/cache、packet-local FrameResourceRef/FramePin、RGBA ImageQuad 和 bgfx；UIIcon 固定 Contain/居中/Ignore/Exclude，并继续复用 Image storage/资源链/atlas/GPU pipeline；Showcase 的图标按钮、Inventory thumbnail、NineSlice panel 已通过 Dark/Light、资源失效和 6-case size matrix。`ui_image_nineslice_v1` 每 build 固定 `Q=5096/U=64/B=1000`、64 resolver hit、5032 cache dedupe、64 pin acquire/release，错误计数与 PMR allocation delta 为 0，high-water/checksum 稳定 | [UI 框架](ui-framework.md) · [性能与内存](performance-memory.md) · `UIImageTests` / `UINineSliceTests` / UI-Render + bgfx tests · UI Showcase lifecycle/size matrix · `tina_bench` |
| UI-TOOLTIP-001 | 独立 `Tooltip` built-in contract 与 `makeTooltipElement()`；同 root 显式 Anchor 双向关系，拒绝自身/循环/跨 root/stale/incompatible；PointerHover/KeyboardFocus/Manual、可注入 monotonic initial/reshow/dismiss delay 与超大有限 delay 饱和；上一份成功 Anchor geometry 的四向/Auto/flip/viewport clamp；单 Window 独占；Ignore hit、无 focus/capture/barrier；input/visibility/enable/destroy/modal dismissal；committed metrics、失败 commit 回滚 clock-driven state；Anchor description/HelpText fallback；Context/Updater/Runtime phase facade 与 header isolation 全部落地 | [UI](ui.md) · [公开 API](public-api.md) · [UI 框架](ui-framework.md) · `UITooltipTests` / `PrimaryWindowUICapabilityTests` |
| UI-VISUAL-COMPONENTS-001 | 强类型 `UISurface`/`UIDivider`/`UIBadge` authoring profiles 与 `UISwitch`；Surface/Divider/Badge 复用普通 Element、StyleRole、Theme、Box/Text paint，Switch 复用 Checkbox Toggle/input/action/capacity 并发布 Switch semantics/UIA TogglePattern；Standard/Compact track/thumb、Dark/Light token、Ignore/Exclude/Label 默认语义、非法 profile/thickness 与 header isolation 已闭环，无第二套 state/runtime/render pipeline | [UI](ui.md) · [公开 API](public-api.md) · [UI 框架](ui-framework.md) · `UIVisualComponentsTests` / `UISwitchTests` / `UIThemeTests` / `WindowsUiaAccessibilityTests` |
| UI-MENU-001 | 独立 `Menu`/`MenuItem` contracts 与 recipes；同 root 显式 Anchor、单 Window active Menu、与 Popup 共用 transient overlay coordinator 但不复用 Popup state；Command/Check/Radio/Separator、radio group、四向 Auto/flip/clamp、Pointer blank/outside barrier、焦点恢复与 input/visibility/Modal dismissal；Keyboard/Gamepad/accessibility 共享激活、Menu/MenuItem UIA Invoke/Toggle、committed metrics/失败原子性；`UIMenuStateStorage`/`UIMenuLayout`/`UIMenuInput` 固定容量模块、Runtime phase facade 与 header isolation 已闭环 | [UI](ui.md) · [公开 API](public-api.md) · [UI 框架](ui-framework.md) · `UIMenuTests` / `PrimaryWindowUICapabilityTests` / `UIInputRouteProducerTextAndPopupTests` / `WindowsUiaAccessibilityTests` |
| UI-SPLITVIEW-001 | 独立 `SplitView`/`Splitter` contract、官方 recipes、同 root 三 direct child parts 关系与 generation-safe 清理；Horizontal/Vertical orientation、fraction/minimum clamp、committed metrics、Splitter pointer drag/grab offset、RangeInput keyboard/UIA SetRangeValue、Focus/Semantics；`UISplitViewStateStorage`/`UISplitViewLayout`/`UISplitViewInput` 固定容量模块接入 UIContext；Runtime phase facade、capacity rollback 与 header isolation 已闭环 | [UI](ui.md) · [公开 API](public-api.md) · [UI 框架](ui-framework.md) · `UISplitViewTests` / `PrimaryWindowUICapabilityTests` |
| UI-TABVIEW-001 | 独立 `TabView`/`Tab` contract 与官方 recipes；完整同 root direct-child Tab/Panel pairs、追加 child 解除旧关系、Top/Bottom/Left/Right layout、Automatic/Manual activation、Pointer/Keyboard/Gamepad/accessibility 共享选择路径；TabList/Tab/TabPanel semantics、专属 `UITabPaint` Theme chrome、committed metrics、失败/容量/generation/root 原子性、Runtime phase facade 与 header isolation 已闭环 | [UI](ui.md) · [公开 API](public-api.md) · [UI 框架](ui-framework.md) · `UITabViewTests` / `PrimaryWindowUICapabilityTests` / `UIInputRouteProducerFocusNavigationTests` |
| UI-RANGE-INPUT-KEYBOARD | `UIRangeInputCommand::{Decrease,Increase}` 与 `routeRangeInputCommand()` 独立于空间焦点导航；Runtime 将 Keyboard Arrow/Gamepad D-pad 在复合方向控件之后、通用空间焦点之前路由给 focused RangeInput。Slider 复用 min/max/step/clamp、callback 与 UIA SetRangeValue 的量化 mutation；成功 Down 使用 fixed-capacity exact-control latch，匹配 Up 在焦点或 enabled 状态变化后仍成对消费；read-only/边界值不修改、不 latch、不误触发空间焦点，Gameplay transition 保持可见 | [公开 API](public-api.md) · [UI](ui.md) · `UIRangeInputTests` / `UIInputRouteProducerRangeInputTests` / UI + Runtime UI tests |
| UI-STATE-FEEDBACK | Slider Focusable/Focus semantics 与 runtime trait 对齐；Checkbox/RadioButton、List/Tree、TextEdit 复用既有 hover/arm/focus/selection 状态并以确定优先级解析 paint；disabled、release/cancel、Hidden/Collapsed、destroy 与 Modal change 不留 stale feedback；Windows MSVC/bgfx/FreeType 产品门禁以22项差分检查证明 Dark/Light 的 normal、hover、focus、pressed/drag、selected、disabled 均可辨识。即时反馈仍不是 Motion | [UI](ui.md) · [Windows 视觉证据](evidence/ui-state-feedback-evidence-windows.md) · `UIThemeTests` / `UIPaintPrimitivesTests` / `UIFocusModalTests` / Runtime UI tests |
| ASSET-SEC-001 | glTF/GLB 主文件与外部 buffer/image 改为单次打开的有界快照；strict UTF-8/percent-decode、最终路径 authoring-root containment、文件替换检测、checked count/range/parser/decode/output budgets 全部 fail closed；root 内 symlink/junction 可用，逃逸链接拒绝；无 `cgltf_parse_file`/`cgltf_load_buffers` 旧 loader 回退 | [资源](resources.md) · [公开 API](public-api.md) · `GltfCookTests` Windows/Linux 24/24 · Windows `tina_asset_tests` 218/218 |
| UI-ELEMENT-AUTHORING | ADR 0022 已完成：descriptor/recipe `createElement()`、Flex/Overlay 与统一 content placement、显式 Semantics/Merge/Exclude、Theme style role 与属性级 override reset、固定容量多节点 build transaction、bounded backend-neutral Canvas `SolidRect` 首切片全部落地；公开 `UIWidgetKind` 与 create-by-kind surface 已删除，Showcase 普通页面迁移到 Flow/Flex | [ADR 0022](adr/0022-ui-element-authoring-and-layout.md) · [UI](ui.md) · `UIElementTests` / UI + Runtime UI + UI-Render + FreeType + UIA + bgfx tests · Showcase Dark/Light smoke/capture · UI-003 visual gate |
| ASSET-HANDLE-SCENE | A1-A6 与 N16.1-N16.4 完成：Scene/Prefab/FX/TileMap 保存 weak Handle，全部 Sprite2D/Mesh3D Render item 使用 packet-local `FrameResourceRef`；Sprite/Mesh3D registry 分别唯一拥有 Lease/GPU/binding，并以 active frame pin 与 retirement ledger 关闭资源 | [资源](resources.md) · [Rendering](rendering.md) · [Scene](scene-ecs.md) · 2D/3D product gates |
| UI-004 | committed `UIFocusScopeMode::Contain`、显式 focus、topmost Modal barrier、嵌套/跨 root focus 恢复；listener 与 Button/Slider/TextEdit 持久 Primary Pointer Capture；Up、cancel、destroy、disable、Hidden/Collapsed 与 Modal scope change 释放；失效沿原 committed ancestry 合成 synthetic-only `PointerCancel`；paint/semantics 失败提交回滚 focus/Modal/capture；UI 282/282、Runtime UI 85/85 | [UI](ui.md) · [ADR 0011](adr/0011-retained-ui.md) · `UIFocusModalTests` / `UIPointerCaptureTests` / `PrimaryWindowUICapabilityTests` |
| UI-FOCUS-SPATIAL | `UIFocusNavigationDirection` + committed geometry beam/axis scoring；Keyboard Arrow/Gamepad D-pad 统一路由；Modal/Contain scope、disabled/复合 item 过滤；成功移动的 Down/Up 由固定容量 latch 成对消费，无焦点/无候选不抢 Gameplay；现有 showcase 多列控件直接演示 Dark/Light focus chrome | [UI](ui.md) · `UIFocusNavigationTests` · `UIInputRouteProducerFocusNavigationTests` |
| UI-THEME-C1-ROUNDED | `UIBoxPaint`/Canvas `SolidRect` 统一 logical corner radius；圆角 border 使用确定性外 ring + inset fill；Integration 投影/夹紧像素半径并纳入 DisplayList checksum；bgfx D3D11/OpenGL/Vulkan shader 用 batchable SDF coverage；Dark/Light 默认 panel/button 圆角 | [UI](ui.md) · `UIPaintPrimitivesTests` / `UIElementTests` / `UIDisplayListTests` / UI-Render / bgfx geometry tests · Showcase Dark/Light smoke |
| RUNTIME-SHUTDOWN-DEADLINE | `shutdownAndJoinFor` 有界 stop/join；invalid 不触发 stop，timeout 保留 stopping TaskSystem/Worker/owner 并可 retry；Host 仅为 TaskSystem worker-exit/join 使用配置 deadline，超时先写 Diagnostics 再 terminate，绝不继续析构 owner；不 detach/强杀 | [Task](task-system.md) · [Runtime](runtime.md) · Disabled/Bounded Task shutdown tests · EngineHost Task-shutdown deadline/death tests |
| DONE-001 | Legacy `Tina.exe`、旧横版 2D 与旧 UI 产品图删除 | [M12 退役说明](evidence/m12-legacy-ui-retirement.md) |
| DONE-002 | `EngineHost`、Platform/Input、WindowSurface、Desktop/bgfx 垂直切片 | [架构](architecture.md) · [测试](testing.md) |
| DONE-003 | Scene 2D/3D extraction 与 Catalog/Cooked/Handle/Lease/Task/Upload 首轮 | [Scene](scene-ecs.md) · [资源](resources.md) |
| DONE-004 | 2D product sample 与 glTF/Prefab 3D product sample | [2D](game-2d.md) · [3D](game-3d.md) |
| DONE-005 | Retained UI 文本/Glyph、Checkbox/Slider/TextEdit、ProgressBar/RadioButton 库级实现 | [UI](ui.md) |
| DONE-006 | glTF multi-mesh Cooker 与 distinct AssetId/Prefab dependency 测试 | [3D](game-3d.md) |
| 2D-INPUT-ADV | Runtime 唯一 unified binding 覆盖 digital/analog value、deadzone/scale、SumClamped/StrongestMagnitude、多 Gamepad、UI consume/claim suppression 与顶层 State transactional rebind（next-frame apply、Reject/Swap、cancel、generation disconnect/reset） | [Platform/Input](platform-input.md) · [Runtime](runtime.md) · ActionMapper/InputAction/Rebind unit + Runtime/UI integration + `tina_sample_2d` 300-frame smoke 是本轮最终验证目标，执行结果以最终验证记录为准 |
| 2D-TILEMAP-LAYERS | 有序 tile/object layer、map-wide 非零唯一稳定 layer/object ID、visibility、UTF-8 name/properties、point/rectangle；runtime render/collision 显式选择 layer；当前由 TileMap v3 stream root 延续该契约，旧 v1/v2 均不双读 | [2D](game-2d.md) · [资源](resources.md) · [物理](physics.md) · TileMap/CatalogCook/TileChunk/TileMapPhysics tests |
| 2D-TILEMAP-STREAM | TileMap v3 stream root + 独立 `TileMapChunk` v1 Cooked asset；chunk 为 deferred dependency；`TileMapStream` 按 Camera/layer 有界 request、cancel、unload、lease 与 transactional capacity；resident generation 贯通 dirty cache；sample 每帧 demand→pump→commit 并验证 visual/collision 两 chunk 驻留 | [2D](game-2d.md) · [资源](resources.md) · [公开 API](public-api.md) · TileMapChunk/TileMapStream/AssetSystem/dirty-cache tests |
| 2D-TILEMAP-LRU | retain window 仅作 optional cache；按最近一次成功 demand update 时位于 load window 的 recency 自动淘汰，读取 API 不 touch；desired 强需求单独超 resident capacity 仍 transactional failure | [2D](game-2d.md) · [资源](resources.md) · TileMapStream retain-overflow/LRU tests |
| 2D-TILEMAP-PRIORITY-GEN | 同 chunk demand 聚合最高 priority，新请求按 `priority desc -> layerId -> chunkY -> chunkX` 消费 budget 且不抢占已 dispatch IO；Editor 从 visible object layer 生成 owning bounded spawn plan，经 game-owned encoder 单次发布 World2D gameplay revision；unknown/duplicate/capacity/encoder failure 保留 document/history；2D sample 按 role/kind 消费而不依赖固定 object ID | [2D](game-2d.md) · [Editor 2D/3D](editor-2d.md) · [资源](resources.md) · `TileMapStreamTests` / `TileMapGameplaySpawnPlanTests` / Editor + 2D smoke |
| 2D-PHYSICS-EXPAND | Body/Shape/Joint 独立 generation handle；Box/Circle/Capsule/ConvexPolygon、多 shape/body、sensor enter/exit、Distance/Revolute/Prismatic joint 与级联 retirement；TileMap bridge/sample 全部迁移，Box2D 保持 PRIVATE；模块测试与产品 300 帧 sensor/joint 证据通过 | [物理](physics.md) · [2D](game-2d.md) · PhysicsWorld2D/TileMapPhysics/CharacterControllerPhysics tests |
| 2D-PHYSICS-CHUNK-COLLIDER | TileMap→Physics 改为 per-resident-chunk static collider：确定性 greedy rectangle 合并降低 body/shape 数；按 residency generation + content revision 增量 add/rebuild/remove，unchanged chunk 复用原 body；变化 chunk 整体 staged 成功才退休旧 body，失败保留上一次发布的 collider 且不推进 stats；`Create()` 固定容量、稳态零分配；逐 cell 同步 API 已删除且无兼容别名。product-2d `physicsStaticBodies=1`（11 solid cell → 2 box shape），字段由 schema 29 继承 | [物理](physics.md) · [2D](game-2d.md) · [公开 API](public-api.md) · `TileMapPhysicsSync2DTest` · `PhysicsGridBodiesTest` · `CharacterControllerPhysicsCoexistenceTest` |
| 2D-AUDIO-ADV | voice gain/pitch/pan/fade 与 transient one-shot retirement；PCM stream 在 Create 固定预分配，owner thread 整块原子 submit，Task worker 必须 marshal；非 EOF underrun 仅静音计数，EOF 排空后单次 Stopped，cancel 单次 Cancelled；terminal completion 在 ring 满/active realtime reader 时延迟而不丢，单 realtime consumer、absorbing terminal 与有界 shutdown 已覆盖；产品 sample 贯通 Catalog PCM stream | [Audio](audio.md) · [公开 API](public-api.md) · AudioEngine PCM stream tests · MiniaudioDevice null-backend stream test · product-2d 300帧 smoke |
| 2D-FX | `ParticleSystem2D` / `Trail2D` 作为独立 Scene systems：Create 唯一持久 PMR 分配、固定容量与稳定 key 单调不复用；粒子固定 seed（含0）确定性、burst/update 失败事务性；trail anchor/segment/break、独立 segment lifetime 与按 age 宽度插值；产品门禁 schema 9 提供结构化与非空像素证据 | [2D](game-2d.md) · [Scene](scene-ecs.md) · [测试](testing.md) · `tina_scene_tests` · `RunProduct2dGate.ps1` |
| RENDER-001-NLIGHT | 删除 Opaque3D key/fill 双 setter；`Mesh3DLightingDesc` 单次提交0..4 directional lights + ambient；Null/bgfx/shader 同一上限与验证；sample_3d 提交3灯并输出 count | [Rendering](rendering.md) · [3D](game-3d.md) · GpuTextureUploadTests |
| RENDER-FENCE | present-return CPU ticket 与 GPU resource retirement 分离；bgfx 用末尾 view 的 blit + `readTexture()` ready frame 作为可证明 completion marker，Texture/Mesh/EnvironmentMap generation 立即失效、native handle 延迟销毁；Texture/Mesh AssetLease pin、suspend flush、显式 drain 与 shutdown hard drain 已贯通 | [Rendering](rendering.md) · [Resources](resources.md) · [ADR 0016](adr/0016-asset-ownership-and-retirement.md) · BgfxRetirementTimeline/AssetGpuRetirement tests |
| 2D-SPRITE-BATCH | 任意有效 packet-local texture ref；bgfx 按最终渲染顺序建立连续 ref batch，解析 device binding 后逐 batch 绑定纹理；双纹理 sample 保持透明排序 | [2D](game-2d.md) · BgfxSprite2DGeometryTests |
| 2D-SPRITE-ANIM | SpriteAnimationClip cooked payload/typed validation/recipe；SpriteAnimator2D Once/Loop/PingPong、暂停、倍速与大 delta；sample 双纹理 `Idle -> Walk -> HitWall`；该 v1 payload 契约已由 2D-ANIM-EVENTS 的 schema v2（notify events）替代 | [2D](game-2d.md) · [资源](resources.md) · SpriteAnimationClipPayloadTests · SpriteAnimator2DTests |
| 3D-001 | multi-mesh 产品 E2E：双 mesh glTF fixture → cook → 两 StaticMesh upload/bind（当时 meshKey 1/2，N15 已替换为 registry allocator）→ Prefab 每节点 resolve → extract/draw → ledger 归零；`tina_sample_3d` 300 帧 `multiMesh=true` | [3D](game-3d.md) |
| TASK-001 | Desktop `resolveDesktopTaskSystemParams`：交互默认 `max(1, hw-1)` CPU worker；`createBoundedTaskSystem(cpu=0)` IO-only 仍 NotSupported；BoundedTaskSystem 单测覆盖 | [Task](task-system.md) · ADR 0017 |
| CLEAN-001 | 删除 vcpkg `legacy` feature 及 EnTT/GLM/spdlog/utfcpp 死依赖声明；preset 无引用 | [dependencies](dependencies.md) |
| CLEAN-002 | 删除无消费者 `StringUtils.hpp`（EASTL/utfcpp）与 Clock/FrameTimer/FixedStepTicker compatibility；`SteadyMonotonicClock` 实现迁到 `MonotonicClock.cpp` | [core](core.md) |
| CLEAN-003 | miniaudio 实现 TU 与 CMake FATAL 文案不再暗示 Legacy ON 可运行 | [dependencies](dependencies.md) |
| CLEAN-004 | 删除 A1 Scene resolver/2D sample target alias、Core 私有转发头、assetc 旧参数、重复 GCC13 host wrapper 与无人使用的 Opaque3D unlit fallback；保留正式 CMake namespace、Legacy FATAL 以及跨设备/平台/backend 适配 | [架构](architecture.md) · [公开 API](public-api.md) · [构建](building.md) |
| TEST-002 | product-2d 同轮：UI/RuntimeUI/bridge/FreeType/Physics2D/Audio/miniaudio/Asset 测试 + sample 300 帧；`productGate=bgfx-physics-freetype-audio`；schema 23 继承双灯双遮挡、authored/committed/culled=`3/2/1`、soft/hard 与 normal-map differential、3份 Texture owner/retirement handoff，并增加 UI Flow base/pause Screen push/pop、Back/Confirm/Menu、单用户设备 revision 与 Pause 提示；同时保留 Dark→Light→Dark 与 Scene Explorer TreeView；脚本 `tools/windows/RunProduct2dGate.ps1` | [building](building.md) · [Windows 证据](evidence/m12-evidence-windows.md) |
| TEST-003 | product-3d 同轮：Core/Scene/AssetFormat/Asset/bgfx Render/UI/RuntimeUI/bridge/FreeType/UIA 的11个 GoogleTest executable + sample 300 帧；2026-08-15 通过的 schema 16 统一验证 glTF/PBR、skin witness、Animator/GPU skinning、IBL、CSM/Spot/Point shadow、surface aspect、responsive UI、lighting snapshot、资源 retirement、packet-local resolver、retained UI，以及4 Material、双 static transparent witness、统一排序 checksum 与 transparency on/off RGB L1；脚本 `tools/windows/RunProduct3dGate.ps1` | [3D](game-3d.md) · [测试](testing.md) |
| ASSET-001 | glTF 外部 URI root containment/`..`/scheme 拒绝 + 64MiB 上限；`tina_sample_3d` 上传/绑定 Cooked Texture2D 到 materialKey；路径逃逸单测 | [3D](game-3d.md) · GltfCookTests |
| UI-001 | ProgressBar/RadioButton 已接入 product-2d；UI/Runtime UI/Render bridge 直接测试、结构化输出与 Windows client-area 视觉证据成立 | [UI](ui.md) · [Windows 证据](evidence/m12-evidence-windows.md) |
| UI-005 | ScrollView、Dropdown/Popup、虚拟 ListView/TreeView 全部完成；List/Tree 100k logical item、固定 row pool、warmup 后零稳态分配、输入/semantics 与 Runtime facade 门禁通过 | [UI](ui.md) · `UIListViewTests` · `UITreeViewTests` · Runtime UI tests |
| DOC-001 | 文档职责与任务体系重组完成；本地链接、configure/build preset、CMake target、Markdown fence 与格式扫描通过；UI 绘制链路和控件矩阵已归档 | [文档索引](README.md) · [Roadmap](roadmap.md) · [UI](ui.md) |
| UI-THEME-AB | 薄 `UITheme` token；`UIBoxPaint` 亮/暗边 + 可选 shadow；sample_2d 设置面板 elevation；hex `rgb`/`argb`；`UIThemeTests` | [UI](ui.md) |
| UI-THEME-DEFAULT | Context/Runtime phase facade `productTheme`/`setProductTheme`；`create*` 自动 apply `make*Chrome`；既有继承属性事务换肤；属性级局部 override；Button focus border 与 pressed depth；`makeLightProductTheme`；`UIThemeTests` | [UI](ui.md) |
| UI-SHOWCASE | 独立 `tina_sample_ui_showcase` 同屏展示20个成熟控件、Panel/elevation/色板/状态栏、Dark/Light 实时换肤、集合导航/滚动与 Slider→ProgressBar 联动；dark/light 自动 smoke 和 Win32 pointer interaction/capture 通过 | [UI](ui.md) · [测试](testing.md) |
| UI-PRODUCT-2D-THEME | product-2d Theme Button 提交 pending intent 并在 `updateUI()` 事务换肤；标准控件继承产品 chrome，Panel/标题局部层级随主题重算；当前 schema 29 仍自动 Dark→Light→Dark 与 Scene Explorer TreeView 后回到 Dark | [UI](ui.md) · [2D](game-2d.md) · [测试](testing.md) |
| UI-PRODUCT-3D-THEME | `Product3DUI` 独立 root；Theme Button、Auto Rotate Checkbox、Rotation Speed Slider、Frame ProgressBar、Asset ListView、Scene TreeView 驱动实际 3D 状态；标准 chrome 继承、Panel/标题局部层级集中换肤；right rail/collection/footer 使用响应式 End/Stretch；2026-08-15 schema 16 继续通过 Dark→Light→Dark、集合导航与暗/亮 FreeType client capture 契约 | [UI](ui.md) · [3D](game-3d.md) · [测试](testing.md) |
| UI-LAYOUT-PADDING | `UILayoutStyle::padding` 同时约束 text auto-size 与 committed content box；多行、paint、TextEdit caret/selection/pointer mapping 共用 committed origin；对应 UI 回归通过 | [UI](ui.md) · `UITextTests` / `UITextEditTests` |
| DOC-002 | `tools/docs/CheckDocs.ps1`：docs 本地链接、cmake configure/build preset、`--target` 名、Legacy 产品文案软警告；不扫 out/build/thirdparty | [building](building.md) · [testing](testing.md) |
| RUNTIME-001 | `GameStateStack` + commands + 唯一 commit；**policy 向下阻断**（fixed/frame/render/UI 自顶向下 `forEachDispatch`）；enter 失败丢 candidate；`GameStateStackTests` / policy dispatch 单测 | [gameplay](gameplay.md) · ADR 0014 |
| RUNTIME-002 | `FramePin`/`FramePinSink`、`RenderFramePacket`、`CpuSubmissionCompletionLedger`；EngineHost submit/present 挂 pin 并在 present/skip 后 complete；shutdown abandon；`FramePinPacketTests` | [rendering](rendering.md) · ADR 0016 |
| PERF-001 | ADR 0018 Accepted；`tools/bench` → `tina_bench` schema v1；workload `null_runtime_frames`；JSON fingerprint/checksum/p50/p95/p99；共享机 `conclusion=provisional`；固定 hard-gate 机与多进程 MAD 后置 | [performance-memory](performance-memory.md) · ADR 0018 |
| RUNTIME-001-INT | Null Host 集成：base `requestPush` overlay（block fixed/frame below）→ overlay `requestPop` → base 恢复；enter 失败无 `onExit`；`GameStateStackIntegrationTests` | [gameplay](gameplay.md) · ADR 0014 |
| RUNTIME-001-SAMPLE | `tina_sample_2d` 收尾自动 pause overlay（≥60 帧）：push/pop + policy block；JSON `pauseOverlay*`；短 smoke 跳过 | [2D](game-2d.md) · ADR 0014 |
| UI-002-SPI | `UIAccessibilityTree`/`IUIAccessibilityProvider`/`UIAccessibilityProbeProvider`：从 committedSemantics 发布 role/name/state/range；stale node 拒绝；`UIAccessibilityTests`；`tina_sample_2d` 产品 JSON `accessibility*` | [UI](ui.md) · [2D](game-2d.md) |
| UI-002-UIA-MAP | 可选 `tina_ui_uia`：UIA 形属性映射 + factory 零 COM；`tina_ui_uia_tests` | [UI](ui.md) |
| UI-002-HWND | `WindowsUiaHostBridge`：SetWindowSubclass + WM_GETOBJECT + fragment root/children `IRawElementProviderSimple`；HostBridge 单测 | [UI](ui.md) |
| UI-002-HOST | `TINA_HAS_UI_UIA`：`EngineHost` 从 surface lease 取 Win32 HWND，startup/每帧 layout 后 publish `committedSemantics`；shutdown detach | [UI](ui.md) · Runtime |
| UI-002-ACTION | 平台中立 `UIAccessibilityAction` + owner-thread `context.input().performAccessibilityAction()`：Focus/Invoke/Toggle/SetRangeValue/SetTextValue 校验 generation、enabled 与控件 kind；Button/selection callback 仍走正常 action 路径，不兼容或 stale target fail closed（`7d84ae67`） | [UI](ui.md) · `UIAccessibilityTests` |
| UI-002-CONTROL-PATTERNS | Windows UIA provider 完成 Button Invoke、Checkbox/RadioButton Toggle、Slider/ProgressBar RangeValue 与 TextEdit Value pattern；跨线程调用经 HWND 消息回到 UI owner thread，旧 provider snapshot 在 republish/detach 后返回 element unavailable（`e82f1aaf`） | [UI](ui.md) · `WindowsUiaAccessibilityTests` |
| UI-002-EXTERNAL-HWND-GATE | `RunUi002UiaGate.ps1` 配置/构建 UIA+bgfx+FreeType 图并直接运行测试，再由独立 `System.Windows.Automation` client 连接真实 `tina_sample_ui_showcase` HWND，验证 Tina Framework、唯一 RuntimeId/AutomationId、fragment 父链、动态属性 republish、Invoke/Toggle/RangeValue/Value/Focus action 与 `WM_CLOSE` 正常退出，输出 schema 1 JSON；该自动 gate 明确不替代 Narrator/Inspect 人工金标（`e82f1aaf`） | [测试](testing.md) · `tools/windows/RunUi002UiaGate.ps1` |
| API-CLEAN-POLICY | `blocksUIUpdateBelow` 替换误导名 `blocksUIInputBelow`；文档/sample/tests 同步 | [gameplay](gameplay.md) |
| API-CLEAN-ASSET-READY | 删除 `AssetLogicalState Ready` 别名（仅 ReadyCpu/ReadyGpu） | [resources](resources.md) |
| API-CLEAN-SCENE-KEYS | Scene `fixture*Key` → `meshKey`/`materialKey`/`spriteKey`；注释改为 bind-table 语义 | [Scene](scene-ecs.md) · [3D](game-3d.md) |
| API-CLEAN-UI-STATS | `UIContextStatistics` phase dirty 单源：`structureDirty`/`layoutDirty`/`hitDirty`/`paintDirty`/`semanticsDirty` 由内部 `phaseDirty` mask 派生；去掉 public `dirty` 与并行 bool 双轨 | [UI](ui.md) |
| RENDER-LEDGER-SPI | `ISubmissionCompletionLedger` + `CpuSubmissionCompletionLedger` 实现；`RenderFramePacket` 走接口 | [rendering](rendering.md) · ADR 0016 |
| RENDER-LEDGER-INJECT | Host 持 `unique_ptr<ISubmissionCompletionLedger>`；可选 `createSubmissionCompletionLedger` 仅替换 CPU 记账实现；所有 composition 都在 present-return complete；mock 多态单测 | [rendering](rendering.md) · ADR 0016 |
| RUNTIME-001-INPUT | `blocksGameplayInputBelow`：下层 fixed/frame 使用空 action snapshot；`gameplayInputBlockedForDepth` + unit tests | [gameplay](gameplay.md) · ADR 0014 |
| RENDER-3D-TEX | Opaque3D unlit 采样 materialKey 绑定贴图（shader `s_texColor` + default white）；关闭「bind 不 draw」假完成 | [3D](game-3d.md) |
| TEST-001-GCC13-NULL | Docker Desktop + `linux-gcc13-vnext`：`tina_tests`/`tina_ui_tests`(255)/`tina_runtime_ui_tests`(83)/bridge(13)/`tina_sample_null` 300 帧；vcpkg baseline 与仓库一致；`artifacts/gates/test-001-linux-gcc13-null.json` | [Linux 证据](evidence/m12-evidence-linux.md) |
| TEST-001-GCC13-PLATFORM | Docker + Xvfb + `linux-gcc13-vnext-platform`：`tina_tests` + `tina_platform_glfw_tests`(34/34) + `tina_sample_platform --frames=60`；`artifacts/gates/test-001-linux-gcc13-platform.json` | [Linux 证据](evidence/m12-evidence-linux.md) |
| SAMPLE-FACADE-001 | `tina_sample_2d` 去手写 factories：`Desktop::CreateEngine(options)` + `wrapWindowSurfaceRenderDevice`；`DeviceCapture` 抽出 sample helper | [2D](game-2d.md) · [public-api](public-api.md) |
| SAMPLE-FACADE-3D | `tina_sample_3d` 同样走 `Desktop::CreateEngine` + wrap；`DeviceCapture.hpp` 抽出；去掉 Glfw/bgfx 直链 factories | [3D](game-3d.md) |
| TEST-001 | Docker Desktop 复验 tip：GCC13 Null + Platform/GLFW(Xvfb) + Clang22 Null + Clang22 ASan/UBSan/LSan；`libclang-rt-22-dev` 修 sanitizer 链接；证据 JSON 见 `artifacts/gates/test-001-linux-*.json` | [Linux 证据](evidence/m12-evidence-linux.md) |
| TEST-001-CLANG22-NULL | `linux-clang22-vnext`：tests/ui/runtime_ui/bridge + sample_null 300；`test-001-linux-clang22-null.json` | [Linux 证据](evidence/m12-evidence-linux.md) |
| TEST-001-CLANG22-SAN | `linux-clang22-vnext-sanitize`：同上 + ASan/UBSan/LSan；`test-001-linux-clang22-sanitize.json` | [Linux 证据](evidence/m12-evidence-linux.md) |
| UI-003-MAP | `buildUIDisplayList` content-scale 映射：logical 100×100 → fb 100/150/200 时 rect 与 clip 像素确定性；`UIRenderDisplayListTest.ContentScale*` | [UI](ui.md) · bridge tests |
| UI-003-VIS | `RunUi003VisualGate.ps1`：title/settings/progress/Scene Explorer/playfield ROI 指纹 + 坐标一致性 + 可选 baseline 比对（`tools/windows/baselines/ui-003-sample2d-960x540.json`）；blankLike 排除；`artifacts/gates/ui-003-visual-*.json` | [UI](ui.md) · CaptureSampleWindow |
| UI-003-SIZE | `tina_sample_2d --width/--height` + `RunUi003SizeMatrix.ps1`：content-scale-like 逻辑窗口 960/1200/1440/1280/1920；分尺寸 ROI baseline；`artifacts/gates/ui-003-size-matrix-*.json` | [UI](ui.md) · sample_2d |
| UI-003-METRICS | sample JSON：`logicalPixel*` / `framebufferPixel*` / `contentScale*`；gate 断言 `fb ≈ logical * scale` 与 capture client 一致 | [UI](ui.md) · sample_2d |
| UI-003-FONT-FP | `RunUi003VisualGate.ps1`：`fontFingerprint`（resolvedPath/source/fileName/sizeBytes/sha256/envTinaUiFontPath/freeTypeLikelyOn/identity）写入 gate JSON 与 baseline；baseline 含 fingerprint 时 mismatch **fail**（可选 `-AllowFontFingerprintMismatch` 仅跳过 ROI）；分尺寸 baseline schema 3 | [UI](ui.md) · baselines |

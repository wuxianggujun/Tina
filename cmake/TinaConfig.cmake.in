@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/TinaTargets.cmake")

# 检查所需的组件是否都已安装
check_required_components(Tina)

# 设置包含目录
set_and_check(TINA_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

# 导出库目标
set(TINA_LIBRARIES Tina::Core) 
#include <tina/asset/AssetFrameResourceResolver.hpp>

#include <type_traits>

static_assert(std::is_trivially_copyable_v<Tina::Asset::AssetFrameResourceResolver>);
static_assert(sizeof(Tina::Asset::AssetFrameResourceResolver) > 0);
